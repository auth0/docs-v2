---
title: SecurityConfig.java
---

```java SecurityConfig.java lines highlight={}
package com.auth0.example.security;

    import org.springframework.context.annotation.Bean;
    import org.springframework.context.annotation.Configuration;
    import org.springframework.security.config.annotation.web.builders.HttpSecurity;
    import org.springframework.security.web.SecurityFilterChain;

    import static org.springframework.security.config.Customizer.withDefaults;

    /**
     * Spring Securityを使用してAPIエンドポイントへのアクセスを制限するようにアプリケーションを設定します。
     */
    @Configuration
    public class SecurityConfig {

@Bean
public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
    /*
    ここでエンドポイントに必要なセキュリティを設定し、JWTの検証を使用してアプリが
    OAuth2リソースサーバーとして機能するよう構成します。
    */
    return http
            .authorizeHttpRequests((authorize) -> authorize
                .requestMatchers("/api/public").permitAll()
                .requestMatchers("/api/private").authenticated()
                .requestMatchers("/api/private-scoped").hasAuthority("SCOPE_read:messages")
            )
            .cors(withDefaults())
            .oauth2ResourceServer(oauth2 -> oauth2
                .jwt(withDefaults())
            )
            .build();
}
    }
```