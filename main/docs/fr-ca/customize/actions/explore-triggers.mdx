---
title: "Explorer les déclencheurs"
'description': "En savoir plus sur les déclencheurs, qui sont des événements qui déclenchent automatiquement une Action lorsqu’une opération spécifique, telle que l’ouverture d’une session par un utilisateur, se produit au moment de l’exécution."
---

Les déclencheurs sont des points dans le processus d’Auth0 où des actions peuvent être ajoutées pour répondre aux demandes de personnalisation. Plusieurs actions, exécutées les unes après les autres, peuvent être associées à un déclencheur. Les déclencheurs peuvent être synchrones (bloquant) ou asynchrones (non bloquant).
Chaque déclencheur dispose d’objets et d’API spécifiques. Ils peuvent varier selon votre mode de connexion (par ex. sans mot de passe).

Pour explorer les objets de référence et les plans pour des Actions spécifiques pour un déclencheur, sélectionnez le déclencheur ci-dessous, en notant la définition Auth0 d’une [connexion sans mot de passe](/docs/fr-ca/authenticate/passwordless).

#### Inscription et connexion

Lorsqu’un utilisateur est ajouté à une base de données ou une connexion sans mot de passe (Inscription), ou dans le cadre d’un processus d’authentification (Connexion).

| Déclencheur | Description |
| --- | --- |
| `pre-user-registration` | Se déclenche avant que l’utilisateur soit créé. |
| `post-user-registration` | Se déclenche de manière asynchrone après la création d’un utilisateur. |
| `post-login` | Se déclenche après l’authentification de l’utilisateur, mais avant l’émission d’un jeton. |

#### Notifications MFA

Lorsque vous utilisez le SMS comme l’un des authentification multifacteur (<Tooltip href="/docs/fr-ca/glossary?term=multifactor-authentication" tip="Authentification multifacteur (MFA)
Processus d’authentification de l’utilisateur qui utilise un facteur en plus du nom d’utilisateur et du mot de passe, tel qu’un code par SMS." cta="Voir le glossaire">MFA</Tooltip>) pour vous authentifier ou pour configurer un fournisseur personnalisé.

| Déclencheur | Description |
| --- | --- |
| `send-phone-message` | Se déclenche lorsqu’un fournisseur personnalisé est utilisé pour envoyer les messages liés à l’inscription et au processus du défi-réponse. |

#### Réinitialisation du mot de passe

Lorsque la réinitialisation du mot de passe est lancée.

| Déclencheur | Description |
| --- | --- |
| `post-change-password` | Se déclenche après la modification du mot de passe. |
| `password-reset-post-challenge` | Se déclenche après le premier défi-réponse et avant la réinitialisation du mot de passe. |

#### machine-machine

Lorsqu’une application, et non un utilisateur, est authentifiée et autorisée à utiliser vos API.

| Déclencheur | Description |
| --- | --- |
| `credentials-exchange` | Se déclenche avant qu’un jeton d’accès soit émis en retour. |