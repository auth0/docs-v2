---
og:description: Learn how to export your Auth0 user data and import it into Watson
  Campaign Automation.
og:image: https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
og:title: Exportation des données utilisateur vers Watson Campaign Automation.
og:url: https://auth0.com/docs/
permalink: watson-campaign-automation
title: Exportation des données utilisateur vers Watson Campaign Automation.
twitter:description: Learn how to export your Auth0 user data and import it into Watson
  Campaign Automation.
twitter:title: Exportation des données utilisateur vers Watson Campaign Automation.
---

Dans cet article, vous apprendrez à exporter les données d’utilisateur depuis Auth0 vers un fichier CSV et à les importer dans le tableau de bord de Watson Campaign Automation.

## Créer un fichier de données utilisateur

Start by navigating to the [Extensions](https://manage.auth0.com/#/extensions) section of the Dashboard and open the **User Import / Export Extension**. On the extension page, select **Export** from the menu.

Ensuite, définissez le **format d’exportation** sur le format de fichier requis. Watson Campaign Automatisation accepte les importations de fichiers au format CSV. Sélectionnez donc l’option `Tab Separated Value file (*.csv)`.

En haut de la section **Champs**, indiquez un **Champ utilisateur** et **Nom de colonne** pour chaque attribut d’utilisateur à inclure dans l’exportation. Par exemple :

<table class="table"><thead>
<tr>
<th>User Field</th>
<th>Column Name</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>email</code></td>
<td>Email Address</td>
</tr>
<tr>
<td><code>created_at</code></td>
<td>Created At</td>
</tr>
<tr>
<td><code>given_name</code></td>
<td>First Name</td>
</tr>
<tr>
<td><code>family_name</code></td>
<td>Last Name</td>
</tr>
</tbody>
</table>

Après avoir ajouté les champs utilisateur, cliquez sur la touche **Exporter les utilisateurs** pour lancer l’exportation. Une fois l’exportation terminée, téléchargez le fichier CSV que vous utiliserez dans la section suivante.

## Importer un fichier de données utilisateur

Pour importer votre fichier CSV dans Watson Campaign Automation, procédez comme suit :

1. Log in to Watson Campaign Automation and navigate to **Data > Import New**.
2. On the **Select File** step, enter the new database's settings in the provided fields.
3. Select the option to upload a file from a local hard drive, then click **Browse** to select the file.
4. Set the file type to CSV (comma-separated values).
5. Click **Next** to proceed to the **Define Data Format** step.
6. Make sure your data was processed correctly, then click **Next**.
7. On the **Map Fields** step, choose which fields to include in the import as well as their types.
8. Click **Next** to proceed to the **Edit Field Settings** step.
9. Edit the settings on default or new fields as needed.
10. To start the import click **Next**.

C’est tout! Vous avez importé avec succès vos utilisateurs Auth0 dans Watson Campaign Automatisation.