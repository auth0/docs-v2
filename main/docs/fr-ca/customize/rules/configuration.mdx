---
og:description: Learn about the global configuration object in rules for storing commonly
  used values.
og:image: https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
og:title: Configurations de règles de stockage
og:url: https://auth0.com/docs/
permalink: configuration
title: Configurations de règles de stockage
twitter:description: Learn about the global configuration object in rules for storing
  commonly used values.
twitter:title: Configurations de règles de stockage
---



L’objet global `configuration` est disponible dans les règles pour stocker les valeurs couramment utilisées, telles que les URL. Les informations sensibles, telles que les identifiants ou les clés API, doivent être stockées via l’objet `configuration` et ne pas figurer dans le code de vos règles.

## Valeurs de configuration

Vous pouvez définir les valeurs de configuration dans vos [Réglages de règles](%24%7Bmanage_url%7D/#/rules/) dans Dashboard.

Pour modifier ou changer une valeur de clé de configuration, retirez les configurations choisies et remplacez-les par la valeur mise à jour. Vous devez avoir créé au moins une Règle pour voir la section de configuration; sinon, la démo des Règles s'affichera.

## Utiliser l’objet `configuration`.

Pour modifier ou changer une valeur de clé de configuration, retirez les configurations choisies et remplacez-les par la valeur mise à jour. Vous devez avoir créé au moins une Règle pour voir la section de configuration; sinon, la démo des Règles s’affichera.



L'exemple suivant est une Règle pour envoyer un message Slack lorsqu'un nouvel utilisateur vient de s'enregistrer. Le [lien de rappel HTTP Slack](https://api.slack.com/incoming-webhooks) est une valeur `configuration` définie avec la clé `SLACK_HOOK_URL`.

L’exemple suivant est une Règle pour envoyer un message Slack lorsqu’un nouvel utilisateur vient de s’enregistrer. [Slack Webhook](https://api.slack.com/incoming-webhooks) est une valeur `configuration` définie avec la clé `SLACK_HOOK_URL`.