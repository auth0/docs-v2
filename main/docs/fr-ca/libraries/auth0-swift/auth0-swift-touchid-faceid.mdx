---
description: ''
og:description: How to implement Touch ID / Face ID authentication with Auth0.swift.
og:image: https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
og:title: 'Auth0.swift : Authentification Touch ID / Face ID'
og:url: https://auth0.com/docs/
permalink: auth0-swift-touchid-faceid
title: 'Auth0.swift : Authentification Touch ID / Face ID'
twitter:description: How to implement Touch ID / Face ID authentication with Auth0.swift.
twitter:title: 'Auth0.swift : Authentification Touch ID / Face ID'
---

Voici le scénario : Après l’authentification de l’utilisateur, il est nécessaire de stocker ses informations d’identification et de les utiliser tant qu’elles restent valides. Lorsque ces informations expirent, il est recommandé de les renouveler à l’aide du `refreshToken`, afin d’éviter de rediriger l’utilisateur de nouveau, vers la page de connexion. Au lieu de procéder automatiquement, vous demanderez à l’utilisateur de valider son identité via son empreinte digitale ou la reconnaissance faciale.

You will be using the [Credentials Manager](https://github.com/auth0/Auth0.swift/blob/master/Auth0/CredentialsManager.swift) utility in [Auth0.swift](https://github.com/auth0/Auth0.swift/) to streamline the management of user credentials and perform biometric authentication.

## Démarrage

Tout d’abord, importez le module `Auth0` :

`import Auth0`

### Gestionnaire des identifiants

Avant de récupérer vos identifiants, vous pouvez activer l’authentification biométrique (Face ID ou Touch ID) disponible sur votre appareil iOS.

Commencez par configurer le Gestionnaire des informations d’identification. Activez ensuite la biométrie. Vous pouvez également transmettre un titre à afficher dans l’invite.

```swift lines
let credentialsManager = CredentialsManager(authentication: Auth0.authentication())
credentialsManager.enableBiometrics(withTitle: "Touch ID / Face ID Login")
```

We strongly recommend that you add the [NSFaceIDUsageDescription](https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CocoaKeys.html#//apple_ref/doc/uid/TP40009251-SW75) setting to your project's `Info.plist` to display a reason for using Face ID. In some cases, if you do not provide a description string and the user attempts Face ID authentication, the user's attempt may fail.

```xml lines
...
<key>NSFaceIDUsageDescription</key>
<string>Reason why we use Face ID here</string>
...
```

### Connexion

Present the <Tooltip tip="Universal Login: Your application redirects to Universal Login, hosted on Auth0's Authorization Server, to verify a user's identity." cta="View Glossary" href="/docs/glossary?term=Universal+Login">Universal Login</Tooltip> page and, upon successful authentication, pass the credentials to the Credentials Manager.

```swift lines
Auth0
    .webAuth()
    .scope("openid profile offline_access")
    .audience("https://{yourDomain}/userinfo")
    .start {
        switch $0 {
        case .failure(let error):
            // Handle error
        case .success(let credentials):
            // Store credentials securely with the Gestionnaire des identifiants
            credentialsManager.store(credentials: credentials)
        }
}
```

### Renouveler les informations d’identification de l’utilisateur

Lorsque vous devez renouveler les informations d’identification de l’utilisateur, vous pouvez appeler la méthode `credentials` du Gestionnaire d’informations d’identification.

```swift lines
credentialsManager.credentials { error, credentials in
    guard error == nil, let credentials = credentials else {
        // Handle error
        // Fallback to login screen
    }
    // Continue routing the user as authentication was successful
}
```

Il n’est pas nécessaire de stocker manuellement les nouvelles informations d’identification, car cette opération est gérée par le Gestionnaire d’informations d’identification lors du renouvellement.

## Prochaines étapes

You can download a sample project and follow the instructions in the iOS quickstart section on [Touch ID / Face ID in iOS](/docs/quickstart/native/ios-swift).