---
title: "ScreenMembersOnMfaVoiceChallenge"
---

Interface étendue des membres de l’écran pour l’écran de défi vocal MFA.

```ts Example
export interface ScreenMembersOnMfaVoiceChallenge extends ScreenMembers {
  /**
   * Données d'écran supplémentaires propres au défi vocal d’AMF.
   */
  data: {
    /**
     * Le numéro de téléphone auquel le code vocal a été envoyé.
     */
    phoneNumber?: string;

    /**
     * Indique si l'option de mémorisation de l'appareil doit être affichée.
     */
    showRememberDevice?: boolean;

    /**
     * Indique si le lien pour passer à la vérification par SMS doit être affiché.
     */
    showLinkSms?: boolean;
  } | null;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="captcha" type={<span><a href="/docs/fr-ca/libraries/acul/react-sdk/API-Reference/Types/interfaces/CaptchaContext">CaptchaContext</a></span>} />

<ParamField body="captchaImage" type="string" />

<ParamField body="captchaProvider" type="string" />

<ParamField body="captchaSiteKey" type="string" />

<ParamField body="data" type="phoneNumber?">
  Données d’écran supplémentaires propres au défi vocal d’AMF.

  <div id="type-declaration">
    #### Déclaration de type
  </div>

  &#123; `phoneNumber?`: `string`; `showLinkSms?`: `boolean`; `showRememberDevice?`: `boolean`; &#125;

  <div id="phonenumber">
    #### phoneNumber?
  </div>

  Le numéro de téléphone auquel le code vocal a été envoyé.

  <div id="showlinksms">
    #### showLinkSms?
  </div>

  Indique s’il faut afficher le lien pour passer à la vérification par SMS.

  <div id="showrememberdevice">
    #### showRememberDevice?
  </div>

  Indique s’il faut afficher l’option de mémorisation de l’appareil.

  `null`
</ParamField>

<ParamField body="isCaptchaAvailable" type="boolean" />

<ParamField body="links" type="Record" />

<ParamField body="name" type="string" />

<ParamField body="texts" type="Record" />