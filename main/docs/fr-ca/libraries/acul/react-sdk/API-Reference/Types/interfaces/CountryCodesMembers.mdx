---
title: "CountryCodesMembers"
---

```ts Example
export interface CountryCodesMembers {
  available: AvailableCountryCode[] | null;
  recommended: string | null;
}
```

## Propriétés {#properties}

<ParamField body="available" type={<span><a href="/docs/fr-ca/libraries/acul/react-sdk/API-Reference/Types/interfaces/AvailableCountryCode">AvailableCountryCode</a>[]</span>}>
  Pays offerts pour la saisie du numéro de téléphone, triés selon l&#39;étiquette (`label`) localisée. Vaut `null` sauf si la configuration de rendu de l&#39;écran les demande.
</ParamField>

<ParamField body="recommended" type="string">
  Code ISO 3166-1 alpha-2 à présélectionner, et non un index dans `available`. Vaut `null` s&#39;il n&#39;est pas résolu.
</ParamField>