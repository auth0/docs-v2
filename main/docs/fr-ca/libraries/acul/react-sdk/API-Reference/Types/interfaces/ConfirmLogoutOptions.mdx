---
title: "ConfirmLogoutOptions"
---

Options pour confirmer le logout

```ts Example
export interface ConfirmLogoutOptions extends CustomOptions {
  /** L'état de la transaction d'authentification */
  action: 'accept' | 'deny';
}
```

## Indexable {#indexable}

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

## Propriétés {#properties}

<ParamField body="action" type="&#x22;accept&#x22;">
  L’état de la transaction d’authentification
</ParamField>