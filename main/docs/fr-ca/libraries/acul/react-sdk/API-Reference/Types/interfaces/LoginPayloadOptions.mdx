---
title: "LoginPayloadOptions"
---

Options pour effectuer des opérations de connexion

```ts Example
export interface LoginPayloadOptions {
  /** L'identifiant de l'utilisateur, dans son orthographe d'origine. Facultatif si `identifier` est fourni. */
  username?: string;
  /** L'identifiant à utiliser pour la connexion. Facultatif si `username` est fourni — `identifier` a priorité si les deux sont fournis. */
  identifier?: string;
  /** Ce que contient `identifier` (ou `username`) — provient généralement de `screen.data.activeIdentifierType`. */
  identifierType?: IdentifierType;
  /** Le mot de passe servant à l'authentification */
  password: string;
  /** Pays ISO 3166-1 alpha-2 pour un identifiant téléphonique, provenant d'un `code` de `countryCodes.available`. */
  phoneCountryCode?: string;
  /** Valeur de CAPTCHA facultative, si requise */
  captcha?: string;
  /** Toute option personnalisée supplémentaire */
  [key: string]: string | number | boolean | undefined;
}
```

## Indexable {#indexable}

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Toute autre option personnalisée supplémentaire

## Propriétés {#properties}

<ParamField body="captcha?" type="string">
  Valeur de CAPTCHA facultative, si requise
</ParamField>

<ParamField body="identifier?" type="string">
  L&#39;identifiant utilisé pour la connexion — courriel, nom d&#39;utilisateur ou numéro de téléphone, selon `identifierType`. Facultatif si `username` est fourni; si les deux sont fournis, `identifier` l&#39;emporte.
</ParamField>

<ParamField body="identifierType?" type={<span><a href="/docs/fr-ca/libraries/acul/react-sdk/API-Reference/Types/type-aliases/IdentifierType">IdentifierType</a></span>}>
  Ce que contient `identifier` (ou `username`). Généralement lu à partir de `screen.data.activeIdentifierType`. Omettez ce paramètre pour soumettre l&#39;identifiant sans type.
</ParamField>

<ParamField body="password" type="string">
  Le mot de passe servant à l&#39;authentification
</ParamField>

<ParamField body="phoneCountryCode?" type="string">
  Le pays au format ISO 3166-1 alpha-2 pour un identifiant téléphonique, provenant d&#39;un `code` de `countryCodes.available`. Requis lorsque `identifierType` vaut `phone`. Transmettez le numéro national — l&#39;indicatif est ajouté côté serveur.
</ParamField>

<ParamField body="username?" type="string">
  L&#39;identifiant de l&#39;utilisateur, dans son orthographe d&#39;origine. Facultatif si `identifier` est fourni — requis sinon.
</ParamField>