---
og:description: Describes how to disable Refresh Token rotation.
og:image: https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
og:title: リフレッシュトークンのローテーションを無効にする
og:url: https://auth0.com/docs/
permalink: disable-refresh-token-rotation
title: リフレッシュトークンのローテーションを無効にする
twitter:description: Describes how to disable Refresh Token rotation.
twitter:title: リフレッシュトークンのローテーションを無効にする
---

DashboardまたはManagement APIを使用して各アプリケーションのリフレッシュトークンのローテーションを無効にできます。

## Dashboardでの無効化

1. [Auth0 Dashboard](%24%7Bmanage_url%7D)から**［Applications（アプリケーション）］>［Applications（アプリケーション）］**に移動して、構成するアプリケーションを選択します。
2. ［Settings（設定）］タブの［Refresh Token Rotation（リフレッシュトークンのローテーション）］セクションにある［Allow Refresh Token Rotation（リフレッシュトークンのローテーションを許可する）］トグルを無効にします。
3. 画面の下部にある**［Save Changes（変更を保存）］**をクリックします。

## Management APIでの無効化

1. 以下のようにManagement APIを使用して、各アプリケーションのローテーションリフレッシュトークンのローテーションローテーションを無効にします。
2. ローテーションなしのリフレッシュトークン設定を以下のように構成します。