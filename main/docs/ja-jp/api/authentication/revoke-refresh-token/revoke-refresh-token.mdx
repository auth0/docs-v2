---
title: "リフレッシュトークンを失効させる"
description: "リフレッシュトークンを無効化し、再度使用できないようにします。"
---

import { ResponseSchema } from '/snippets/ja-jp/ResponseSchema.jsx';

## エンドポイント {#endpoint}

`POST /oauth/revoke`

このエンドポイントは、リフレッシュトークン が漏えいした場合に無効化するために使用します。

このエンドポイントの動作は、[Refresh Token Revocation Deletes Grant](/docs/ja-jp/secure/tokens/refresh-tokens/revoke-refresh-tokens#choose-whether-token-revocation-deletes-grants) トグルの設定によって異なります。
このトグルが有効になっている場合、失効リクエストを行うたびに、特定のトークンだけでなく、同じ認可グラントに基づくほかのすべてのトークンも無効になります。つまり、**同じユーザー、アプリケーション、audience に対して発行されたすべての リフレッシュトークン が失効します**。
このトグルが無効になっている場合は、リフレッシュトークン のみが失効し、グラントはそのまま維持されます。

## 注記 {#remarks}

* Client Secret を安全に保持できない非機密アプリケーション (たとえばネイティブアプリ) の場合、この エンドポイント では Client Secret を渡さないことが可能ですが、アプリケーション自体の `tokenEndpointAuthMethod` プロパティが `none` に設定されている必要があります。これは、UI ([Dashboard &gt; Applications &gt; Application Settings](https://manage.auth0.com/dashboard)) からでも、[Management API](/docs/ja-jp/api/management/v2/clients/patch-clients-by-id) を使用してでも設定できます。

## 詳しく見る {#learn-more}

* [リフレッシュトークン](/docs/ja-jp/secure/tokens/refresh-tokens)

## Body パラメータ {#body-parameters}

<ParamField body="client_id" type="string" required>
  アプリケーションの `client_id`。
</ParamField>

<ParamField body="client_assertion" type="string">
  アプリケーションの認証情報で署名したアサーションを含む JWT。アプリケーションの認証方式が Private Key JWT の場合に必要です。
</ParamField>

<ParamField body="client_assertion_type" type="string">
  値は `urn:ietf:params:oauth:client-assertion-type:jwt-bearer` です。アプリケーションの認証方式が Private Key JWT の場合に必要です。
</ParamField>

<ParamField body="client_secret" type="string">
  アプリケーションの `client_secret`。アプリケーションの認証方式が Client Secret Basic または Client Secret Post の場合に必要です。特に、Regular Web Applications **のみ** で必要です。
</ParamField>

<ParamField body="token" type="string" required>
  失効するリフレッシュトークン。
</ParamField>

## レスポンススキーマ {#response-schema}

<ResponseSchema>
  成功した 200 レスポンスには本文がありません。エラーレスポンスは次の形式を使用します。

  <ResponseField name="error" type="string">
    エラーコード。
  </ResponseField>

  <ResponseField name="error_description" type="string">
    エラーの説明。
  </ResponseField>
</ResponseSchema>

## レスポンスメッセージ {#response-messages}

| Status  | Description    |
| ------- | -------------- |
| 200     |  (レスポンスボディなし)  |
| 400     | 不正なリクエスト       |
| 401     | 認証されていません      |
| default | 予期しないエラー       |