---
title: "チームアクティビティ"
'description': "チームアクティビティを使用すると、チーム所有者はチームメンバーによって生成されたイベントログを表示および監査できます。"
---

チームアクティビティを使用すると、チーム所有者は、Auth0アカウントで実行されたメンバーアクティビティに関連付けられた監査ログを表示できます。

チームアクティビティレポートは、Teams Dashboardの **［Reports（レポート）］** セクションにあります。

チームアクティビティには、イベントを生成したメンバー、イベントの種類、およびイベントが生成された日付の概要ビューが表示されます。

**［View Details（詳細を表示）］** を選択すると、イベントの説明、イベントのステータス、イベントをトリガーしたユーザーのIPアドレス、ユーザーエージェントの詳細などの詳細情報を確認できます。

## イベントタイプ

チームアクティビティイベントタイプについては以下で説明します。

| イベントタイプ | 説明 |
| --- | --- |
| Team Invitation（チーム招待） | チームメンバーの招待が作成されました<br/>チームメンバーの招待が削除されました |
| Team Member（チームメンバー） | チームメンバーが削除されました<br/>チームメンバーのロールが更新されました |
| Security Policy（セキュリティポリシー） | セキュリティポリシーが更新されました<br/>ソーシャル接続が更新されました |
| Team Setting（チーム設定） | チーム設定が更新されました |

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">

今後、Auth0 Dashboard、Teams Dashboardや他のイベントに対し、ダッシュボートユーザー認証のサポートが提供される予定です。

</Callout>

## イベントスキーマ

チームアクティビティイベントスキーマについては以下で説明します。

```json lines
{  
    "id": "string", 
    "team_id": "string",
    "user_id": "string",
    "type": "string",
    "description": "string",
    "status": "string",
    "details": "object",
    "ip": "string",
    "hostname": "string",
    "user_agent": "string",
    "version": "string",
    "created_at": "date",
    "team_slug": "string",
    "user": "object"
}
```

| 名前 | タイプ | 説明 |
| --- | --- | --- |
| `id` | 文字列 | イベントログの一意のIDです。 |
| `team_id` | 文字列 | 一意のチームIDです。 |
| `user_id` | 文字列 | イベントを生成したユーザーの一意の識別子です。 |
| `type` | 文字列 | イベントの種類です。 |
| `description` | 文字列 | イベントの種類の要約です。 |
| `status` | 文字列 | イベントのステータスです。可能な値は`Success`または`Failure`です。 |
| `details` | オブジェクト | ログに記録されたイベントの詳細です。 |
| `ip` | 文字列 | イベントログを生成したユーザーのIPアドレスです。 |
| `hostname` | 文字列 | イベントが実行されたAuth0アプリのFQDNです。 |
| `user_agent` | 文字列 | WebブラウザーやデバイスOSの種類とバージョンです。 |
| `version` | 文字列 | アクティビティログが生成されたスキーマのバージョンです。 |
| `created_at` | 日時 | イベントログが生成された日時です。 |
| `team_slug` | 文字列 | テキスト形式の一意のチーム識別子です。 |
| `user` | オブジェクト | 名前、アバターURL、チームメンバーシップのステータス（ブール値）を含みます。 |

## 監査ログの保存

チームアクティビティ監査ログの保持期間はプランによって異なります。詳細については、[価格](https://auth0.com/pricing)ページをご覧ください。