---
description: ''
og:description: Lock documentation on setting authentication parameters.
og:image: https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
og:title: Lock認証パラメーター
og:url: https://auth0.com/docs/
permalink: lock-authentication-parameters
title: Lock認証パラメーター
twitter:description: Lock documentation on setting authentication parameters.
twitter:title: Lock認証パラメーター
---

ログインを開始するときには、オプションオブジェクトにパラメーターを追加して送信することができます。下の例では、`'foo'`と等しい値のある`state`パラメーターを追加します。



上の例は、`https://${account.namespace}/authorize?state=foo&...`を使用してログインをトリガーすること類似しています。

パラメーターの`scope`、`device`、`nonce`と`state`に対応しています。

## 対応しているパラメーター

### scope{string}



スコープはさまざまな値に対応しています。JWTはすべてのAPI要求に送信されるため、できる限り小さくしておくことが賢明です。

Lockでデフォルトの`scope`値は、`openid profile email`です。この最小のスコープ値は、**Last time you logged in with（最終ログイン日）**機能を正しく動作させるためには必須です。



スコープの詳細については、[スコープに関するドキュメント](/apis/scopes)のページを参照してください。

#### 例：トークンを取得する

Lockでユーザーのプロファイルデータを取得できるトークンを受け取りたい場合には、`scope`パラメーターを追加する必要があります。



Lockには`connectionScopes`構成オプションもあり、任意の接続を特定してスコープを指定することができます。これは、スコープのセット（ダッシュボードで定義済み）で始めてから、後で特定の接続に追加の権限や属性を要求したい場合に便利です。詳細については、「[Lock構成オプション](/libraries/lock/lock-configuration)」をお読みください。

### state {string}

`state`パラメーターには任意の状態値が設定され、リダイレクト間で維持されます。[XSRF攻撃](http://en.wikipedia.org/wiki/Cross-site_request_forgery)の軽減や、認証プロセスが完了した後で必要になるかもしれない任意のコンテキスト情報（[リターンURL](/logout/redirect-users-after-logout)など）に便利です。カスタムの状態パラメーターが提供されない場合には、Lockが自動的に生成します。詳細については、「[Stateパラメーター](/attack-protection/state-parameters)」を参照してください。

### nonce {string}

`nonce`パラメーターはリプレイ攻撃を防ぐために使用され、カスタム値が提供されない場合にはLockが自動的に生成します。

### device {string}

`device`パラメーターは、認証を要求しているデバイスやブラウザーの名前を設定します。