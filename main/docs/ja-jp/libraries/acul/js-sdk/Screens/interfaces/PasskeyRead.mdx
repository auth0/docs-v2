---
title: "PasskeyRead"
---

```ts Example
export interface PasskeyRead {
  public_key: {
    challenge: string;
    allowCredentials?: AllowCredential[];
    rpId?: string;
  };
}
```

## プロパティ {#properties}

### public_key {#public_key}

> **public&#95;key**: `object`

<ParamField body="allowCredentials?" type={<span><a href="/docs/ja-jp/libraries/acul/js-sdk/Screens/interfaces/AllowCredential">AllowCredential</a>[]</span>} />

<ParamField body="challenge" type="string" />

<ParamField body="rpId?" type="string">
  パスキーの自動入力リクエストのスコープを限定する証明書利用者 (リライングパーティー) ID。省略可能で、省略した場合は既定の証明書利用者が使用されます。
</ParamField>