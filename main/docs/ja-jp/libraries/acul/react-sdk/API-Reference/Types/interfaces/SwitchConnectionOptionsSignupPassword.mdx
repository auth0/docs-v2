---
title: "接続切り替えオプション（新規登録／パスワード）"
---

```ts Example
export interface SwitchConnectionOptionsSignupPassword {
  connection: string;
  [key: string]: string | number | boolean;
}
```

## 添字指定可能 {#indexable}

[`key`: `string`]: `string` | `number` | `boolean`

## プロパティ {#properties}

<ParamField body="connection" type="string" required>
  signup-password 認証フロー中に切り替える接続の識別子です。
</ParamField>