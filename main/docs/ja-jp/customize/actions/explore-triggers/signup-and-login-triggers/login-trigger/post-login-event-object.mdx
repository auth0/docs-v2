---
title: "アクションのトリガー: ポストログイン - イベントオブジェクト"
description: "Auth0経由のシングルユーザーログインに関するコンテキスト情報を提供する、post-loginアクショントリガーのイベントオブジェクトについて説明します。"
---

| プロパティ | 説明 |
| --- | --- |
| `event.authentication`<br/>*（任意）* | ログインフロー中に取得した認証シグナルについての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li> `methods`*：オブジェクトの配列* ユーザーがセッション中に完了した認証方式を含みます。<br/>配列要素：<br/><ul> <li> 以下のオブジェクトスキーマの1つです。<br/><ul> <li> 以下のプロパティを持つオブジェクトです。<br/><ul> <li> `name`*：文字列* 完了した第一要素の名前です。値には以下が含まれます。<br/><ul> <li>`"federated"`：ソーシャル接続またはエンタープライズ接続を第一要素としてユーザーを認証しました。</li> <li>`"pwd"`：パスワードを第一要素としてデータベース接続のユーザーを認証しました。</li> <li>`"passkey"`：パスキーを第一要素としてデータベース接続のユーザーを認証しました。</li> <li>`"sms"`：パスワードレスのSMS接続を第一要素としてユーザーを認証しました。</li> <li>`"email"`：パスワードレスのメール接続をを第一要素としてユーザーを認証したか、またはパスワードリセットのための確認メールです。</li> <li>`"phone_number"`：電話番号を使用してパスワードをリセットしました。</li> <li>`"mock"`：内部テストに使用されます。</li> <li>`string`：URLで指定されたカスタムの認証方法（第二以降の要素として）です。</li> </ul> </li> <li>`timestamp`*：文字列*</li> </ul> </li> <li> 以下のプロパティを持つオブジェクトです。<br/><ul> <li>`name`*："mfa"の値*です。ユーザーが多要素認証（第二以降の要素として）を完了しました。</li> <li>`timestamp`*：文字列*</li> </ul> </li> </ul> </li> </ul> </li> <li> `riskAssessment`*：任意のオブジェクト* ログインまたはパスワードリセットフロー中に取得したリスク評価についての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li> `assessments`*：オブジェクト* 以下のプロパティを含みます。<br/><ul> <li> `ImpossibleTravel`*：任意のオブジェクト* ユーザーが不可能な移動を示す場所からログインしているかを判定します。<br/>以下のプロパティを含みます。<br/><ul> <li> `code`*：文字列* 可能な値には以下が含まれます。<br/><ul> <li>`minimal_travel_from_last_login`</li> <li>`travel_from_last_login`</li> <li>`substantial_travel_from_last_login`</li> <li>`impossible_travel_from_last_login`</li> <li>`invalid_travel`</li> <li>`missing_geoip`</li> <li>`anonymous_proxy`</li> <li>`unknown_location`</li> <li>`initial_login`</li> <li>`location_history_not_found`</li> <li>`assessment_not_available`</li> </ul> </li> <li> `confidence`*：文字列* 可能な値には以下が含まれます。<br/><ul> <li>`low`</li> <li>`medium`</li> <li>`high`</li> <li>`neutral`</li> </ul> </li> </ul> </li> <li> `NewDevice`*：任意のオブジェクト* ユーザーが既知のデバイスからログインしているかを判定します。<br/>以下のプロパティを含みます。<br/><ul> <li> `code`*：文字列* 可能な値には以下が含まれます。<br/><ul> <li>`match`</li> <li>`partial_match`</li> <li>`no_match`</li> <li>`initial_login`</li> <li>`unknown_device`</li> <li>`no_device_history`</li> <li>`assessment_not_available`</li> </ul> </li> <li> `confidence`*：文字列* 可能な値には以下が含まれます。<br/><ul> <li>`low`</li> <li>`medium`</li> <li>`high`</li> <li>`neutral`</li> </ul> </li> <li> `details`*：任意のオブジェクト* 以下のプロパティを含みます。<br/><ul> <li> `device`*：任意の文字列* 可能な値には以下が含まれます。<br/><ul> <li>`known`</li> <li>`unknown`</li> </ul> </li> <li> `useragent`*：任意の文字列* 可能な値には以下が含まれます。<br/><ul> <li>`known`</li> <li>`unknown`</li> </ul> </li> </ul> </li> </ul> </li> <li> `UntrustedIP`*：任意のオブジェクト* IPアドレスがAuth0の低信頼IPのリポジトリにあるかを示します。<br/>以下のプロパティを含みます。<br/><ul> <li> `code`*：文字列* 可能な値には以下が含まれます。<br/><ul> <li>`not_found_on_deny_list`</li> <li>`found_on_deny_list`</li> <li>`invalid_ip_address`</li> <li>`assessment_not_available`</li> </ul> </li> <li> `confidence`*：文字列* 可能な値には以下が含まれます。<br/><ul> <li>`low`</li> <li>`medium`</li> <li>`high`</li> <li>`neutral`</li> </ul> </li> <li> `details`*：任意のオブジェクト* 以下のプロパティを含みます。<br/><ul> <li>`category`*：任意の文字列*</li> <li>`ip`*：任意の文字列*要求の送信元のIPアドレスです。</li> <li>`matches`*：任意の文字列*</li> <li>`source`*：任意の文字列*</li> </ul> </li> </ul> </li> </ul> </li> <li> `confidence`*：文字列* 全体的なリスクスコアです。<br/>可能な値には以下が含まれます。<br/><ul> <li>`low`</li> <li>`medium`</li> <li>`high`</li> <li>`neutral`</li> </ul> </li> <li>`version`*：文字列*</li> </ul> </li> </ul> |
| `event.authorization`<br/>*（任意）* | ログインしようとしているユーザーに付与された認可を説明する情報を含むオブジェクトです。<br/>以下のプロパティを含みます。<br/><ul> <li>`roles`*：文字列の配列*。ユーザにー割り当てられたロール名を含む配列です。</li> </ul> |
| `event.client` | このログイントランザクションを開始した相手であるクライアントの情報です。<br/>以下のプロパティを含みます。<br/><ul> <li>`client_id`*：文字列*。ユーザーがログインしようとしているアプリケーションのクライアントIDです。</li> <li>`metadata`*：辞書*。アプリケーションの他のプロパティを含むオブジェクトです。</li> <li>`name`*：文字列*。アプリケーションの名前（ダッシュボードで定義）です。</li> </ul> |
| `event.connection` | ユーザーの認証に使用された接続についての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li>`id`*：文字列*。接続の一意の識別子です。</li> <li>`metadata`*：任意の辞書*。接続に付けられているメタデータです。</li> <li>`name`*：文字列*。ユーザーの認証に使用した接続の名前（`twitter`や`some-g-suite-domain`など）です。</li> <li>`strategy`*：文字列*。接続の種類です。ソーシャル接続の場合は`event.connection.strategy === event.connection.name`です。エンタープライズ接続の場合は`waad`（Windows Azure AD）、`ad`（Active Directory/LDAP）、`auth0`（データベース接続）などです。</li> </ul> |
| `event.organization`<br/>*（任意）* | 現在のトランザクションに関連付けられているOrganizationについての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li>`display_name`*：文字列*。Organizationのフレンドリー名です。</li> <li>`id`*：文字列*。Organizationの識別子です。</li> <li>`metadata`*：辞書*。Organizationに関連付けられているメタデータです。</li> <li>`name` *：文字列*。Organizationの名前です。</li> </ul> |
| `event.prompt`<br/>*（任意）* | レンダリングされたカスタムプロンプトから収集したデータです。<br/>以下のプロパティを含みます。<br/><ul> <li>`fields`*：任意の辞書*。フィールドと隠しフィールドのデータです。</li> <li>`id`*：文字列*。プロンプトIDです。</li> <li>`vars`*：任意の辞書*。共有変数のデータです。</li> </ul> |
| `event.refresh_token`<br/>*（任意）* | ［エンタープライズ顧客］現在のリフレッシュトークンです。<br/>以下のプロパティを含みます。<br/><ul> <li>`client_id`*：任意の文字列*。［エンタープライズ顧客］リフレッシュトークンに関連付けられているクライアントのIDです。</li> <li>`created_at`*：文字列*。［エンタープライズ顧客］リフレッシュトークンが作成された時点のタイムスタンプです。</li> <li> `device`*：任意のオブジェクト* 以下のプロパティを含みます。<br/><ul> <li>`initial_asn`*：任意の文字列*。［エンタープライズ顧客］このリフレッシュトークンに関連付けられている最初のAS番号です。</li> <li>`initial_ip`*：任意の文字列*。［エンタープライズ顧客］このリフレッシュトークンに関連付けられている最初のIPアドレスです。</li> <li>`initial_user_agent`*：任意の文字列*。［エンタープライズ顧客］このリフレッシュトークンに関連付けられている最初のユーザエージェントです。</li> <li>`last_asn`*：任意の文字列*。［エンタープライズ顧客］このリフレッシュトークンが最後に交換された最後のAS番号です。</li> <li>`last_ip`*：任意の文字列*。［エンタープライズ顧客］このリフレッシュトークンが最後に交換された最後のIPアドレスです。</li> <li>`last_user_agent`*：任意の文字列*。［エンタープライズ顧客］このリフレッシュトークンが最後に交換されたデバイスの最後のユーザエージェントです。</li> </ul> </li> <li>`expires_at`*：任意の文字列*。［エンタープライズ顧客］リフレッシュトークンが失効する絶対有効期限日のタイムスタンプです。</li> <li>`id`*：文字列*。［エンタープライズ顧客］リフレッシュトークンのIDです。</li> <li>`idle_expires_at`*：任意の文字列*。［エンタープライズ顧客］リフレッシュトークンが失効するアイドル有効期限日のタイムスタンプです。</li> <li>`last_exchanged_at`*：任意の文字列*。［エンタープライズ顧客］リフレッシュトークンが最後に交換に成功したときのタイムスタンプです。</li> <li> `resource_servers`*：任意のオブジェクト配列* 要素は以下のプロパティを含みます。<br/><ul> <li>`audience`*：文字列*。［エンタープライズ顧客］リフレッシュトークンのオーディエンスです。</li> <li>`scopes`*：文字列*。［エンタープライズ顧客］リフレッシュトークンのスコープです。</li> </ul> </li> <li>`rotating`*：任意のブール値*。［エンタープライズ顧客］リフレッシュトークンがローテーションするリフレッシュトークンかどうかです。</li> <li>`session_id`*：任意の文字列*。［エンタープライズ顧客］リフレッシュトークンにバインドされたセッションのIDです。</li> <li>`user_id`*：任意の文字列*。［エンタープライズ顧客］リフレッシュトークンにバインドされたユーザのIDです。</li> </ul> |
| `event.request` | トランザクションを開始した要求についての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li>`asn`*：任意の文字列*。要求を行っているユーザエージェントのAS番号です。</li> <li>`body`*：辞書*。POST要求本文です。このデータはリフレッシュトークンとクライアント資格情報交換、ログイン後アクションの実行中にのみ利用可能です。</li> <li> `geoip`*：オブジェクト* 以下のプロパティを含みます。<br/><ul> <li>`cityName`*：任意の文字列*</li> <li>`continentCode`*：任意の文字列*</li> <li>`countryCode`*：任意の文字列*</li> <li>`countryCode3`*：任意の文字列*</li> <li>`countryName`*：任意の文字列*</li> <li>`latitude`*：任意の数値*</li> <li>`longitude`*：任意の数値*</li> <li>`subdivisionCode`*：任意の文字列*</li> <li>`subdivisionName`*：任意の文字列*</li> <li>`timeZone`*：任意の文字列*</li> </ul> </li> <li>`hostname`*：任意の文字列*。認証フローで使用されるホスト名です。</li> <li>`ip`*：文字列*。要求の送信元のIPアドレスです。</li> <li>`language`*：任意の文字列*。ブラウザーが要求した言語です。</li> <li>`method`*：文字列*。要求が使用したHTTPメソッドです。</li> <li>`query`*：辞書*。認可要求に送信されたクエリ文字列パラメーターです。</li> <li>`user_agent`*：任意の文字列*。トランザクションの開始時に受信した`User-Agent`ヘッダーの値です。</li> </ul> |
| `event.resource_server`<br/>*（任意）* | アクセスの要求対象であるリソースサーバーについての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li>`identifier`*：文字列*。リソースサーバの識別子です。例：`https://your-api.example.com`.</li> </ul> |
| `event.session`<br/>*（任意）* | 現在のログインセッションです。<br/>以下のプロパティを含みます。<br/><ul> <li>`authenticated_at`*：任意の文字列*。［エンタープライズ顧客］セッションが最後に認証された日付と時間です。</li> <li> `clients`*：任意のオブジェクト配列* ［エンタープライズ顧客］セッションのためのクライアント詳細のリストです。<br/>要素は以下のプロパティを含みます。<br/><ul> <li>`client_id`*：文字列*。［エンタープライズ顧客］セッションのクライアントIDです。</li> </ul> </li> <li>`created_at`*：任意の文字列*。［エンタープライズ顧客］セッションが作成された日付と時間です。</li> <li> `device`*：任意のオブジェクト* ［エンタープライズ顧客］セッションで使用されたデバイスに関連したメタデータです。<br/>以下のプロパティを含みます。<br/><ul> <li>`initial_asn`*：任意の文字列*。［エンタープライズ顧客］このセッションに関連付けられている最初のAS番号です。</li> <li>`initial_ip`*：任意の文字列*。［エンタープライズ顧客］このセッションに関連付けられている最初のIPアドレスです。</li> <li>`initial_user_agent`*：任意の文字列*。［エンタープライズ顧客］このセッションに関連付けられているデバイスの最初のユーザエージェントです。</li> <li>`last_asn`*：任意の文字列*。［エンタープライズ顧客］このユーザが最後のログインを行ったログイン元のAS番号です。</li> <li>`last_ip`*：任意の文字列*。［エンタープライズ顧客］このユーザが最後のログインを行ったログイン元のIPアドレスです。</li> <li>`last_user_agent`*：任意の文字列*。［エンタープライズ顧客］このユーザが最後のログインを行ったログイン元のデバイスの最後のユーザエージェントです。</li> </ul> </li> <li>`expires_at`*：任意の文字列*。［エンタープライズ顧客］セッションの有効期限が切れる日付と時間です。</li> <li>`id`*：文字列*。現在のセッションのIDです。</li> <li>`idle_expires_at`*：任意の文字列*。［エンタープライズ顧客］アイドル状態のセッションが失効する日付と時間です。</li> <li>`last_interacted_at`*：任意の文字列*。［エンタープライズ顧客］セッションで最後に対話があった日付と時間です。</li> <li>`updated_at`*：任意の文字列*。［エンタープライズ顧客］セッションが最後に更新された日付と時間です。</li> <li>`user_id`*：任意の文字列*。［エンタープライズ顧客］別のAPIとの対話に使用可能なユーザーIDです。</li> </ul> |
| `event.stats` | 現在のユーザーのログイン統計です。<br/>以下のプロパティを含みます。<br/><ul> <li>`logins_count`*：数値*。このユーザがこれまでにログインした回数です。</li> </ul> |
| `event.tenant` | 現在のトランザクションに関連付けられているテナントについての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li>`id`*：文字列*。テナントの名前です。</li> </ul> |
| `event.transaction`<br/>*（任意）* | 現在のトランザクションについての詳細です。<br/>以下のプロパティを含みます。<br/><ul> <li>`acr_values`*：文字列の配列*。元の認証要求で提供された任意のacr_valuesです。</li> <li>`linking_id`*：任意の文字列*。開発者がこのトランザクションを参照するためのダイナミックリンクIDです。</li> <li>`locale`*：文字列*。このトランザクションに使用されるロケールです。ブラウザーが要求した言語とテナントの言語設定を比較して決定されます。</li> <li>`login_hint`*：任意の文字列*。エンドユーザーが（必要であれば）使用するかもしれないログイン識別子について認証サーバーに提供するヒントです。</li> <li>`prompt`*：任意の文字列配列*。ユーザーに再認証や同意を求めるかを示す指示のリストです。</li> <li> `protocol`*：任意の文字列* 可能な値には以下が含まれます。<br/><ul> <li>`oidc-basic-profile`：最も使用頻度の高いWebベースのログインです。</li> <li>`oidc-ciba`：CIBA（Client-Initiated Backchannel Authentication）フローで使用されます。</li> <li>`oidc-implicit-profile`：モバイルデバイスやシングルページアプリで使用されます。</li> <li>`oidc-hybrid-profile`：アクセストークンとリフレッシュトークンを安全に取得しながら、アプリケーションがIDトークンに即座にアクセスできるようにします。</li> <li>`samlp` ：SaaSアプリで使用されるSAMLプロトコルです。</li> <li>`wsfed`：Office365などのMicrosoft社製品で使用されるWS-Federationです。</li> <li>`wstrust-usernamemixed`：CRMやOffice365で使用されるWS-trustのユーザ名/パスワードです。</li> <li>`oauth2-device-code`：デバイス認可フローを使用したトランザクションです。</li> <li>`oauth2-resource-owner`：データベース接続で一般的に使用されるユーザ名/パスワードによるログインです。</li> <li>`oauth2-resource-owner-jwt-bearer`：ユーザーの秘密鍵で署名されたベアラーJWTを使用したログインです。</li> <li>`oauth2-password`：パスワード交換を使用したログインです。</li> <li>`oauth2-webauthn`：webauthn交換を使用したログインです。</li> <li>`oauth2-access-token`：リフレッシュトークンの交換を使用したトークンの更新です。</li> <li>`oauth2-refresh-token`：リフレッシュトークンの交換を使用したトークンの更新です。</li> <li>`oauth2-token-exchange`</li> </ul> </li> <li>`redirect_uri`*：任意の文字列*。トランザクションの完了後、Auth0がブラウザーのリダイレクト先に使用するURLです。</li> <li> `requested_authorization_details`*：任意のオブジェクト配列* https://datatracker.ietf.org/doc/html/draft-ietf-oauth-rar#section-2が提供するRich Authorization Requests仕様のセクション2を基にしたリッチ認証要求についての詳細です。<br/>要素は以下のプロパティを含みます。<br/><ul> <li>`type`*：文字列*。認証タイプの詳細の文字列です。タイプフィールドの値がオブジェクトに含められる内容を決定します。</li> </ul> </li> <li>`requested_scopes`*：文字列の配列*。この認証フローの開始時に要求したスコープ（ある場合）です。</li> <li> `response_mode`*：任意の文字列* 認可エンドポイントからパラメーターを返す時に使用されるメカニズムを認可サーバーに知らせます。<br/>可能な値には以下が含まれます。<br/><ul> <li>`query`</li> <li>`fragment`</li> <li>`form_post`</li> <li>`web_message`</li> </ul> </li> <li> `response_type`*：任意の文字列配列* 可能な値には以下が含まれます。<br/><ul> <li>`code`</li> <li>`token`</li> <li>`id_token`</li> </ul> </li> <li>`state`*：任意の文字列*。アプリが当初の要求に含め、Auth0がアプリにリダイレクトで戻すときに含める不透明な英数字の文字列です。</li> <li>`ui_locales`*：文字列の配列*。当初の認証要求で提供されたui_localesです。</li> </ul> |
| `event.user` | 代理で始めた現在のトランザクションの対象ユーザーを記述したオブジェクトです。<br/>以下のプロパティを含みます。<br/><ul> <li>`app_metadata`*：辞書*。サポートプラン、セキュリティロール、アクセス制御グループなど、ユーザーのアクセスに影響を与えるユーザー情報を保管しているカスタムフィールドです。</li> <li>`created_at`*：文字列*。ユーザーのプロファイル情報が最初に作成されたときを示すタイムスタンプ。</li> <li>`email`*：任意の文字列*。ユーザーの（一意の）メールアドレスです。</li> <li>`email_verified`*：ブール値*。ユーザーに確認済みのメールアドレスがあるかを示します。</li> <li> `enrolledFactors`*：任意のオブジェクト配列* ユーザーが登録した認証要素の配列です。<br/>配列要素：<br/><ul> <li> 登録済みの認証要素の種類と要素特有のオプションを記述したオブジェクトです。<br/>以下のプロパティを含みます。<br/><ul> <li>`options`*：任意の辞書*。登録された要素を説明する追加のオプションです。</li> <li>`type`*：文字列*。`push-notification`、`phone`、`email`、`otp`、`webauthn-roaming`、`webauthn-platform`など、認証要素の種類です。</li> </ul> </li> </ul> </li> <li>`family_name`*：任意の文字列*。ユーザーの姓です。</li> <li>`given_name`*：任意の文字列*。ユーザーの名です。</li> <li> `identities`*：オブジェクトの配列* ユーザーが元の認証に使用したIDプロバイダーから取得した情報が含まれます。ユーザーがプロファイルを複数のIDロバイダーにをリンクしているかもしれません。そのようなアイデンティティもこの配列に含まれます。IDプロバイダーオブジェクトのコンテンツはプロバイダーに依存します。<br/>要素は以下のプロパティを含みます。<br/><ul> <li>`connection`*：任意の文字列*。ユーザーの認証に使用されたAuth0接続の名前です。</li> <li>`isSocial`*：任意のブール値*。接続がソーシャル接続がを示します。</li> <li>`profileData`*：任意の辞書*。接続に関連付けられているユーザー情報です。プロファイルがリンクされている場合は、セカンダリーアカウントのユーザー情報も含まれます。</li> <li>`provider`*：任意の文字列*。Facebook、Google、SAML、独自のプロバイダーなど、ユーザーを認証するエンティティの名前です。</li> <li>`user_id`*：任意の文字列*。この接続またはプロバイダーでユーザーに一意の識別子です。</li> </ul> </li> <li>`last_password_reset`*：任意の文字列*。ユーザーのパスワードが最後にリセットまたは変更されたときを示すタイムスタンプです。ユーザー作成時にはこのフィールドは存在しません。このプロパティはデータベース接続にのみ存在します。</li> <li>`multifactor`*：任意の文字列配列*。ユーザーが登録した多要素認証（MFA）プロバイダーのリストです。この配列は、ユーザーがMFAに登録したときと、管理者がユーザーのMFA登録をリセットしたときに更新されます。</li> <li>`name`*：任意の文字列*。ユーザーのフルネームです。</li> <li>`nickname`*：任意の文字列*。ユーザーのニックネームです。</li> <li>`phone_number`*：任意の文字列*。ユーザーの電話番号です。</li> <li>`phone_verified`*：任意のブール値*。ユーザーの電話番号検証が完了したかを示します。</li> <li>`picture`*：任意の文字列*。[ユーザーのプロフィール画像](https://auth0.com/docs/ja-jp/users/change-user-picture)をポイントするURLです。</li> <li>`updated_at`*：文字列*。ユーザーのプロファイル情報が最後に更新または変更されたときを示すタイムスタンプです。</li> <li>`user_id`*：文字列*。ユーザーの（一意の）識別子です。</li> <li>`user_metadata`*：辞書*。勤務先の住所、自宅の住所、ユーザー設定など、アクセス権に影響を与えないユーザー情報を保管するカスタムフィールドです。</li> <li>`username`*：任意の文字列*。ユーザーの（一意の）ユーザー名です。</li> </ul> |
