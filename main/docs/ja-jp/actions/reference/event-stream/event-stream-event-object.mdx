---
#
# THIS FILE IS AUTO-GENERATED.
# DO NOT EDIT MANUALLY.
#
description: トリガーの実行に関するコンテキスト情報を提供する、event-stream Action トリガーのイベントオブジェクトについて説明します。
trigger: event-stream
pageType: event
title: 'イベントオブジェクト'
sidebarTitle: 'イベントオブジェクト'
---

event-stream Actions トリガーの `event` オブジェクトは、トリガーの実行に関するコンテキスト情報を提供します。

```json Event Object
{
  "secrets": {},
  "message": {
    "id": "string",
    "type": "string",
    "source": "string",
    "specversion": "string",
    "a0tenant": "string"
  }
}
```

## `event.message` {#eventmessage}

<ResponseField name="event.message" type="object">
  すべてのイベントプロパティを含むCloudEventメッセージ。

  <Expandable title="event.messageのプロパティ" defaultOpen>
    <ResponseField name="id" type="string">
      イベントを識別します。
    </ResponseField>

    <ResponseField name="type" type="string">
      発生元の出来事に関連するイベントの種類を示します。
    </ResponseField>

    <ResponseField name="data" type="dictionary" post={["optional"]}>
      イベントのpayload。
    </ResponseField>

    <ResponseField name="source" type="string">
      イベントが発生したコンテキストを識別します。
    </ResponseField>

    <ResponseField name="specversion" type="string">
      イベントで使用されるCloudEvents仕様のバージョン。
    </ResponseField>

    <ResponseField name="time" type="string" post={["optional"]}>
      出来事が発生した日時を示すタイムスタンプ。RFC 3339に準拠する必要があります。
    </ResponseField>

    <ResponseField name="a0tenant" type="string">
      イベントが関連付けられているAuth0テナントの識別子。
    </ResponseField>

    <ResponseField name="a0stream" type="string" post={["optional"]}>
      イベントが配信されたストリームのAuth0 イベントストリーム ID。
      イベントがイベントストリーム経由で配信された場合に存在し、
      Events API (GET /api/v2/events) 経由でイベントを取得した場合は省略されます。
    </ResponseField>

    <ResponseField name="a0purpose" type="string" post={["optional"]}>
      このイベントの目的。テストイベントなどの特別な場合にのみ設定され、通常のイベントでは省略されます。

      指定可能な値：`test`
    </ResponseField>
  </Expandable>
</ResponseField>

## `event.secrets` {#eventsecrets}

<ResponseField name="event.secrets" type="secrets">
  この Action に安全に関連付けられたシークレット値。
</ResponseField>