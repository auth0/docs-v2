---
description: Learn how to configure ACUL
'og:image': https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
'og:title': Configure ACUL
'og:url': https://auth0.com/docs/
permalink: configure
title: Configure ACUL
'twitter:description': Learn how to configure ACUL
'twitter:title': Configure ACU
---

You can configure ACUL using the Auth0 Management API, Dashboard and Infrastructure-as-Code(IaC) tools.

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
To configure ACUL, make sure your custom configuration files and assets are hosted on a public CDN. 
</Callout>

## The ACUL rendering configuration object

The ACUL rendering configuration is a single JSON object that is used to enable ACUL and list all the available properties, per screen, which consist of specific assets, context data, and applicable conditional logic. 

ACUL rendering configuration object: 

```json
{
  "rendering_mode": "advanced",
  "context_configuration": [...],
  "default_head_tags_disabled": false,
  "head_tags": [...],
  "filters": {
    "match_type": "includes_any",
    "clients": [...],
    "organizations": [...],
    "domains": [...]
  },
  "use_page_template": false
}
``` 

ACUL rendering configuration object properties:

<table class="table"><thead>
<tr>
<th><strong>Property</strong></th>
<th><strong>Required</strong></th>
<th><strong>Description</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td><code>rendering_mode</code></td>
<td> Yes </td>
<td> String. It enables ACUL for the screen. 
Possible values:
<ul>
  <li><code>standard</code> Renders the standard Universal Login screen.</li>
  <li><code>advanced</code> Enables ACUL and uses the ACUL rendering configuration object. </li>
</ul>
Defaults to <code>standard</code>.
</td>
</tr>
<tr>
<td><code>use_page_template</code></td>
<td> Yes </td>
<td> Boolean. Defines whether your custom screen is rendered inside your tenant's existing custom page template.
Possible values:
<ul>
  <li><code>True</code> If a custom page template is defined, the screen renders within it. A warning is logged if no template exists.</li>
  <li><code>False</code> The screen renders using a minimal template.
 </li>
</ul>
Defaults to <code>false</code>.
</td>
</tr>
<tr>
<td><code>default_head_tags_disabled</code></td>
<td> No </td>
<td> Boolean. Defines whether Auth0 injects its default set of <code>head</code> tags into the page.
Possible values:
<ul>
  <li><code>True</code> Disables all default tags, giving you complete control over the <code>head</code>. You are responsible for providing necessary tags.</li>
  <li><code>False</code> Auth0 will include its standard default tags.
 </li>
</ul>
Defaults to <code>false</code>.
</td>
</tr>
<tr>
<td><code>context_configuration</code></td>
<td> Yes </td>
<td> Boolean. Array of Strings. An array specifying additional data points to include in the <code>universal_login_context</code> object passed to your UI.
Possible values:
<ul>
  <li><code>branding.settings</code></li>
  <li><code>branding.themes.default</code></li>
  <li><code>client.logo_uri</code></li>
  <li><code>client.description</code></li>
  <li><code>client.metadata.[keyName]</code></li>
  <li><code>organization.display_name</code></li>
  <li><code>organization.branding</code></li>
  <li><code>organization.metadata.[keyName]</code></li>
  <li><code>request.ip</code></li>
  <li><code>request.geoip</code></li>
  <li><code>request.hostname</code></li>
  <li><code>request.language</code></li>
  <li><code>screen.texts</code></li>
  <li><code>tenant.name</code></li>
  <li><code>tenant.friendly_name</code></li>
  <li><code>tenant.enabled_locales</code></li>
  <li><code>transaction.domain.metadata.[keyName]</code></li>
  <li><code>untrusted_data.submitted_form_data</code></li>
  <li><code>untrusted_data.authorization_params.ui_locales</code></li>
  <li><code>untrusted_data.authorization_params.login_hint</code></li>
  <li><code>untrusted_data.authorization_params.screen_hint</code></li>
  <li><code>untrusted_data.authorization_params.ext-[keyName]</code></li>
  <li><code>user.organizations</code></li>
  <li><code>user.app_metadata.[keyName]</code></li>
  <li><code>user.user_metadata.[keyName]</code></li>
</ul>
Limit of 100 entries.
</td>
</tr>
<tr>
<td><code>head_tags</code></td>
<td> Yes </td>
<td> Array of Objects. Injects HTML tags into the <head> of the host page.
Each object defines and HTML tag and can contain:
<ul>
  <li><code>tag</code> (String, required) Must be an HTML element valid for use in the <head> tag.</li>
  <li><code>content</code> (String, optional)  Supports using the context data as liquid variables, for example <code>client.logo_url</code>. Limit of 250 characters. </li>
  <li>
  	<code>attributes</code> (object, optional) Key-value map.
  	<ul>
  		<li><code>integrity</code> can be an array. Entity Limit: 10 </li>
  		<li>URL attributes (src, href, etc) support dynamic segments and allow the usage of following variables:
  			<ul>
  			<li><code>locale</code></li>
  			<li><code>screen.name</code></li>
  			<li><code>prompt.name</code></li>
  			<li><code>client.id</code></li>
  			<li><code>client.name</code></li>
  			<li><code>client.metadata.[keyName]</code></li>
  			<li><code>organization.id</code></li>
  			<li><code>organization.name</code></li>
  			<li><code>organization.metadata.[keyName]</code></li>
  			</ul>
  		</li>
  	</ul>
  	Limit of 10 attributes.	
  </li>
</ul>
</td>
</tr>
<tr>
<td><code>filters</code></td>
<td> No </td>
<td> Object. Allows the conditional application of the entire ACUL configuration:
Each object can contain:
<ul>
  <li><code>match_type</code> (String, required) 
	Possible values:
	<ul>
		<li><code>includes_any</code></li>
		<li><code>excludes_any</code></li>
	</ul>
 </li>
  <li><code>clients</code> (Array of Objects, optional)
  	<ul>
  		<li>Uses <code>id</code> or <code>metadata</code></li>
  		<li>Limit of 25 objects</li>
  	</ul>
  </li>
  <li>
  	<code>Organizations</code> (Array of Objects, optional).
  	<ul>
  		<li>Uses <code>id</code> or <code>metadata</code></li>
  		<li>Limit of 25 objects</li>
  	</ul>	
  </li>
</ul>
</td>
</tr>
</tbody>
</table>

## Auth0 Dashboard
You can use the Dashboard to customize your screens with the ACUL:

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
The Dashboard settings follow the properties of the ACUL rendering configuration object. 
</Callout>

1. Navigate to [Dashboard > Branding > Universal Login](https://manage.auth0.com/dashboard/#/universal-login/customizations-new)
	1. Select **Customize authentication screens**
	2. Select a <code>screen</code> to  customize
	3. From **Settings > Rendering mode** select **Advanced mode**
	4. (Optional) Select **Use custom page template**  
	5. (Optional) Select **Advanced mode filters**
	6. Select **Head Tags** to add your own html head tags
	7. Select **Additional Data** to add data from the <code>universal_login_context object</code>
2. Select Save & Publish

## Infrastructure-as-Code(IaC)

You can configure ACUL with IaC tools, such as Auth0 Terraform and Auth0 Deploy CLI, Auth0 CLI < > 


### **Auth0 Terraform Provider**
Use the auth0_prompt_rendering resource.
Needs HCL example update based on finalized Section 2 structure ACUL rendering configuration object

### **Auth0 Deploy CL**
Define in rendering.yaml.
Needs YAML example update based on finalized Section 2 structure ACUL rendering configuration object

### **Auth0 CLI** 

## Auth0 Management API

You can use the [/v2/prompts/{prompt}/screen/{screen}/rendering](https://auth0.com/docs/api/management/v2/prompts/patch-rendering) endpoint to enable ACUL using the ACUL rendering configuration object. 

Make a `PATCH` request to the [/v2/prompts/{prompt}/screen/{screen}/rendering](https://auth0.com/docs/api/management/v2/prompts/patch-rendering) endpoint . 


Sample `PATCH` request to configure ACUL for the login-id screen:

```curl
# PATCH to /api/v2/prompts/login-id/screen/login-id/rendering
{
  "rendering_mode": "advanced",
  "context_configuration": [
    "branding.themes.default",
    "client.logo_uri",
    "client.description",
    "client.metadata.google_tracking_id",
    "screen.texts",
    "tenant.enabled_locales",
    "untrusted_data.submitted_form_data",
    "untrusted_data.authorization_params.ext-my_param"
  ],
  "head_tags": [
    {
      "tag": "script",
      "attributes": {
        "src": "https://cdn.sass.app/auth-screens/{{client.name}}.js",
        "defer": true,
        "integrity": [
          "sha256-someHash/Abc+123",
          "sha256-someHash/cDe+456"
        ]
      }
    },
    {
      "tag": "link",
      "attributes": {
        "rel": "stylesheet",
        "href": "https://cdn.sass.app/auth-screens/{{client.name}}.css"
      }
    }
  ]
}
```
