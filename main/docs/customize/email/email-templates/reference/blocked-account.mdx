---
title: Blocked Account Email Template
sidebarTitle: Blocked Account
---

If you enable the **Blocked Account Email** template, this email is sent when access to an account from a specific IP address is blocked by [brute-force protection](/docs/secure/attach-protection/brute-force-protection).

The message contains a link to re-enable the origin of the request.

## Template-Specific Variables

In addition to the [common Liquid variables](/docs/customize/email/email-templates/supported-liquid-syntax) available to all email templates, this email template has the following variables:

* `user.source_ip`
* `user.city`
* `user.country`
