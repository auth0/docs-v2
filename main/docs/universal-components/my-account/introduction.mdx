import {
  Badge,
  Callout,
  QuickNotes,
  ButtonGrid,
  ButtonCard,
} from "@okta/docs-ui";

# My Account <Badge type="beta" />

React components for building user account management interfaces with Auth0's My Account features.

---

## Available Components

<ButtonGrid>
  <ButtonCard
    title="UserMFAMgmt"
    status="Available"
    description="A comprehensive Multi-Factor Authentication (MFA) management component that allows users to view, enroll, and delete MFA factors including phone, OTP, email, and push notifications."
    href="#"
    cta="View Docs →"
  />
</ButtonGrid>

---

## Setup Requirements

Before using any My Account components, you need to configure your Auth0 tenant with the proper applications, MFA settings, and permissions. Follow these steps to set up your environment:

### Auth0 Dashboard Configuration

#### 1. Enable My Account API

<Callout type="info">
  Go to <b>APIs → Auth0 My Account API</b> and make sure it's enabled.
</Callout>

#### 2. Create SPA Application

Go to <b>Applications → Create Application</b>:

- Choose <b>Single Page Web Applications</b>
- For development mode, add <code>http://localhost:5173</code> to:
  - Allowed Callback URLs
  - Allowed Logout URLs
  - Allowed Web Origins

#### 3. Setup Database & User

- Create a Database connection
- In Applications tab, enable your new SPA app
- Create a test user in this database

#### 4. Create Client Grant

Create a client grant for the user & client pair to solve access control:

**Client Grant Creation**

```json
POST https://{{auth0_domain}}/api/v2/client-grants
{
  "client_id": "{{auth0_client_id}}", // use your app client_id
  "audience": "https://{{auth0_domain}}/me/", // use your domain
  "scope": [
    "read:me:authentication_methods",
    "delete:me:authentication_methods",
    "update:me:authentication_methods",
    "read:me:factors",
    "create:me:authentication_methods"
  ],
  "subject_type": "user"
}
```

#### 5. Environment Variables

Create a <code>.env</code> file in your project:

```env
VITE_AUTH0_DOMAIN=your-domain.auth0.com
VITE_AUTH0_CLIENT_ID=your-spa-client-id
```

---

## Quick Notes

<QuickNotes>
  - User must be authenticated before using these components - MFA configuration
  may take a few minutes to propagate across Auth0's systems - Test MFA
  enrollment in a development environment before production
</QuickNotes>

---

## Start Building

Ready to add My Account components to your application? Choose your path:

<ButtonGrid>
  <ButtonCard
    title="MFA Management"
    description="Enable users to manage their multi-factor authentication settings and secure their accounts."
    href="#"
    cta="Explore UserMFAMgmt"
    style={{
      background: "linear-gradient(90deg, #A259FF 0%, #377DF7 100%)",
      color: "#fff",
    }}
  />
  <ButtonCard
    title="More Components Coming"
    description="Additional account management features like profile editing, password changes, and more will be added soon."
    cta="Coming Soon"
    disabled
  />
</ButtonGrid>
