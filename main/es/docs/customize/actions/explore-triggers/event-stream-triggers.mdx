---
description: Obtén información sobre las Actions de Event Stream.
'og:image': https://cdn2.auth0.com/docs/1.14553.0/img/share-image.png
'og:title': activadores de Event Stream
'og:url': https://auth0.com/docs/
permalink: event-stream-trigger
title: activadores de Event Stream
sidebarTitle: Información general
'twitter:description': Obtén información sobre las Actions de Event Stream, que se ejecutan como parte del flujo de Event Stream.
'twitter:title': activadores de Event Stream
---

Las Actions de Event Stream son funciones basadas en lógica que se ejecutan de forma asíncrona cuando se producen eventos específicos dentro de tu inquilino de Auth0. Son Actions no bloqueantes y no afectarán la latencia de la experiencia del usuario.

Cada Action de Event Stream está asociada a un Event Stream y escucha un conjunto predefinido de tipos de evento (por ejemplo, inicios de sesión correctos, cambios de contraseña o eliminaciones de usuarios). Cuando se produce un evento al que está suscrita, se activa la Action. A diferencia de las Actions de Login o Pre-Registration, las Actions de Event Stream se ejecutan en segundo plano y no afectan la transacción principal del usuario.

<Frame>![Diagrama que muestra el TBD.](/docs/images/customize/actions/event-stream-triggers.svg)</Frame>

Estas Actions no son bloqueantes (asíncronas) y están suscritas a un conjunto específico de [tipos de evento](/es/docs/customize/events/event-types).

<div id="triggers">
  ## Activadores
</div>

<div id="event-stream">
  ### Event Stream
</div>

Las Actions `event-stream` son funciones que se ejecutan cuando se producen los tipos de evento a los que están suscritas.

<div id="references">
  #### Referencias
</div>

* [Objeto de evento](/es/docs/customize/actions/explore-triggers/event-stream-triggers/event-stream-event-object): Proporciona contexto tanto para el mensaje de Event Stream como para la ejecución de la Action.
* [Objeto de API](/es/docs/customize/actions/explore-triggers/event-stream-triggers/event-stream-api-object): Proporciona métodos para modificar el comportamiento del flujo.

<div id="use-cases">
  ## Casos de uso
</div>

<div id="synchronization">
  ### Sincronización
</div>

Se puede usar una Action de Event Stream para comunicar un evento concreto a un servicio externo según una lógica personalizada. La siguiente Action muestra cómo reenviar de forma segura un mensaje de evento a un servicio externo mediante una clave de API almacenada.

```javascript lines
/**
* Controlador que se ejecuta durante el procesamiento de eventos en un Event Stream.
* @param {Event} event - Detalles sobre el evento entrante.
* @param {EventStreamAPI} api - Métodos y utilidades para definir el procesamiento del flujo de eventos.
*/
exports.onExecuteEventStream = async (event, api) => {
  const message = event.message;

  try {
    await fetch(event.secrets.URL, {
      method: 'POST',
      headers: {
        'X-API-Key': event.secrets.API_KEY,
      },
      body: JSON.stringify(message),
    });
  } catch (err) {
    throw new Error('External service failure');
  }

  return;
};
```

Para obtener más información sobre cómo escribir Actions, consulta [Escribe tu primera Action](/es/docs/customize/actions/write-your-first-action).

Para obtener más información sobre cómo crear un Event Stream, consulta [Crea un Event Stream](/es/docs/customize/events/create-an-event-stream#create-an-event-stream-actions).