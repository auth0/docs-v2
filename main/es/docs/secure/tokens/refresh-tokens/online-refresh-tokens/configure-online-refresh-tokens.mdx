---
title: Configurar y usar tokens de actualización en línea
description: Cómo configurar y usar tokens de actualización en línea.
---

import { ReleaseStageNotice } from "/snippets/es/ReleaseStageNotice.jsx"

<ReleaseStageNotice feature="Tokens de actualización en línea" stage="beta" contact="su Technical Account Manager" />

<div id="configure-orts">
  ## Configurar ORTs
</div>

Los tokens de actualización en línea (ORTs) deben habilitarse a nivel de la [API (servidor de recursos)](/es/docs/get-started/apis/api-settings) mediante Auth0 Dashboard o la Management API.

<div id="configure-using-the-dashboard">
  ### Configurar desde el Dashboard
</div>

1. Vaya a [Dashboard &gt; Applications &gt; API](https://manage.auth0.com/#/apis).

2. Seleccione la API que desea configurar.

3. En la pestaña **Settings**, active el interruptor **Allow Online Access**.

   <Frame>![Dashboard Applications APIs Settings Allow Online Access](/docs/images/refresh-tokens/allow-online-access.png)</Frame>

4. Seleccione **Save**.

Para revisar y configurar los valores del tiempo de espera por inactividad y la duración absoluta de las sesiones, consulte la configuración de [Expiración de la sesión del inquilino](https://manage.auth0.com/#/tenant/advanced). Para obtener más información, consulte [Configurar la duración de la sesión](/es/docs/manage-users/sessions/configure-session-lifetime).

<div id="configure-using-the-management-api">
  ### Configuración mediante la Management API
</div>

Para habilitar los ORT, realice una solicitud `PATCH` al endpoint [Update Resource Server](/es/docs/api/management/v2/resource-servers/patch-resource-servers-by-id):

```bash
curl --request PATCH \
  --url 'https://<YOUR_DOMAIN>/api/v2/resource-servers/<YOUR_RESOURCE_SERVER_ID>' \
  --header 'authorization: Bearer <YOUR_MANAGEMENT_API_TOKEN>' \
  --header 'content-type: application/json' \
  --data '{"allow_online_access": true}'
```

Para verificar la configuración, haga una solicitud `GET` al servidor de recursos:

```bash
curl --request GET \
  --url 'https://<YOUR_DOMAIN>/api/v2/resource-servers/<YOUR_RESOURCE_SERVER_ID>' \
  --header 'authorization: Bearer <YOUR_MANAGEMENT_API_TOKEN>'
```

La respuesta incluye la propiedad `allow_online_access`:

```json
{
  "id": "resource-server-id",
  "name": "My API",
  "identifier": "https://my-api.example.com",
  "allow_online_access": true,
  ...
}
```

<div id="get-an-ort">
  ## Obtén un ORT
</div>

Para obtener un ORT, incluye el `online_access` [scope](/es/docs/get-started/apis/scopes) en tu solicitud de autorización usando el [Flujo de código de autorización](/es/docs/get-started/authentication-and-authorization-flow/authorization-code-flow) o el [Flujo de código de autorización con PKCE](/es/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce).

```bash
https://<YOUR_DOMAIN>/authorize \
    audience=<YOUR_API_AUDIENCE> \
    scope=openid profile online_access \
    response_type=code \
    client_id=<YOUR_CLIENT_ID> \
    redirect_uri=<https://YOUR_APP/callback> \
    state=<OPAQUE_VALUE>
```

Después de que el usuario se autentique, intercambia el authorization code por tokens:

```bash
curl --request POST \
  --url 'https://<YOUR_DOMAIN>/oauth/token' \
  --header 'content-type: application/x-www-form-urlencoded' \
  --data grant_type=authorization_code \
  --data 'client_id=<YOUR_CLIENT_ID>' \
  --data 'code=<YOUR_AUTHORIZATION_CODE>' \
  --data 'redirect_uri=<https://YOUR_APP/callback>' \
  --data 'code_verifier=<YOUR_CODE_VERIFIER>'
```

La respuesta del token incluye el ORT:

```json
{
  "access_token": "eyJ...",
  "id_token": "eyJ...",
  "refresh_token": "ORT...",
  "token_type": "Bearer",
  "expires_in": 86400
}
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Los ORT llevan el prefijo `ORT` para distinguirse de los tokens de actualización. Sin embargo, debe tratar el token como un valor opaco y no basarse en su estructura interna.
</Callout>

<div id="use-an-ort">
  ## Use un ORT
</div>

Intercambia el ORT por un nuevo [token de acceso](/es/docs/secure/tokens/access-tokens) cuando el token de acceso actual caduque o esté a punto de hacerlo:

```bash
curl --request POST \
  --url 'https://<YOUR_DOMAIN>/oauth/token' \
  --header 'content-type: application/x-www-form-urlencoded' \
  --data grant_type=refresh_token \
  --data 'client_id=<YOUR_CLIENT_ID>' \
  --data 'refresh_token=<YOUR_ONLINE_REFRESH_TOKEN>'
```

La respuesta contiene un nuevo token de acceso.
Si tu solicitud incluye el scope `openid`, la respuesta incluye un nuevo [token de ID](/es/docs/secure/tokens/id-tokens):

```json
{
  "access_token": "eyJ...",
  "id_token": "eyJ...",
  "token_type": "Bearer",
  "expires_in": 86400
}
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Auth0 no emite un nuevo `refresh_token` y, como los ORT no rotan, debes seguir usando el mismo ORT en los intercambios posteriores.
</Callout>

<div id="session-extension-behavior">
  ### Comportamiento de la extensión de la sesión
</div>

Cada intercambio de tokens exitoso:

* **Restablece el tiempo de inactividad de la sesión:** El tiempo de inactividad se restablece a su duración completa.
* **No extiende la duración absoluta:** La expiración absoluta de la sesión no cambia.
* **Mantiene el SSO:** Otras aplicaciones pueden obtener tokens mediante SSO siempre que la sesión siga activa.

<div id="revoke-an-ort">
  ## Revocar un ORT
</div>

Cuando revocas un ORT, se cierra toda la sesión de Auth0, no solo el token. Esto invalida todos los ORT asociados a esa sesión y finaliza el SSO del usuario.

```bash
curl --request POST \
  --url 'https://<YOUR_DOMAIN>/oauth/revoke' \
  --header 'content-type: application/x-www-form-urlencoded' \
  --data 'client_id=<YOUR_CLIENT_ID>' \
  --data 'token=<YOUR_ONLINE_REFRESH_TOKEN>'
```

<div id="use-orts-with-actions">
  ## Use ORTs con Actions
</div>

Puede usar ORTs con Auth0 [Actions](/es/docs/customize/actions/actions-overview) y el [trigger de post-login](/es/docs/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger).

Con Actions, puede:

* Usar el objeto `event.refresh_token` para determinar si el token es un ORT.
* Acceder a datos específicos de la sesión mediante el objeto `event.session` para tomar decisiones en función del estado actual de la sesión

```javascript
exports.onExecutePostLogin = async (event, api) => {
  // Verificar si el token es un Token de actualización en línea
  
  if (event.refresh_token?.access == 'online') {
    // El token es un ORT, puedes referenciar event.session y api.session
    console.log('Exchanging Online Refresh Token bound to Session ID: ', event.session?.id);
    // Obtener los metadatos de sesión y agregarlos en los tokens
    // Suponiendo que los metadatos de sesión se almacenaron previamente
    const importantInformation = event.session?.metadata?.importantInformation;
    api.accessToken.setCustomClaim('info', importantInformation);
    api.idToken.setCustomClaim('info', importantInformation);
  }
};
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Auth0 revoca toda la sesión del usuario, no solo el token, cuando se usa el método `api.refreshToken.revoke()` con un ORT.
</Callout>

<div id="learn-more">
  ## Más información
</div>

* [Tokens de actualización en línea](/es/docs/secure/tokens/refresh-tokens/online-refresh-tokens/online-refresh-tokens)
* [Rotación de Tokens de actualización](/es/docs/secure/tokens/refresh-tokens/refresh-token-rotation)
* [Obtener Tokens de actualización](/es/docs/secure/tokens/refresh-tokens/get-refresh-tokens)
* [Usar Tokens de actualización](/es/docs/secure/tokens/refresh-tokens/use-refresh-tokens)
* [Ciclo de vida de la sesión](/es/docs/manage-users/sessions/session-lifecycle)
* [Configurar la autenticación silenciosa](/es/docs/authenticate/login/configure-silent-authentication)
* [Prácticas recomendadas sobre tokens](/es/docs/secure/tokens/token-best-practices)