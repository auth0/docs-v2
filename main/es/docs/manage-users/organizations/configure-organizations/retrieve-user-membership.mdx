---
description: Aprenda a recuperar las membresías de organización de un usuario mediante la Auth0 Management API.
title: Recuperar las membresías de organización de un usuario
---

import {AuthCodeBlock} from "/snippets/es/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/es/AuthCodeGroup.jsx";

Al trabajar con [organizaciones](/es/docs/manage-users/organizations/organizations-overview) de forma programática, puede que necesites recuperar una lista de las organizaciones de las que un usuario es miembro.

<div id="management-api">
  ## Management API
</div>

<Warning>
  Se pueden mostrar hasta 1000 miembros de la organización mediante Auth0 Dashboard o la Management API, aunque puede haber más.
</Warning>

Realiza una llamada `GET` al endpoint `Get User Organizations`. Asegúrate de reemplazar los valores de marcador de posición `USER_ID` y `MGMT_API_ACCESS_TOKEN` por el ID del usuario y tu <Tooltip tip="Management API: Un producto que permite a los clientes realizar tareas administrativas." cta="Ver glosario" href="/es/docs/glossary?term=Management+API">Management API</Tooltip> <Tooltip tip="Access Token: Credencial de autorización, en forma de una cadena opaca o JWT, que se utiliza para acceder a una API." cta="Ver glosario" href="/es/docs/glossary?term=Access+Token">Token de acceso</Tooltip>, respectivamente.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/users/USER_ID/organizations' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/users/USER_ID/organizations");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/users/USER_ID/organizations"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/users/USER_ID/organizations")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/users/USER_ID/organizations',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/users/USER_ID/organizations",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/users/USER_ID/organizations", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/users/USER_ID/organizations")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Encuentra tu dominio de Auth0**

  Tu dominio de Auth0 se compone del nombre de tu inquilino, tu subdominio regional (a menos que tu inquilino esté en la región de EE. UU. y se haya creado antes de junio de 2020) y `.auth0.com`. Por ejemplo, si el nombre de tu inquilino fuera `travel0`, el nombre de tu dominio de Auth0 sería `travel0.us.auth0.com`. (Si tu inquilino estuviera en EE. UU. y se hubiera creado antes de junio de 2020, entonces el nombre de tu dominio sería `https://travel0.auth0.com`.)

  Si usas dominios personalizados, este debe ser el nombre de tu dominio personalizado.
</Callout>

| Valor | Descripción |
| `USER_ID` | ID del usuario cuya pertenencia a una organización desea recuperar. |
| `MGMT_API_ACCESS_TOKEN` | [Token de acceso para la Management API](/es/docs/secure/tokens/access-tokens/management-api-access-tokens) con el scope `read:organizations`. |

<div id="response-status-codes">
  ### Códigos de estado de respuesta
</div>

Los posibles códigos de estado de respuesta son los siguientes:

| Código de estado | Código de error        | Mensaje                                                                                                      | Causa                                                                                                      |
| ---------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `200`            |                        | Organizaciones recuperadas correctamente.                                                                    |                                                                                                            |
| `400`            | `invalid_uri`          | `invalid_request_uri`                                                                                        | La ruta no es válida.                                                                                      |
| `400`            | `invalid_query_string` | Cadena de consulta de la solicitud no válida. El mensaje variará según la causa.                             | La cadena de consulta no es válida.                                                                        |
| `401`            |                        | Token no válido.                                                                                             |                                                                                                            |
| `401`            |                        | Se recibió una firma no válida para la validación de JSON Web Token.                                         |                                                                                                            |
| `403`            | `insufficient_scope`   | scope insuficiente; se esperaba alguno de los siguientes: `read:organizations`.                              | Se intentó leer o escribir un campo que no está permitido con los alcances del Bearer Token proporcionado. |
| `404`            |                        | Usuario no encontrado                                                                                        |                                                                                                            |
| `429`            |                        | Demasiadas solicitudes. Revise los encabezados X-RateLimit-Limit, X-RateLimit-Remaining y X-RateLimit-Reset. |                                                                                                            |