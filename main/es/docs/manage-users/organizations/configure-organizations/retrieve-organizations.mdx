---
description: Aprenda a obtener las organizaciones mediante el Auth0 Dashboard y la Management API.
title: Obtener organizaciones
---

import {AuthCodeBlock} from "/snippets/es/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/es/AuthCodeGroup.jsx";

Al trabajar con [organizaciones](/es/docs/manage-users/organizations/organizations-overview) de forma programática, puede que necesite obtener una lista de todas las organizaciones o una organización individual por nombre o ID.

<Warning>
  Solo debe accederse a la Management API mediante un cliente confidencial. Su uso está sujeto a los [límites de frecuencia de Auth0 Management API](/es/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy/management-api-endpoint-rate-limits).
</Warning>

<div id="retrieve-tenant-organizations">
  ## Obtener las organizaciones del inquilino
</div>

Puede obtener todas las organizaciones de un inquilino mediante la <Tooltip tip="Management API: un producto que permite a los clientes realizar tareas administrativas." cta="Ver glosario" href="/es/docs/glossary?term=Management+API">Management API</Tooltip>.

<Warning>
  Para obtener más de 1000 resultados, debe usar el método de paginación por checkpoint. Para obtener más información, consulte el [endpoint Get Organizations](https://auth0.com/docs/api/management/v2#!/Organizations/get_organizations) en nuestro API Explorer.
</Warning>

Haga una solicitud `GET` al endpoint `Get Organizations`. Asegúrese de reemplazar el valor del marcador de posición `MGMT_API_ACCESS_TOKEN` por su <Tooltip tip="Access Token: credencial de autorización, en forma de una cadena opaca o JWT, que se utiliza para acceder a una API." cta="Ver glosario" href="/es/docs/glossary?term=Access+Token">Token de acceso</Tooltip> de la Management API.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/organizations' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/organizations")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/organizations',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/organizations", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Busque su dominio de Auth0**

  Su dominio de Auth0 se compone del nombre de su inquilino, su subdominio regional (a menos que su inquilino esté en la región de EE. UU. y se haya creado antes de junio de 2020) y `.auth0.com`. Por ejemplo, si el nombre de su inquilino fuera `travel0`, el nombre de su dominio de Auth0 sería `travel0.us.auth0.com`. (Si su inquilino estuviera en EE. UU. y se hubiera creado antes de junio de 2020, entonces el nombre de su dominio sería `https://travel0.auth0.com`).

  Si usa dominios personalizados, este debe ser el nombre de su dominio personalizado.
</Callout>

| Valor                   | Descripción                                                                                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `MGMT_API_ACCESS_TOKEN` | [Token de acceso para la Management API](/es/docs/secure/tokens/access-tokens/management-api-access-tokens) con el scope `read:organizations`. |

<div id="response-status-codes">
  ### Códigos de estado de respuesta
</div>

Los posibles códigos de estado de respuesta son los siguientes:

| Código de estado | Código de error        | Message                                                                                                      | Causa                                                                                             |
| ---------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| `200`            |                        | Organizaciones recuperadas correctamente.                                                                    |                                                                                                   |
| `400`            | `invalid_paging`       | La página solicitada supera el máximo permitido de 1000 registros.                                           | La API está limitada a devolver solo hasta 1000 registros.                                        |
| `400`            | `invalid_query_string` | La cadena de consulta de la solicitud no es válida. El mensaje variará según la causa.                       | La cadena de consulta no es válida.                                                               |
| `401`            |                        | Token no válido.                                                                                             |                                                                                                   |
| `401`            |                        | Se recibió una firma no válida para la validación del JSON Web Token.                                        |                                                                                                   |
| `401`            |                        | El cliente no es global.                                                                                     |                                                                                                   |
| `403`            | `insufficient_scope`   | Scope insuficiente; se esperaba alguno de los siguientes: `read:organizations`.                              | Se intentó leer o escribir un campo no permitido con los alcances del Bearer Token proporcionado. |
| `429`            |                        | Demasiadas solicitudes. Revise los encabezados X-RateLimit-Limit, X-RateLimit-Remaining y X-RateLimit-Reset. |                                                                                                   |

<div id="retrieve-organization-by-id">
  ## Recuperar una organización por ID
</div>

Puede recuperar una organización por su ID mediante la Management API.

Haga una solicitud `GET` al endpoint `Get Organization`. Asegúrese de reemplazar los valores de marcador de posición `ORG_ID` y `MGMT_API_ACCESS_TOKEN` por el ID de su organización y su token de acceso de la Management API, respectivamente.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/organizations/ORG_ID' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations/ORG_ID");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations/ORG_ID"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/organizations/ORG_ID")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/organizations/ORG_ID',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations/ORG_ID",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/organizations/ORG_ID", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations/ORG_ID")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Encuentre su dominio de Auth0**

  Su dominio de Auth0 se compone del nombre de su inquilino, su subdominio regional (a menos que su inquilino esté en la región de EE. UU. y se haya creado antes de junio de 2020) y `.auth0.com`. Por ejemplo, si el nombre de su inquilino fuera `travel0`, el nombre de su dominio de Auth0 sería `travel0.us.auth0.com`. (Si su inquilino estuviera en EE. UU. y se hubiera creado antes de junio de 2020, entonces el nombre de su dominio sería `https://travel0.auth0.com`.)

  Si usa dominios personalizados, este debe ser el nombre de su dominio personalizado.
</Callout>

| Valor                   | Descripción                                                                                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `ORG_ID`                | ID de la organización que desea recuperar.                                                                                                  |
| `MGMT_API_ACCESS_TOKEN` | [Token de acceso para la Management API](/es/docs/secure/tokens/access-tokens/management-api-access-tokens) con el scope `read:organizations`. |

<div id="response-status-codes">
  ### Códigos de estado de respuesta
</div>

Los posibles códigos de estado de respuesta son los siguientes:

| Código de estado | Código de error        | Message                                                                                                      | Causa                                                                                                      |
| ---------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `200`            |                        | Organización obtenida correctamente.                                                                         |                                                                                                            |
| `400`            | `invalid_query_string` | Cadena de consulta de la solicitud no válida. El mensaje variará según la causa.                             | La cadena de consulta no es válida.                                                                        |
| `401`            |                        | Token no válido.                                                                                             |                                                                                                            |
| `401`            |                        | Se recibió una firma no válida para la validación del JSON Web Token.                                        |                                                                                                            |
| `401`            |                        | El cliente no es global.                                                                                     |                                                                                                            |
| `403`            | `insufficient_scope`   | Scope insuficiente; se esperaba alguno de los siguientes: `read:organizations`.                              | Se intentó leer o escribir un campo que no está permitido con los scopes del Bearer Token proporcionado. |
| `429`            |                        | Demasiadas solicitudes. Revise los encabezados X-RateLimit-Limit, X-RateLimit-Remaining y X-RateLimit-Reset. |                                                                                                            |

<div id="retrieve-organization-by-name">
  ## Recuperar una organización por nombre
</div>

Puede recuperar una organización por su nombre mediante la Management API.

Haga una solicitud `GET` al endpoint `Get Organization by Name`. Asegúrese de reemplazar los valores de marcador de posición `ORG_NAME` y `MGMT_API_ACCESS_TOKEN` por el nombre de su organización y su token de acceso de la Management API, respectivamente.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/organizations/name/ORG_NAME' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations/name/ORG_NAME");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations/name/ORG_NAME"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/organizations/name/ORG_NAME")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/organizations/name/ORG_NAME',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations/name/ORG_NAME",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/organizations/name/ORG_NAME", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations/name/ORG_NAME")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Encuentre su dominio de Auth0**

  Su dominio de Auth0 se compone del nombre de su inquilino, su subdominio regional (a menos que su inquilino esté en la región de EE. UU. y se haya creado antes de junio de 2020) y `.auth0.com`. Por ejemplo, si el nombre de su inquilino fuera `travel0`, el nombre de su dominio de Auth0 sería `travel0.us.auth0.com`. (Si su inquilino estuviera en EE. UU. y se hubiera creado antes de junio de 2020, entonces el nombre de su dominio sería `https://travel0.auth0.com`).

  Si usa dominios personalizados, este debe ser el nombre de su dominio personalizado.
</Callout>

| Valor                   | Descripción                                                                                                                                 |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `ORG_ NAME`             | Nombre de la organización que desea recuperar. Longitud máxima de 50 caracteres.                                                            |
| `MGMT_API_ACCESS_TOKEN` | [Token de acceso para la Management API](/es/docs/secure/tokens/access-tokens/management-api-access-tokens) con el scope `read:organizations`. |

<div id="response-status-codes">
  ### Códigos de estado de respuesta
</div>

Los posibles códigos de estado de respuesta son los siguientes:

| Código de estado | Código de error        | Message                                                                                                      | Causa                                                                                                      |
| ---------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `200`            |                        | Organizaciones recuperadas correctamente.                                                                    |                                                                                                            |
| `400`            | `invalid_query_string` | La cadena de consulta de la solicitud no es válida. El mensaje variará según la causa.                       | La cadena de consulta no es válida.                                                                        |
| `401`            |                        | Token no válido.                                                                                             |                                                                                                            |
| `401`            |                        | Se recibió una firma no válida para la validación de JSON Web Token.                                         |                                                                                                            |
| `401`            |                        | El cliente no es global.                                                                                     |                                                                                                            |
| `403`            | `insufficient_scope`   | Scope insuficiente; se esperaba alguno de los siguientes: `read:organizations`.                              | Se intentó leer o escribir un campo que no está permitido con los scopes del Bearer Token proporcionado. |
| `429`            |                        | Demasiadas solicitudes. Revise los encabezados X-RateLimit-Limit, X-RateLimit-Remaining y X-RateLimit-Reset. |                                                                                                            |