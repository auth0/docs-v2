---
description: La implementación en Python de la tarea cron del servidor para el escenario de arquitectura Cliente de servidor + API
title: 'Aplicaciones de servidor + API: implementación en Python para la tarea cron'
---

import {AuthCodeBlock} from "/snippets/es/AuthCodeBlock.jsx";

Como parte del [escenario de arquitectura de servidor + API](/es/docs/get-started/architecture-scenarios/server-application-api), explicaremos cómo implementar el proceso de servidor en Python. Consulta el documento del [escenario de arquitectura de servidor + API](/es/docs/get-started/architecture-scenarios/server-application-api) para obtener más información sobre la solución implementada.

El código fuente completo de la implementación en Python del proceso de servidor se encuentra en [este repositorio de GitHub](https://github.com/auth0-samples/auth0-pnp-exampleco-timesheets/tree/master/timesheets-cron/python).

<div id="get-an-access-token">
  ## Obtener un token de acceso
</div>

Para realizar la solicitud HTTP al endpoint de la API `/oauth/token` de Auth0, usaremos las bibliotecas `json`, `urllib` y `urllib2`.

Esta es una implementación de ejemplo:

export const codeExample = `def main():
  import json, urllib, urllib2, httplib

  # Valores de configuración
  domain = "{yourDomain}" # Tu dominio de Auth0
  api_identifier = "API_IDENTIFIER" # Identificador de API para tu API
  client_id = "{yourClientId}" # ID de cliente de tu aplicación de máquina a máquina
  client_secret = "{yourClientSecret}" # Secreto del cliente de tu aplicación de máquina a máquina
  api_url = "http://localhost:8080/timesheets/upload"
  grant_type = "client_credentials" # Flujo de OAuth 2.0 que se usará

  # Obtener un token de acceso de Auth0
  base_url = "https://{domain}".format(domain=domain)
  data = urllib.urlencode({'client_id': client_id,
                            'client_secret': client_secret,
                            'audience': api_identifier,
                            'grant_type': grant_type})
  req = urllib2.Request(base_url + "/oauth/token", data, headers={"Accept": "application/x-www-form-urlencoded"})
  response = urllib2.urlopen(req)
  resp_body = response.read()
  oauth = json.loads(resp_body)
  access_token = oauth['access_token']

# Código estándar para llamar a la función main().
if __name__ == '__main__':
  main()`;

<AuthCodeBlock children={codeExample} language="python" />

Para probarlo, modifica tu código para imprimir la variable `access_token` y ejecuta el proceso con `python cron.py`.

<div id="invoke-the-api">
  ## Invocar la API
</div>

Los pasos que seguimos en nuestra implementación son:

* Crear un objeto JSON que contenga los datos del registro de horas y asignarlo a una variable `timesheet`.
* Añadir la URL de la API y el contenido de la variable `timesheet` al cuerpo de la solicitud con `urllib2.Request`.
* Añadir el encabezado `Authorization` a la solicitud.
* Establecer el encabezado `Content-Type` en `application/json`.
* Invocar la API con `urllib2.urlopen` y agregar manejo de errores. Recuperar la respuesta con `json.loads` e imprimirla en la consola.

Esta es nuestra implementación de ejemplo (se omite parte del código por brevedad):

```python lines expandable
def main():
  # importar bibliotecas - código omitido

  # Valores de configuración - código omitido

  # Obtener un token de acceso de Auth0 - código omitido

  #Enviar nuevo registro de horas a la API
  timesheet = {'user_id': '007',
                          'date': '2017-05-10T17:40:20.095Z',
                          'project': 'StoreZero',
                          'hours': 5}
  req = urllib2.Request(api_url, data = json.dumps(timesheet))
  req.add_header('Authorization', 'Bearer ' + access_token)
  req.add_header('Content-Type', 'application/json')

  try:
    response = urllib2.urlopen(req)
    res = json.loads(response.read())
    print 'Created timesheet ' + str(res['id']) + ' for employee ' + str(res['user_id'])
  except urllib2.HTTPError, e:
    print 'HTTPError = ' + str(e.code) + ' ' + str(e.reason)
  except urllib2.URLError, e:
    print 'URLError = ' + str(e.reason)
  except httplib.HTTPException, e:
    print 'HTTPException'
  except Exception, e:
    print 'Generic Exception' + str(e)

# Código estándar para llamar a la función main() - código omitido
```

Para probarlo, asegúrate de que tu API esté en ejecución y ejecuta el proceso con `python cron.py`.

¡Eso es todo! ¡Ya terminaste!