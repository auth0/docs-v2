---
description: Aprende a proporcionar un cliente de red personalizado para personalizar las solicitudes realizadas por el SDK de Auth0 para Android
title: Cliente de red personalizado para Auth0.Android
---

import {AuthCodeBlock} from "/snippets/es/AuthCodeBlock.jsx";

Esta biblioteca le permite personalizar el comportamiento del cliente de red para configuraciones habituales, así como definir y usar su propia implementación de cliente de red.

Puede configurar la clase Auth0 con un `NetworkingClient`, que se utilizará al realizar peticiones. Puede configurar el cliente predeterminado con valores de tiempo de espera personalizados, los encabezados que deban enviarse en todas las peticiones y si se debe registrar información de las solicitudes y respuestas (solo con fines de depuración en entornos no productivos). Para configuraciones más avanzadas, puede proporcionar su propia implementación de `NetworkingClient`.

<div id="timeout-configuration">
  ### Configuración del tiempo de espera
</div>

export const codeExample1 = `val netClient = DefaultClient(
    connectTimeout = 30,
    readTimeout = 30
)

val account = Auth0("{yourClientId}", "{yourDomain}")
account.networkingClient = netClient`;

<AuthCodeBlock children={codeExample1} language="kotlin" />

<div id="logging-configuration">
  ### Configuración del logging
</div>

export const codeExample2 = `val netClient = DefaultClient(
    enableLogging = true
)

val account = Auth0("{yourClientId}", "{yourDomain}")
account.networkingClient = netClient`;

<AuthCodeBlock children={codeExample2} language="kotlin" />

<div id="set-additional-headers-for-all-requests">
  ### Establecer encabezados adicionales para todas las solicitudes
</div>

export const codeExample3 = `val netClient = DefaultClient(
    defaultHeaders = mapOf("{HEADER-NAME}" to "{HEADER-VALUE}")
)

val account = Auth0("{yourClientId}", "{yourDomain}")
account.networkingClient = netClient`;

<AuthCodeBlock children={codeExample3} language="kotlin" />

<div id="advanced-configuration">
  ### Configuración avanzada
</div>

Para una configuración más avanzada del cliente de red, puedes proporcionar una implementación personalizada de `NetworkingClient`. Esto puede resultar útil si quieres reutilizar tu propio cliente de red, configurar un proxy, entre otras cosas.

export const codeExample4 = `class CustomNetClient : NetworkingClient {
    override fun load(url: String, options: RequestOptions): ServerResponse {
        // Crea y ejecuta la solicitud a la URL especificada con las opciones indicadas
        val response = // ...

        // Devuelve un ServerResponse a partir de los datos de respuesta recibidos
        return ServerResponse(responseCode, responseBody, responseHeaders)        
    }
}

val account = Auth0("{yourClientId}", "{yourDomain}")
account.networkingClient = netClient`;

<AuthCodeBlock children={codeExample4} language="kotlin" />