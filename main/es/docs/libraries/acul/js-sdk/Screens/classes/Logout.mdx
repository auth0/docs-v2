---
title: "Cierre de sesión"
description: "Describe todas las propiedades y los métodos disponibles para personalizar la pantalla `logout` de Universal Login."
---

La clase Logout implementa la funcionalidad de la pantalla `logout`. Esta pantalla permite a los usuarios confirmar o rechazar la acción de cierre de sesión.

<Frame>
  <img style={{maxHeight:"400px"}} src="/docs/images/cdy7uua7fh8z/4fvafgoqxrpjSNPdR8tlGj/185beaff20e8fe2b23a4ca3810940fe9/Screenshot_2025-05-15_at_16.04.46.png" alt="" />
</Frame>

<div id="constructors">
  ## Constructores
</div>

Cree una instancia del administrador de la pantalla de cierre de sesión:

```typescript Example
import Logout from '@auth0/auth0-acul-js/logout';
const logoutManager = new Logout();
logoutManager.confirmLogout({
  action: 'accept',
});
```

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>}>
  Proporciona configuraciones relacionadas con la marca, como el tema y sus ajustes.
</ParamField>

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Proporciona configuraciones relacionadas con el cliente, como `id`, `name` y `logoUrl`, para la pantalla `logout`.
</ParamField>

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Proporciona información sobre la organización del usuario, como `id` y `name`.
</ParamField>

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Contiene datos sobre la pantalla actual del flujo de autenticación.
</ParamField>

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembers">ScreenMembers</a></span>}>
  Contiene detalles específicos de la pantalla `logout`, incluida su configuración y contexto.
</ParamField>

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>}>
  Contiene datos relacionados con el inquilino, como `id` y los metadatos asociados.
</ParamField>

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Proporciona datos específicos de la transacción, como identificadores activos y estados del flujo.
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>}>
  Contiene datos no confiables enviados al SDK, como la entrada del usuario durante el inicio de sesión.
</ParamField>

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>}>
  Contiene detalles del usuario activo, incluidos `username`, `email` y `roles`.
</ParamField>

<div id="methods">
  ## Métodos
</div>

<ParamField body="changeLanguage" type="Promise<void>">
  Este método cambia el idioma de visualización de la página de Universal Login.

  ```typescript Example
  import Logout from '@auth0/auth0-acul-js/logout';
  const logoutManager = new Logout();
  logoutManager.changeLanguage({
    language: 'fr',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [LanguageChangeOptions](/es/docs/libraries/acul/js-sdk/Screens/interfaces/LanguageChangeOptions).
    </ParamField>

    <ParamField body="language" type="string" required>
      El código de configuración regional del idioma deseado (por ejemplo, `'en'`, `'fr'`, `'es'`).
    </ParamField>

    <ParamField body="persist?" type="&#x22;session&#x22;">
      Si se establece en `'session'`, el idioma seleccionado se mantiene durante toda la sesión.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="confirmLogout" type="Promise<void>">
  Este método confirma la acción de cierre de sesión (aceptarla o rechazarla).

  ```typescript Example
  import Logout from '@auth0/auth0-acul-js/logout';
  const logoutManager = new Logout();
  logoutManager.confirmLogout({
    action: 'accept',
  });
  ```

  **Parámetros del método**

  <Expandable title="Parámetros">
    <ParamField body="options">
      [ConfirmLogoutOptions](/es/docs/libraries/acul/js-sdk/Screens/interfaces/ConfirmLogoutOptions).
    </ParamField>

    <ParamField body="action" type="&#x22;accept&#x22; | &#x22;deny&#x22;" required>
      El estado de la transacción de autenticación.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="getErrors" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/Error">Error</a>[]</span>}>
  Este método recupera el array de errores de la transacción desde el contexto, o un array vacío si no hay ninguno. Devuelve un array de objetos de error del contexto de la transacción.
</ParamField>