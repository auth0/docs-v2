---
title: "MfaWebAuthnRoamingChallengeMembers"
---

MfaWebAuthnRoamingChallengeMembers

````ts Example
export interface MfaWebAuthnRoamingChallengeMembers extends BaseMembers {
  /**
   * Screen-specific properties and data, including opciones del desafío de WebAuthn.
   * @type {ScreenMembersOnMfaWebAuthnRoamingChallenge}
   */
  screen: ScreenMembersOnMfaWebAuthnRoamingChallenge;

  /**
   * Inicia el desafío de WebAuthn llamando a `navigator.credentials.get()` con las
   * opciones proporcionadas en `screen.data.publicKeyChallengeOptions`.
   * Si tiene éxito, envía la credencial resultante a Auth0 con `action: "default"`.
   * Si `navigator.credentials.get()` falla con un error conocido de la API de WebAuthn (como `NotAllowedError`),
   * este método llamará internamente a `reportWebAuthnError` para informar a Auth0.
   *
   * @param {VerifySecurityKeyOptions} [options] - Parámetros opcionales para el proceso de verificación,
   * como `rememberDevice` y otras opciones personalizadas.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se envía el intento de verificación.
   *                          No devuelve datos directamente, ya que una operación exitosa generalmente resulta en una redirección.
   * @throws {Error} Lanza un error si `screen.data.publicKeyChallengeOptions` no está disponible,
   *                 si `navigator.credentials.get()` falla con un error inesperado,
   *                 o si el envío del formulario a Auth0 falla.
   *
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnRoamingChallenge
   * try {
   *   await sdk.verify({ rememberDevice: true });
   *   // On success, Auth0 handles redirection.
   * } catch (error) {
   *   console.error("Security key verification failed:", error);
   *   // Check sdk.transaction.errors for server-side validation messages if the page reloads.
   * }
   * ```
   */
  verify(options?: VerifySecurityKeyOptions): Promise<void>;

  /**
   * Reports a specific API de WebAuthn error to Auth0.
   * This method should be used if `navigator.credentials.get()` fails in a way that
   * `verify()` doesn't automatically handle, or if the developer wants to explicitly
   * report an error before trying another action.
   * It submits the error details with `action: "showError::{errorDetails}"`.
   *
   * @param {ReportWebAuthnErrorOptions} options - Contains the `error` object with `name` and `message`
   * from the API de WebAuthn, and any other custom options.
   * @returns {Promise<void>} A promise that resolves when the error report is submitted.
   * @throws {Error} Throws an error if the form submission fails.
   *
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnRoamingChallenge
   * // And webAuthnError is an error object from a failed navigator.credentials.get() call.
   * try {
   *   await sdk.reportWebAuthnError({
   *     error: { name: webAuthnError.name, message: webAuthnError.message }
   *   });
   * } catch (submitError) {
   *   console.error("Failed to report WebAuthn error:", submitError);
   * }
   * ```
   */
  reportWebAuthnError(options: ReportWebAuthnErrorOptions): Promise<void>;

  /**
   * Allows the user to opt-out of the WebAuthn challenge and select a different MFA method.
   * This action submits `action: "pick-authenticator"` to Auth0, which should navigate
   * the user to an pantalla de selección de factor de MFA.
   *
   * @param {TryAnotherMethodOptions} [options] - Optional custom parameters for the request.
   * @returns {Promise<void>} A promise that resolves when the action is submitted.
   * @throws {Error} Throws an error if the form submission fails.
   *
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnRoamingChallenge
   * try {
   *   await sdk.tryAnotherMethod();
   *   // On success, Auth0 handles redirection to MFA selection.
   * } catch (error) {
   *   console.error("Failed to switch MFA method:", error);
   * }
   * ```
   */
  tryAnotherMethod(options?: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnRoamingChallenge">ScreenMembersOnMfaWebAuthnRoamingChallenge</a></span>}>
  Propiedades y datos específicos de la pantalla, incluidas las opciones del desafío de WebAuthn.
</ParamField>

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="reportWebAuthnError" type="Promise<void>">
  Notifica a Auth0 un error específico de la API de WebAuthn.
  Este método debe usarse si `navigator.credentials.get()` falla de una forma que
  `verify()` no controla automáticamente, o si el desarrollador quiere
  notificar explícitamente un error antes de intentar otra acción.
  Envía los detalles del error con `action: "showError::{errorDetails}"`.

  Una promesa que se resuelve cuando se envía el informe del error.

  #### Lanza

  Lanza un error si falla el envío del formulario.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnRoamingChallenge
  // Y que webAuthnError es un objeto de error de una llamada fallida a navigator.credentials.get().
  try {
    await sdk.reportWebAuthnError({
      error: { name: webAuthnError.name, message: webAuthnError.message }
    });
  } catch (submitError) {
    console.error("Failed to report WebAuthn error:", submitError);
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="options" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnRoamingChallengeReportErrorOptions">MfaWebAuthnRoamingChallengeReportErrorOptions</a></span>}>
      Contiene el objeto `error` con `name` y `message`
      de la API de WebAuthn, además de cualquier otra opción personalizada.

      **Propiedades**

      <ParamField body="error" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        El objeto de error de la API de WebAuthn que se notificará.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permite al usuario omitir el desafío de WebAuthn y seleccionar otro método de MFA.
  Esta acción envía `action: "pick-authenticator"` a Auth0, lo que debería dirigir
  al usuario a una pantalla de selección de factor de MFA.

  Una promesa que se resuelve cuando se envía la acción.

  #### Lanza

  Lanza un error si falla el envío del formulario.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnRoamingChallenge
  try {
    await sdk.tryAnotherMethod();
    // Si se completa correctamente, Auth0 gestiona la redirección a la selección de MFA.
  } catch (error) {
    console.error("Failed to switch MFA method:", error);
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnRoamingChallengeTryAnotherMethodOptions">MfaWebAuthnRoamingChallengeTryAnotherMethodOptions</a></span>}>
      Parámetros personalizados opcionales para la solicitud.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="verify" type="Promise<void>">
  Inicia el desafío de WebAuthn llamando a `navigator.credentials.get()` con las
  opciones proporcionadas en `screen.data.publicKeyChallengeOptions`.
  Si se completa correctamente, envía la credencial resultante a Auth0 con `action: "default"`.
  Si `navigator.credentials.get()` falla con un error conocido de la API de WebAuthn (como `NotAllowedError`),
  este método llamará internamente a `reportWebAuthnError` para notificárselo a Auth0.

  Una promesa que se resuelve cuando se envía el intento de verificación.
  No devuelve datos directamente, ya que una operación correcta normalmente da lugar a una redirección.

  #### Lanza

  Lanza un error si falta `screen.data.publicKeyChallengeOptions`,
  si `navigator.credentials.get()` falla con un error inesperado,
  o si falla el envío del formulario a Auth0.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnRoamingChallenge
  try {
    await sdk.verify({ rememberDevice: true });
    // Si se completa correctamente, Auth0 gestiona la redirección.
  } catch (error) {
    console.error("Security key verification failed:", error);
    // Comprueba sdk.transaction.errors para ver los mensajes de validación del lado del servidor si la página se recarga.
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="options?" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/VerifySecurityKeyOptions">VerifySecurityKeyOptions</a></span>}>
      Parámetros opcionales para el proceso de verificación,
      como `rememberDevice` y otras opciones personalizadas.

      **Propiedades**

      <ParamField body="rememberDevice?" type="boolean">
        Opcional. Si es true, intenta recordar el navegador para futuros desafíos de MFA.
        Corresponde al campo de formulario `rememberBrowser`.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>