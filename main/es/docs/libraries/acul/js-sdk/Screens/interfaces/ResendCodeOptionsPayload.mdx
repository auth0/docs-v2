---
title: "ResendCodeOptionsPayload"
---

ResendCodeOptions
description Opciones para el método `resendCode` en la pantalla de verificación del correo electrónico de inicio de sesión.
Esta operación se utiliza cuando el usuario solicita que se envíe un nuevo code de verificación a su correo electrónico,
por ejemplo, si no recibió el code inicial o si este ha expirado.

```ts Example
export interface ResendCodeOptions extends CustomOptions {}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`