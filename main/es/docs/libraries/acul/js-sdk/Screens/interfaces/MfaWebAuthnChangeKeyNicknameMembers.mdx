---
title: "MfaWebAuthnChangeKeyNicknameMembers"
---

MfaWebAuthnChangeKeyNicknameMembers

````ts Example
export interface MfaWebAuthnChangeKeyNicknameMembers extends BaseMembers {
  /**
   * El objeto de pantalla con la estructura de datos específica de MFA WebAuthn Change Key Nickname.
   */
  screen: ScreenMembersOnMfaWebAuthnChangeKeyNickname;
  
  /**
   * Envía el nuevo apodo para la clave de seguridad WebAuthn.
   * Esta acción corresponde al usuario que ingresa un nuevo apodo y hace clic en el botón "Guardar" o "Continuar".
   * El SDK enviará este nuevo apodo mediante POST al endpoint `/u/mfa-webauthn-change-key-nickname` de Auth0.
   *
   * Si el nuevo apodo es válido y la actualización se realiza correctamente, Auth0 redirigirá al usuario
   * a la siguiente pantalla correspondiente (por ejemplo, de vuelta a la gestión de factores MFA o a una confirmación de éxito).
   * Si el apodo no es válido (por ejemplo, vacío, demasiado largo o demasiado corto), Auth0 volverá a renderizar la
   * pantalla 'mfa-webauthn-change-key-nickname', y el array `transaction.errors` en el contexto
   * del SDK se actualizará con los detalles del error de validación.
   *
   * @param {ContinueOptions} payload - Un objeto que contiene el `nickname` (string) ingresado por el usuario.
   *                                    También puede incluir cualquier `CustomOptions` para extensibilidad.
   * @returns {Promise<void>} Una promesa que se resuelve cuando se inicia el envío del formulario.
   *                          No devuelve datos directamente al resolverse, ya que el resultado habitual
   *                          es una redirección o un nuevo renderizado de la página.
   * @throws {Error} Lanza un error si `payload.nickname` no se proporciona o no es un string,
   *                 o si el `FormHandler` encuentra un problema irrecuperable durante el envío (por ejemplo, un error de red).
   *                 Los errores de validación de Auth0 (como un apodo no válido) no se lanzan como errores de JavaScript,
   *                 sino que se reflejan en `this.transaction.errors` tras la operación.
   *
   * @example
   * ```typescript
   * // Se asume que 'sdk' es una instancia de MfaWebAuthnChangeKeyNickname
   * const newNickname = "My Favorite YubiKey"; // Valor proveniente de la entrada del usuario
   * try {
   *   await sdk.continueWithNewNickname({ nickname: newNickname });
   *   // Si tiene éxito, la página redirige.
   * } catch (error) {
   *   // Este catch es para errores inesperados durante la llamada al SDK.
   *   console.error("Failed to submit the new nickname:", error);
   * }
   * // Tras el await, verifique siempre sdk.transaction.errors para detectar problemas de validación del lado del servidor.
   * if (sdk.transaction.errors && sdk.transaction.errors.length > 0) {
   *   sdk.transaction.errors.forEach(err => {
   *     if (err.field === 'nickname') {
   *       // Mostrar err.message relacionado con el campo de entrada del apodo.
   *       // p. ej., "El nombre es demasiado corto", "El nombre es obligatorio"
   *     }
   *   });
   * }
   * ```
   */
  continueWithNewNickname(payload: ContinueOptions): Promise<void>;
}
````

<div id="properties">
  ## Propiedades
</div>

<ParamField body="branding" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnChangeKeyNickname">ScreenMembersOnMfaWebAuthnChangeKeyNickname</a></span>}>
  El objeto pantalla con la estructura de datos específica de la pantalla MFA WebAuthn Change Key Nickname.
</ParamField>

<ParamField body="tenant" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Métodos
</div>

<ParamField body="continueWithNewNickname" type="Promise<void>">
  Envía el nuevo apodo de la clave de seguridad WebAuthn.
  Esta acción corresponde a que el usuario introduzca un nuevo apodo y haga clic en un botón &quot;Guardar&quot; o &quot;Continuar&quot;.
  El SDK enviará mediante POST este nuevo apodo al endpoint `/u/mfa-webauthn-change-key-nickname` de Auth0.

  Si el nuevo apodo es válido y la actualización se realiza correctamente, Auth0 normalmente redirigirá al usuario
  a la siguiente pantalla correspondiente (por ejemplo, de vuelta a la administración de factores de MFA o a una confirmación de éxito).
  Si el apodo no es válido (por ejemplo, está vacío, es demasiado largo o demasiado corto), Auth0 volverá a mostrar la
  pantalla &#39;mfa-webauthn-change-key-nickname&#39;, y el array `transaction.errors` en el contexto del SDK
  se actualizará con detalles sobre el error de validación.

  Una promesa que se resuelve cuando se inicia el envío del formulario.
  No devuelve datos directamente al resolverse, ya que una redirección o
  la recarga de la página es el resultado habitual.

  #### Genera

  Genera un error si no se proporciona `payload.nickname` o si no es una cadena,
  o si `FormHandler` encuentra un problema irrecuperable durante el envío (por ejemplo, un error de red).
  Los errores de validación de Auth0 (como un apodo no válido) no se generan como errores de JavaScript,
  sino que se reflejan en `this.transaction.errors` después de la operación.

  ```typescript Example
  // Suponiendo que 'sdk' es una instancia de MfaWebAuthnChangeKeyNickname
  const newNickname = "My Favorite YubiKey"; // Valor introducido por el usuario
  try {
    await sdk.continueWithNewNickname({ nickname: newNickname });
    // Si se realiza correctamente, la página redirige.
  } catch (error) {
    // Este catch es para errores inesperados durante la propia llamada al SDK.
    console.error("Failed to submit the new nickname:", error);
  }
  // Después del await, comprueba siempre sdk.transaction.errors para detectar problemas de validación del lado del servidor.
  if (sdk.transaction.errors && sdk.transaction.errors.length > 0) {
    sdk.transaction.errors.forEach(err => {
      if (err.field === 'nickname') {
        // Muestra err.message relacionado con el campo de entrada del apodo.
        // p. ej., "Name is too short", "Name is required"
      }
    });
  }
  ```

  <Expandable title="Parámetros">
    <ParamField body="payload" type={<span><a href="/es/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnChangeKeyNicknameContinueOptions">MfaWebAuthnChangeKeyNicknameContinueOptions</a></span>}>
      Un objeto que contiene el `nickname` (string) introducido por el usuario.
      También puede incluir cualquier `CustomOptions` para extensibilidad.

      **Propiedades**

      <ParamField body="nickname" type="string">
        El nuevo apodo de la clave de seguridad WebAuthn.
        Este es el valor introducido por el usuario en el campo de entrada.
        Debe cumplir las restricciones de longitud y caracteres definidas por el servidor.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>