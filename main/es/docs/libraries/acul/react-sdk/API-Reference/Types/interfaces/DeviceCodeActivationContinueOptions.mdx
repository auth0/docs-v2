---
title: "DeviceCodeActivationContinueOptions"
---

carga útil para continuar con la activación del código de dispositivo.

Esta interfaz extiende `CustomOptions`, lo que significa que todas las propiedades de las opciones personalizadas
se incorporan al nivel superior del objeto. Esto evita el anidamiento y proporciona
una estructura más simple para quienes consumen esta interfaz.

```ts Example
export interface ContinueOptions extends CustomOptions {
  code: string;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string" />