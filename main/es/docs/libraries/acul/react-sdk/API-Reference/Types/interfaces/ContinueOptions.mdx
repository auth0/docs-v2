---
title: "ContinueOptions"
---

Opciones para continuar con el desafío por correo electrónico.

```ts Example
export interface ContinueOptions {
  /** El code introducido por el usuario. */
  code: string;
  /** Indica si se debe recordar el dispositivo. */
  rememberDevice?: boolean;
  /** Cualquier opción personalizada adicional */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Cualquier otra opción personalizada

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string">
  El code introducido por el usuario.
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indica si se debe recordar el dispositivo.
</ParamField>