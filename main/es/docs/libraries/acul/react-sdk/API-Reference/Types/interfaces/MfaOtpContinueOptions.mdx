---
title: "MfaOtpContinueOptions"
---

Opciones para continuar con el código de inscripción en MFA con OTP.

```ts Example
export interface ContinueOptions {
  /**
   * El código OTP introducido por el usuario.
   */
  code: string;

  /**
   * Cualquier opción personalizada adicional.
   */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Cualquier otra opción personalizada.

<div id="properties">
  ## Propiedades
</div>

<ParamField body="code" type="string">
  El código OTP que introdujo el usuario.
</ParamField>