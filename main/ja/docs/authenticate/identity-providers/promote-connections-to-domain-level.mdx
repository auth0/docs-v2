---
description: Auth0 Dashboard または Auth0 Management API を使用して、接続をドメインレベルに引き上げる方法について説明します。
title: 接続をドメインレベルに引き上げる
---

import {AuthCodeBlock} from "/snippets/ja/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/ja/AuthCodeGroup.jsx";

接続をドメインレベルに昇格すると、テナント内の任意の[サードパーティアプリケーション](/ja/docs/get-started/applications/third-party-applications)が、その接続を使ってユーザーを認証できるようになります。これにより、サードパーティアプリケーションはお使いのIDプロバイダーを利用できるようになります。サードパーティアプリケーションが使用できるのは、ドメインレベルの接続のみです。

動的に登録されたクライアントは、作成時に個別の接続へマッピングできないため、[Dynamic Client Registration](/ja/docs/get-started/applications/dynamic-client-registration)を使用する場合は、接続をドメインレベルに昇格する必要があります。

接続は、<Tooltip tip="Auth0 Dashboard: Auth0 のサービスを設定するための主要な製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip>または<Tooltip tip="Management API: 管理タスクを実行するための製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Management+API">Management API</Tooltip>を使用して昇格できます。

<Tabs>
  <Tab title="Auth0 Dashboard">
    Auth0 Dashboard から直接、接続をドメインレベルに昇格できます。このオプションは、すべての接続タイプ (データベース、ソーシャル、エンタープライズ、パスワードレス) で利用できます。

    1. **Dashboard &gt; Authentication** に移動し、接続タイプ (例: **Database, Social, Enterprise**) を選択します。
    2. 新しい接続を作成するか、設定する既存の接続を選択します。
    3. **Settings** タブを開きます。
    4. 下にスクロールして **Promote Connection to Domain Level** 設定を見つけます。通常、この設定は **Advanced** セクションにあります。
    5. トグルスイッチを有効にします。

    <Frame>![接続をドメインレベルに昇格](/docs/images/cdy7uua7fh8z/promote-connection-to-domain-level.png)</Frame>

    6. **Save Changes** をクリックします。
  </Tab>

  <Tab title="Management API">
    または、Management API の [Update a Connection エンドポイント](https://auth0.com/docs/api/management/v2#!/Connections/patch_connections_by_id) に `PATCH` 呼び出しを行うことで、接続を昇格させることもできます。`CONNECTION_ID` および `MGMT_API_ACCESS_TOKEN` のプレースホルダー値は、実際の情報に置き換えてください。

    <AuthCodeGroup>
      ```bash cURL
      curl --request PATCH \
        --url 'https://{yourDomain}/api/v2/connections/CONNECTION_ID' \
        --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN' \
        --header 'cache-control: no-cache' \
        --header 'content-type: application/json' \
        --data '{ "is_domain_connection": true }'
      ```

      ```csharp C#
      var client = new RestClient("https://{yourDomain}/api/v2/connections/CONNECTION_ID");
      var request = new RestRequest(Method.PATCH);
      request.AddHeader("content-type", "application/json");
      request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
      request.AddHeader("cache-control", "no-cache");
      request.AddParameter("application/json", "{ "is_domain_connection": true }", ParameterType.RequestBody);
      IRestResponse response = client.Execute(request);
      ```

      ```go Go
      package main

      import (
        "fmt"
        "strings"
        "net/http"
        "io/ioutil"
      )

      func main() {

        url := "https://{yourDomain}/api/v2/connections/CONNECTION_ID"

        payload := strings.NewReader("{ "is_domain_connection": true }")

        req, _ := http.NewRequest("PATCH", url, payload)

        req.Header.Add("content-type", "application/json")
        req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
        req.Header.Add("cache-control", "no-cache")

        res, _ := http.DefaultClient.Do(req)

        defer res.Body.Close()
        body, _ := ioutil.ReadAll(res.Body)

        fmt.Println(res)
        fmt.Println(string(body))

      }
      ```

      ```java Java
      HttpResponse<String> response = Unirest.patch("https://{yourDomain}/api/v2/connections/CONNECTION_ID")
        .header("content-type", "application/json")
        .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
        .header("cache-control", "no-cache")
        .body("{ "is_domain_connection": true }")
        .asString();
      ```

      ```javascript Node.JS
      var axios = require("axios").default;

      var options = {
        method: 'PATCH',
        url: 'https://{yourDomain}/api/v2/connections/CONNECTION_ID',
        headers: {
          'content-type': 'application/json',
          authorization: 'Bearer MGMT_API_ACCESS_TOKEN',
          'cache-control': 'no-cache'
        },
        data: {is_domain_connection: true}
      };

      axios.request(options).then(function (response) {
        console.log(response.data);
      }).catch(function (error) {
        console.error(error);
      });
      ```

      ```php PHP
      $curl = curl_init();

      curl_setopt_array($curl, [
        CURLOPT_URL => "https://{yourDomain}/api/v2/connections/CONNECTION_ID",
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_ENCODING => "",
        CURLOPT_MAXREDIRS => 10,
        CURLOPT_TIMEOUT => 30,
        CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
        CURLOPT_CUSTOMREQUEST => "PATCH",
        CURLOPT_POSTFIELDS => "{ "is_domain_connection": true }",
        CURLOPT_HTTPHEADER => [
          "authorization: Bearer MGMT_API_ACCESS_TOKEN",
          "cache-control: no-cache",
          "content-type: application/json"
        ],
      ]);

      $response = curl_exec($curl);
      $err = curl_error($curl);

      curl_close($curl);

      if ($err) {
        echo "cURL Error #:" . $err;
      } else {
        echo $response;
      }
      ```

      ```python Python
      import http.client

      conn = http.client.HTTPSConnection("")

      payload = "{ "is_domain_connection": true }"

      headers = {
          'content-type': "application/json",
          'authorization': "Bearer MGMT_API_ACCESS_TOKEN",
          'cache-control': "no-cache"
          }

      conn.request("PATCH", "/{yourDomain}/api/v2/connections/CONNECTION_ID", payload, headers)

      res = conn.getresponse()
      data = res.read()

      print(data.decode("utf-8"))
      ```

      ```ruby Ruby
      require 'uri'
      require 'net/http'
      require 'openssl'

      url = URI("https://{yourDomain}/api/v2/connections/CONNECTION_ID")

      http = Net::HTTP.new(url.host, url.port)
      http.use_ssl = true
      http.verify_mode = OpenSSL::SSL::VERIFY_NONE

      request = Net::HTTP::Patch.new(url)
      request["content-type"] = 'application/json'
      request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'
      request["cache-control"] = 'no-cache'
      request.body = "{ "is_domain_connection": true }"

      response = http.request(request)
      puts response.read_body
      ```
    </AuthCodeGroup>

    | 値                       | 説明                                                                                                                                       |
    | ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
    | `CONNECTION_ID`         | 昇格する接続のID。                                                                                                                               |
    | `MGMT_API_ACCESS_TOKEN` | [スコープ](/ja/docs/glossary?term=scope) `update:connections` を持つ、[Management API 用のアクセストークン](https://auth0.com/docs/api/management/v2/tokens)。 |
  </Tab>
</Tabs>