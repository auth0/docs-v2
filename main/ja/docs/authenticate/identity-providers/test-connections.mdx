---
description: Dashboard から Auth0 の接続をテストし、設定済みの IDプロバイダーに対して認証が正常に行われ、有効なユーザープロファイルが返されることを確認します。
title: 接続をテストする
---

import {AuthCodeBlock} from "/snippets/ja/AuthCodeBlock.jsx";

export const codeExample = `https://{yourDomain}/authorize?response_type=token&scope=openid%20profile&client_id={yourClientId}&redirect_uri=http%3A%2F%2Fjwt.io&connection={connectionToTest}`;

通常、接続をテストするには、<Tooltip tip="Auth0 Dashboard: サービスを設定するための Auth0 の主要製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip> にログインする必要があります。

1. [Auth0 Dashboard](https://manage.auth0.com/#) に移動し、**Authentication** を選択してから、テストする接続の種類を選択します。
2. テストする接続を見つけて、その **More Options** メニュー (**...**) から **Try Connection** を選択します。
3. IDプロバイダーにログインします。
4. 結果が表示される **It Works!** ページが表示されるまで待ちます。

Auth0 は、アプリケーションであるかのように認証フローをシミュレートし、認証に成功した結果として生成されるユーザープロファイルを表示します。

<div id="test-partner-connections">
  ## パートナー接続をテストする
</div>

他者が所有する接続をテストしていて、その相手先のテスト用認証情報を持っていない場合は、Auth0 Dashboard にログインしたままテストすることはできません。これは、<Tooltip tip="Security Assertion Markup Language (SAML): パスワードなしで 2 者間が認証情報を交換できるようにする標準化されたプロトコル。" cta="用語集を見る" href="/ja/docs/glossary?term=SAML">SAML</Tooltip> <Tooltip tip="Security Assertion Markup Language (SAML): パスワードなしで 2 者間が認証情報を交換できるようにする標準化されたプロトコル。" cta="用語集を見る" href="/ja/docs/glossary?term=Identity+Providers">IDプロバイダー</Tooltip> (IdPs) や Active Directory などの Enterprise 接続に接続する場合によくあります。対処方法は、パートナーに新しい接続をテストしてもらうことです。

1. テスト用アプリを登録します。

   * [Auth0 Dashboard &gt; Applications &gt; Applications](https://manage.auth0.com/#/applications) に移動し、**Create Application** を選択します。名前は任意です (たとえば `Test App`) 。
   * 新しく作成したアプリの設定で、**Allowed Callback URLs** を `http://jwt.io` に設定します。
   * **Save Changes** を選択します。
2. ログイン用リンクをパートナーに送信します。

   <AuthCodeBlock children={codeExample} language="http" />

   次の 2 つのパラメーターを置き換えます。

   * `client_id`: 手順 1 で作成したアプリケーションのクライアントID。
   * `connection`: テストする接続の名前。
3. 接続をテストします。パートナーがリンクを開くと、設定されている IDプロバイダー (接続) にリダイレクトされます。認証に成功すると、[JWT.io](http://jwt.io) に戻り、そこでトークンからすべてのユーザー プロパティがデコードされます。

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  このテスト用アプリは実際のアプリではありません。[JWT.io](http://jwt.io) は、URL フラグメントで送信されたトークンをデコードするテスト用 Web サイトです。
</Callout>