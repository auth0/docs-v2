---
title: "MfaWebAuthnErrorMembers"
---

MfaWebAuthnErrorMembers

````ts Example
export interface MfaWebAuthnErrorMembers extends BaseMembers {
  /**
   * MFA WebAuthn エラー固有のデータ構造を持つ画面オブジェクト。
   */
  screen: ScreenMembersOnMfaWebAuthnError;
  
  /**
   * ユーザーが直前の WebAuthn 操作を再試行できるようにします。
   * 通常、WebAuthn 操作が開始された画面にユーザーをリダイレクトします。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるオプションのカスタムオプション。
   * @returns {Promise<void>} アクションが正常に送信されたときに解決される Promise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.tryAgain();
   * ```
   */
  tryAgain(payload?: CustomOptions): Promise<void>;

  /**
   * 設定されている場合、ユーザーが代替手段としてパスワードベースの認証を選択できるようにします。
   * 通常、プライマリ認証または前のステップでパスワードが使用されている場合に利用可能です。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるオプションのカスタムオプション。
   * @returns {Promise<void>} アクションが正常に送信されたときに解決される Promise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.usePassword();
   * ```
   */
  usePassword(payload?: CustomOptions): Promise<void>;

  /**
   * 他の要素が登録済みまたは利用可能な場合、ユーザーが別の MFA 方式を選択できるようにします。
   * ユーザーを認証方式の選択画面に遷移させます。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるオプションのカスタムオプション。
   * @returns {Promise<void>} アクションが正常に送信されたときに解決される Promise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: CustomOptions): Promise<void>;

  /**
   * ユーザーがデバイスの追加または WebAuthn ステップの完了を拒否し、現在の WebAuthn フローを実質的にスキップできるようにします。
   * このアクションは通常、ユーザーが続行しないことを選択した登録シナリオで使用されます。
   *
   * @param {CustomOptions} [payload] - リクエストに含めるオプションのカスタムオプション。
   * @returns {Promise<void>} アクションが正常に送信されたときに解決される Promise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.noThanks();
   * ```
   */
  noThanks(payload?: CustomOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnError">ScreenMembersOnMfaWebAuthnError</a></span>}>
  MFA WebAuthn Error 固有のデータ構造を持つ 画面 オブジェクト。
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="noThanks" type="Promise<void>">
  ユーザーがデバイスの追加や WebAuthn ステップの完了を拒否し、現在の WebAuthn フローを辞退できるようにします。
  このアクションは通常、登録シナリオでユーザーが続行しないことを選択した場合に使用されます。

  アクションが正常に送信されると解決される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.noThanks();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプションです。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAgain" type="Promise<void>">
  ユーザーが直前の WebAuthn 操作を再試行できるようにします。
  通常、ユーザーは WebAuthn 操作を開始した画面に戻ります。

  アクションが正常に送信されると解決される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.tryAgain();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプションです。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  他の要素が登録済みまたは利用可能な場合に、ユーザーが別の MFA 方式を選択できるようにします。
  これにより、ユーザーは認証方式の選択画面に移動します。

  アクションが正常に送信されると解決される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.tryAnotherMethod();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプションです。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="usePassword" type="Promise<void>">
  設定されている場合、ユーザーが代替手段としてパスワードベースの認証を選択できるようにします。
  通常、これはプライマリ認証または前のステップでパスワードが使用された場合に利用できます。

  アクションが正常に送信されると解決される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnError のインスタンスであると仮定します
  await sdk.usePassword();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能なカスタムオプションです。
    </ParamField>
  </Expandable>
</ParamField>