---
title: "MfaWebAuthnChangeKeyNicknameMembers"
---

MfaWebAuthnChangeKeyNicknameMembers

````ts Example
export interface MfaWebAuthnChangeKeyNicknameMembers extends BaseMembers {
  /**
   * MFA WebAuthn Change Key Nickname 固有のデータ構造を持つ画面オブジェクト。
   */
  screen: ScreenMembersOnMfaWebAuthnChangeKeyNickname;
  
  /**
   * WebAuthn セキュリティキーの新しいニックネームを送信します。
   * このアクションは、ユーザーが新しいニックネームを入力して「保存」または「続行」ボタンをクリックする操作に対応します。
   * SDK はこの新しいニックネームを Auth0 の `/u/mfa-webauthn-change-key-nickname` エンドポイントに POST します。
   *
   * 新しいニックネームが有効で更新が成功した場合、Auth0 は通常、ユーザーを次の適切な画面
   * （例: MFA 認証要素管理画面または成功確認画面）にリダイレクトします。
   * ニックネームが無効な場合（例: 空、長すぎる、短すぎる）、Auth0 は
   * 'mfa-webauthn-change-key-nickname' 画面を再レンダリングし、SDK の
   * コンテキスト内の `transaction.errors` 配列が検証失敗の詳細で更新されます。
   *
   * @param {ContinueOptions} payload - ユーザーが入力した `nickname`（文字列）を含むオブジェクト。
   *                                    拡張機能のために任意の `CustomOptions` を含めることもできます。
   * @returns {Promise<void>} フォームの送信が開始されたときに解決される Promise。
   *                          リダイレクトまたはページの再レンダリングが一般的な結果となるため、
   *                          解決時にデータを直接返しません。
   * @throws {Error} `payload.nickname` が指定されていないか文字列でない場合、
   *                 または `FormHandler` が送信中に回復不能な問題（例: ネットワークエラー）に
   *                 遭遇した場合にエラーをスローします。
   *                 Auth0 からの検証エラー（無効なニックネームなど）は JavaScript エラーとしてスローされず、
   *                 操作後に `this.transaction.errors` に反映されます。
   *
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnChangeKeyNickname のインスタンスであると仮定します
   * const newNickname = "My Favorite YubiKey"; // ユーザー入力からの値
   * try {
   *   await sdk.continueWithNewNickname({ nickname: newNickname });
   *   // 成功した場合、ページがリダイレクトされます。
   * } catch (error) {
   *   // この catch は SDK 呼び出し自体で発生した予期しないエラーを処理します。
   *   console.error("新しいニックネームの送信に失敗しました:", error);
   * }
   * // await の後、サーバー側の検証エラーを確認するために必ず sdk.transaction.errors をチェックしてください。
   * if (sdk.transaction.errors && sdk.transaction.errors.length > 0) {
   *   sdk.transaction.errors.forEach(err => {
   *     if (err.field === 'nickname') {
   *       // ニックネーム入力フィールドに関連する err.message を表示します。
   *       // 例: "名前が短すぎます"、"名前は必須です"
   *     }
   *   });
   * }
   * ```
   */
  continueWithNewNickname(payload: ContinueOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnMfaWebAuthnChangeKeyNickname">ScreenMembersOnMfaWebAuthnChangeKeyNickname</a></span>}>
  MFA WebAuthn Change Key Nickname に固有のデータ構造を持つ画面オブジェクトです。
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="continueWithNewNickname" type="Promise<void>">
  WebAuthn セキュリティキーの新しいニックネームを送信します。
  このアクションは、ユーザーが新しいニックネームを入力し、「Save」または「Continue」ボタンをクリックする操作に対応します。
  SDK は、この新しいニックネームを Auth0 の `/u/mfa-webauthn-change-key-nickname` エンドポイントに POST します。

  新しいニックネームが有効で、更新に成功した場合、Auth0 は通常、ユーザーを
  次の適切な画面 (例: MFA 認証要素の管理画面や成功確認画面) にリダイレクトします。
  ニックネームが無効な場合 (例: 空、長すぎる、短すぎる) 、Auth0 は
  `mfa-webauthn-change-key-nickname` 画面を再描画し、SDK の
  コンテキスト内の `transaction.errors` 配列が、検証エラーの詳細で更新されます。

  フォーム送信が開始されると解決される Promise です。
  解決時にデータが直接返されることはありません。通常はリダイレクトまたは
  ページの再レンダリングが行われるためです。

  #### 例外

  `payload.nickname` が指定されていない場合、または文字列でない場合は、エラーをスローします。
  また、送信中に `FormHandler` で回復不能な問題 (例: ネットワークエラー) が発生した場合も同様です。
  Auth0 からの検証エラー (無効なニックネームなど) は JavaScript エラーとしてはスローされず、
  操作後に `this.transaction.errors` に反映されます。

  ```typescript Example
  // 'sdk' が MfaWebAuthnChangeKeyNickname のインスタンスであるとします
  const newNickname = "My Favorite YubiKey"; // ユーザー入力の値
  try {
    await sdk.continueWithNewNickname({ nickname: newNickname });
    // 成功した場合、ページはリダイレクトされます。
  } catch (error) {
    // この catch は、SDK 呼び出し自体の実行中に発生した予期しないエラー用です。
    console.error("Failed to submit the new nickname:", error);
  }
  // await の後は、サーバー側の検証の問題がないか、必ず sdk.transaction.errors を確認してください。
  if (sdk.transaction.errors && sdk.transaction.errors.length > 0) {
    sdk.transaction.errors.forEach(err => {
      if (err.field === 'nickname') {
        // ニックネーム入力フィールドに関連する err.message を表示します。
        // 例: "名前が短すぎます", "名前は必須です"
      }
    });
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/MfaWebAuthnChangeKeyNicknameContinueOptions">MfaWebAuthnChangeKeyNicknameContinueOptions</a></span>}>
      ユーザーが入力した `nickname` (文字列) を含むオブジェクトです。
      拡張機能のための `CustomOptions` を含めることもできます。

      **プロパティ**

      <ParamField body="nickname" type="string">
        WebAuthn セキュリティキーの新しいニックネームです。
        これは、ユーザーが入力フィールドに入力した値です。
        サーバーで定義された長さおよび文字の制約に従う必要があります。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>