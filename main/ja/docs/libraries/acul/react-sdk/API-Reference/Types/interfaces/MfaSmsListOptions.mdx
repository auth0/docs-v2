---
title: "MfaSmsListOptions"
---

MfaSmsListOptions
mfa-sms-list 画面での操作に関するオプション。

```ts Example
export interface MfaSmsListOptions {
  /**
   * @param {number} index - 選択する電話番号のインデックス。
   */
  index: number;
  /** 追加のカスタムオプション */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

追加のカスタムオプション

<div id="properties">
  ## プロパティ
</div>

<ParamField body="index" type="number">
  選択する電話番号の位置を示すインデックスです。
</ParamField>