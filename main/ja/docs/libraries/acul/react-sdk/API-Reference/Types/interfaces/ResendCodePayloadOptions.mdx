---
title: "ResendCodePayloadOptions"
---

メールアドレスに送信された code を再送するためのオプション

```ts Example
export interface ResendCodeOptions {
  /** 追加のカスタムオプション */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## インデックス指定可能
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

追加のカスタムオプション