---
title: "UsernameValidationError"
---

検証時に返される単一の username 検証エラーを表します。

各エラーには、機械可読な `code` と、人が読んで理解できる `message` があります。

```ts Example
export interface UsernameValidationError {
  code: string;
  message: string;
  field: string;
}
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="code" type="string" required>
  失敗した検証ルールの一意の識別子です。

  例: `"username-too-short"`、`"username-invalid-characters"`
</ParamField>

<ParamField body="message" type="string" required>
  エラーの内容を人が理解しやすい形で説明したものです。

  例: `"Username must be at least 3 characters long."`
</ParamField>

<ParamField body="field" type="string" required>
  検証エラーに関連付けられたフィールドです。通常は `"username"`、`"email"`、または `"phone"` です。
</ParamField>