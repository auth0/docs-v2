---
title: "MfaWebAuthnErrorMembers"
---

MfaWebAuthnErrorMembers

````ts Example
export interface MfaWebAuthnErrorMembers extends BaseMembers {
  /**
   * The screen object with MFA WebAuthn Error specific data structure.
   */
  screen: ScreenMembersOnMfaWebAuthnError;
  
  /**
   * Allows the user to attempt the previous WebAuthn operation again.
   * This typically redirects the user back to the screen where the WebAuthn operation was initiated.
   *
   * @param {CustomOptions} [payload] - Optional custom options to include with the request.
   * @returns {Promise<void>} A promise that resolves when the action is successfully submitted.
   * @throws {Error} Throws an error if the submission fails.
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnError
   * await sdk.tryAgain();
   * ```
   */
  tryAgain(payload?: CustomOptions): Promise<void>;

  /**
   * Allows the user to opt for password-based authentication as an alternative, if configured.
   * This is typically available if the primary authentication or a previous step involved a password.
   *
   * @param {CustomOptions} [payload] - Optional custom options to include with the request.
   * @returns {Promise<void>} A promise that resolves when the action is successfully submitted.
   * @throws {Error} Throws an error if the submission fails.
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnError
   * await sdk.usePassword();
   * ```
   */
  usePassword(payload?: CustomOptions): Promise<void>;

  /**
   * Allows the user to select a different MFA method if other factors are enrolled or available.
   * This navigates the user to an authenticator selection screen.
   *
   * @param {CustomOptions} [payload] - Optional custom options to include with the request.
   * @returns {Promise<void>} A promise that resolves when the action is successfully submitted.
   * @throws {Error} Throws an error if the submission fails.
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnError
   * await sdk.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: CustomOptions): Promise<void>;

  /**
   * ユーザーがデバイスの追加または WebAuthn ステップの完了を拒否し、現在の WebAuthn フローを実質的にスキップできるようにします。
   * このアクションは通常、ユーザーが続行しないことを選択した登録シナリオで使用されます。
   *
   * @param {CustomOptions} [payload] - リクエストに含める追加のカスタムオプション。
   * @returns {Promise<void>} アクションが正常に送信されると解決される Promise。
   * @throws {Error} 送信に失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnError のインスタンスであると仮定します
   * await sdk.noThanks();
   * ```
   */
  noThanks(payload?: CustomOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnError">ScreenMembersOnMfaWebAuthnError</a></span>}>
  MFA WebAuthn Error 固有のデータ構造を持つ画面オブジェクト。
</ParamField>

<ParamField body="tenant" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="noThanks" type="Promise<void>">
  ユーザーがデバイスの追加や WebAuthn ステップの完了を拒否し、現在の WebAuthn フローを辞退できるようにします。
  通常、このアクションは、ユーザーが登録を続行しないことを選択した場合に使用されます。

  アクションが正常に送信されると resolve される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' は MfaWebAuthnError のインスタンスであると想定します
  await sdk.noThanks();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能な追加のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAgain" type="Promise<void>">
  ユーザーが直前の WebAuthn 操作を再試行できるようにします。
  通常、これによりユーザーは WebAuthn 操作が開始された画面に戻ります。

  アクションが正常に送信されると resolve される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' は MfaWebAuthnError のインスタンスであると想定します
  await sdk.tryAgain();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能な追加のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  他の認証要素が登録済みまたは利用可能な場合、ユーザーが別の MFA 方法を選択できるようにします。
  これにより、ユーザーは認証器の選択画面に移動します。

  アクションが正常に送信されると resolve される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' は MfaWebAuthnError のインスタンスであると想定します
  await sdk.tryAnotherMethod();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能な追加のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="usePassword" type="Promise<void>">
  設定されている場合、代替手段としてユーザーがパスワードベースの認証を選択できるようにします。
  通常、これはプライマリ認証または前のステップでパスワードが使用されていた場合に利用できます。

  アクションが正常に送信されると resolve される Promise です。

  #### スロー

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' は MfaWebAuthnError のインスタンスであると想定します
  await sdk.usePassword();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能な追加のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>