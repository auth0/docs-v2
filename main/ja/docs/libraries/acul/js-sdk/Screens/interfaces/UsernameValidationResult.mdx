---
title: "UsernameValidationResult"
---

username を 1 つ以上のルールに照らして検証した結果です。

```ts Example
export interface UsernameValidationResult {
  /**
   * Indicates whether the username passed all validation rules.
   */
  isValid: boolean;

  /**
   * 失敗した個々のルールを表す {@link UsernameValidationError} オブジェクトの配列（存在する場合）。
   *
   * {@link UsernameValidationResult.isValid} が `true` の場合、この配列は空です。
   */
  errors: UsernameValidationError[];
}
```

<div id="properties">
  ## プロパティ
</div>

<ParamField body="errors" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/UsernameValidationError">UsernameValidationError</a></span>}>
  失敗した各ルールを表す [UsernameValidationError](/ja/docs/libraries/acul/js-sdk/Screens/interfaces/UsernameValidationError) オブジェクトの配列です (該当する場合) 。

  [UsernameValidationResult.isValid](#isvalid) が `true` の場合、この配列は空になります。
</ParamField>

<ParamField body="isValid" type="boolean">
  username がすべての検証ルールを満たしているかどうかを示します。
</ParamField>