---
title: "MfaWebAuthnRoamingEnrollmentMembers"
---

mfa-webauthn-roaming-enrollment 画面で利用可能なメソッドとプロパティを定義するインターフェース。

````ts Example
export interface MfaWebAuthnRoamingEnrollmentMembers extends BaseMembers {
  screen: ScreenMembersOnMfaWebAuthnRoamingEnrollment;
  /**
   * WebAuthn 認証情報の作成を開始し、結果をサーバーに送信します。
   * これは、ユーザーが FIDO Security Keys プロンプトを操作する際に対応します。
   * @param payload リクエストに含める追加のカスタムオプション（省略可能）。
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * try {
   *   // ブラウザの WebAuthn プロンプトをトリガーします
   *   const credential = await navigator.credentials.create({ publicKey: webauthnEnrollment.screen.data.passkey.public_key });
   *   // 通常はここで認証情報のレスポンスをシリアライズします
   *   const response = JSON.stringify(credential);
   *   await webauthnEnrollment.enroll({ response });
   * } catch (error) {
   *   console.error('WebAuthn enrollment failed:', error);
   *   // エラーを処理します。例: ユーザーにエラーメッセージを表示するか、エラーの詳細を送信します
   *   // await webauthnEnrollment.showError({ error: { name: error.name, message: error.message } });
   * }
   * ```
   */
  enroll(payload: CustomOptions): Promise<void>;
  /**
   * WebAuthn ブラウザエラーの詳細をサーバーに送信します。
   * ブラウザの WebAuthn API でエラーが発生した場合に使用します。
   * @param payload エラーの詳細を含むオプション。
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * // ブラウザの WebAuthn API 呼び出しに対するエラーハンドラーの例
   * const handleError = async (error: any) => {
   *   console.error('WebAuthn エラー:', error);
   *   await webauthnEnrollment.showError({
   *     error: {
   *       name: error.name,
   *       message: error.message,
   *       // 必要に応じて、その他の関連するエラープロパティも含めてください
   *     },
   *   });
   * };
   * // ... WebAuthn API 呼び出しの catch ブロックで handleError を使用してください
   * ```
   */
  showError(payload: ShowErrorOptions): Promise<void>;
  /**
   * ユーザーが別の MFA 方式を試せるようにします。
   * これは「Try Another Method」ボタンに対応します。
   * @param payload リクエストに含める追加のカスタムオプション（省略可能）。
   * @example
   * ```typescript
   * import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';
   *
   * const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
   * await webauthnEnrollment.tryAnotherMethod();
   * ```
   */
  tryAnotherMethod(payload?: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnRoamingEnrollment">ScreenMembersOnMfaWebAuthnRoamingEnrollment</a></span>} />

<ParamField body="tenant" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="enroll" type="Promise<void>">
  WebAuthn 認証情報の作成を開始し、その結果をサーバーに送信します。
  これは、ユーザーが FIDO Security Keys プロンプトを操作した場合に相当します。

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  try {
    // これによりブラウザーの WebAuthn プロンプトが表示されます
    const credential = await navigator.credentials.create({ publicKey: webauthnEnrollment.screen.data.passkey.public_key });
    // 通常はここで認証情報のレスポンスをシリアライズします
    const response = JSON.stringify(credential);
    await webauthnEnrollment.enroll({ response });
  } catch (error) {
    console.error('WebAuthn enrollment failed:', error);
    // エラーを処理します（例: ユーザーにエラーメッセージを表示する、またはエラーの詳細を送信する）
    // await webauthnEnrollment.showError({ error: { name: error.name, message: error.message } });
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める省略可能な追加のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="showError" type="Promise<void>">
  ブラウザーで発生した WebAuthn エラーの詳細をサーバーに送信します。
  これは、ブラウザーの WebAuthn API でエラーが発生した場合に使用します。

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  // ブラウザーの WebAuthn API 呼び出し用のエラーハンドラーの例
  const handleError = async (error: any) => {
    console.error('WebAuthn error:', error);
    await webauthnEnrollment.showError({
      error: {
        name: error.name,
        message: error.message,
        // 利用可能な場合は、その他の関連するエラープロパティも含めます
      },
    });
  };
  // ... WebAuthn API 呼び出しの catch ブロックで handleError を使用します
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/ShowErrorOptions">ShowErrorOptions</a></span>}>
      エラーの詳細を含むオプションです。

      **プロパティ**

      <ParamField body="error" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        WebAuthn エラーの詳細です。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  ユーザーが別の MFA 方式を試せるようにします。
  これは、「Try Another Method」ボタンに対応します。

  ```typescript Example
  import MfaWebAuthnRoamingEnrollment from '@auth0/auth0-acul-js/mfa-webauthn-roaming-enrollment';

  const webauthnEnrollment = new MfaWebAuthnRoamingEnrollment();
  await webauthnEnrollment.tryAnotherMethod();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnRoamingEnrollmentTryAnotherMethodOptions">MfaWebAuthnRoamingEnrollmentTryAnotherMethodOptions</a></span>}>
      リクエストに含める省略可能な追加のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>