---
title: "メンバーによるMFA WebAuthnキーのニックネーム変更"
---

メンバーによるMFA WebAuthnキーのニックネーム変更

````ts Example
export interface MfaWebAuthnChangeKeyNicknameMembers extends BaseMembers {
  /**
   * MFA WebAuthn Change Key Nickname 固有のデータ構造を持つ画面オブジェクト。
   */
  screen: ScreenMembersOnMfaWebAuthnChangeKeyNickname;
  
  /**
   * WebAuthn セキュリティキーの新しいニックネームを送信します。
   * このアクションは、ユーザーが新しいニックネームを入力して「保存」または「続行」ボタンをクリックする操作に対応します。
   * SDK はこの新しいニックネームを Auth0 の `/u/mfa-webauthn-change-key-nickname` エンドポイントに POST します。
   *
   * 新しいニックネームが有効で更新が成功した場合、Auth0 は通常、ユーザーを次の適切な画面
   * （例: MFA 認証要素管理画面または成功確認画面）にリダイレクトします。
   * ニックネームが無効な場合（例: 空、長すぎる、短すぎる）、Auth0 は
   * 'mfa-webauthn-change-key-nickname' 画面を再レンダリングし、SDK の
   * コンテキスト内の `transaction.errors` 配列が検証失敗の詳細で更新されます。
   *
   * @param {ContinueOptions} payload - ユーザーが入力した `nickname`（文字列）を含むオブジェクト。
   *                                    拡張機能のために任意の `CustomOptions` を含めることもできます。
   * @returns {Promise<void>} フォームの送信が開始されたときに解決される Promise。
   *                          リダイレクトまたはページの再レンダリングが一般的な結果となるため、
   *                          解決時にデータを直接返しません。
   * @throws {Error} `payload.nickname` が指定されていないか文字列でない場合、
   *                 または `FormHandler` が送信中に回復不能な問題（例: ネットワークエラー）に
   *                 遭遇した場合にエラーをスローします。
   *                 Auth0 からの検証エラー（無効なニックネームなど）は JavaScript エラーとしてスローされず、
   *                 操作後に `this.transaction.errors` に反映されます。
   *
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnChangeKeyNickname のインスタンスであると仮定します
   * const newNickname = "My Favorite YubiKey"; // ユーザー入力からの値
   * try {
   *   await sdk.continueWithNewNickname({ nickname: newNickname });
   *   // 成功した場合、ページがリダイレクトされます。
   * } catch (error) {
   *   // この catch は SDK 呼び出し自体で発生した予期しないエラーを処理します。
   *   console.error("新しいニックネームの送信に失敗しました:", error);
   * }
   * // await の後、サーバー側の検証問題を確認するために sdk.transaction.errors を必ずチェックしてください。
   * if (sdk.transaction.errors && sdk.transaction.errors.length > 0) {
   *   sdk.transaction.errors.forEach(err => {
   *     if (err.field === 'nickname') {
   *       // ニックネーム入力フィールドに関連する err.message を表示します。
   *       // 例: "名前が短すぎます"、"名前は必須です"
   *     }
   *   });
   * }
   * ```
   */
  continueWithNewNickname(payload: ContinueOptions): Promise<void>;
}
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnChangeKeyNickname">ScreenMembersOnMfaWebAuthnChangeKeyNickname</a></span>}>
  MFA WebAuthn Change Key Nickname に固有のデータ構造を持つ画面オブジェクトです。
</ParamField>

<ParamField body="tenant" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="continueWithNewNickname" type="Promise<void>">
  WebAuthn セキュリティキーの新しいニックネームを送信します。
  このアクションは、ユーザーが新しいニックネームを入力して「Save」または「Continue」ボタンをクリックする操作に対応します。
  SDK はこの新しいニックネームを Auth0 の `/u/mfa-webauthn-change-key-nickname` エンドポイントに POST します。

  新しいニックネームが有効で、更新に成功した場合、Auth0 は通常、ユーザーを
  次の適切な画面 (例: MFA 認証要素の管理画面や成功確認画面) にリダイレクトします。
  ニックネームが無効な場合 (例: 空、長すぎる、短すぎる) 、Auth0 は
  `mfa-webauthn-change-key-nickname` 画面を再レンダリングし、SDK の
  context 内の `transaction.errors` 配列が検証失敗の詳細で更新されます。

  フォーム送信が開始されると resolve される promise です。
  一般的な結果はリダイレクトまたは
  ページの再レンダリングであるため、resolve 時にデータが直接返されることはありません。

  #### 例外

  `payload.nickname` が指定されていない場合、または文字列ではない場合、
  あるいは送信中に `FormHandler` が回復不能な問題 (例: ネットワークエラー) に遭遇した場合にエラーをスローします。
  Auth0 からの検証エラー (無効なニックネームなど) は JavaScript エラーとしてスローされず、
  操作後に `this.transaction.errors` に反映されます。

  ```typescript Example
  // 'sdk' は MfaWebAuthnChangeKeyNickname のインスタンスであると仮定します
  const newNickname = "My Favorite YubiKey"; // ユーザー入力の値
  try {
    await sdk.continueWithNewNickname({ nickname: newNickname });
    // 成功した場合、ページはリダイレクトされます。
  } catch (error) {
    // この catch は、SDK 呼び出し自体で発生する予期しないエラーを処理するためのものです。
    console.error("新しいニックネームの送信に失敗しました:", error);
  }
  // await の後は、サーバー側の検証エラーがないか必ず sdk.transaction.errors を確認してください。
  if (sdk.transaction.errors && sdk.transaction.errors.length > 0) {
    sdk.transaction.errors.forEach(err => {
      if (err.field === 'nickname') {
        // ニックネーム入力フィールドに関連する err.message を表示します。
        // 例: "Name is too short", "Name is required"
      }
    });
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/ja/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnChangeKeyNicknameContinueOptions">MfaWebAuthnChangeKeyNicknameContinueOptions</a></span>}>
      ユーザーが入力した `nickname` (文字列) を含むオブジェクトです。
      拡張機能のための `CustomOptions` を含めることもできます。

      **プロパティ**

      <ParamField body="nickname" type="string">
        WebAuthn セキュリティキーの新しいニックネームです。
        これは、ユーザーが入力フィールドに入力した値です。
        サーバーで定義された長さおよび文字の制約に従う必要があります。
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>