---
title: "MfaWebAuthnPlatformEnrollmentMembers"
---

MfaWebAuthnPlatformEnrollmentMembers

````ts Example
export interface MfaWebAuthnPlatformEnrollmentMembers extends BaseMembers {
  /**
   * MFA WebAuthn プラットフォーム登録固有のデータ構造を持つ画面オブジェクト。
   */
  screen: ScreenMembersOnMfaWebAuthnPlatformEnrollment;
  
  /**
   * `this.screen.publicKey` で利用可能な公開鍵オプションを使用して WebAuthn プラットフォームの資格情報作成プロセスを開始し、
   * 作成された資格情報をサーバーに送信します。
   * このメソッドは内部的に `createPasskeyCredentials`（`navigator.credentials.create()` をラップ）を呼び出します。
   *
   * @param {SubmitPasskeyCredentialOptions} [payload] - 作成された資格情報と共にサーバーに送信するオプションのカスタムパラメーター。
   * @returns {Promise<void>} 資格情報が正常に作成・送信されたときに解決される Promise。
   * @throws {Error} `this.screen.publicKey` が利用できない場合、`createPasskeyCredentials` が失敗した場合
   * （例: ユーザーによるキャンセル、ハードウェアの問題）、またはサーバーへの送信が失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnPlatformEnrollment のインスタンスと仮定
   * try {
   *   if (!sdk.screen.publicKey) { // オプションが利用可能か確認
   *     throw new Error("Public key creation options are not available on the screen context.");
   *   }
   *   await sdk.submitPasskeyCredential(); // publicKey を明示的に渡す必要はない
   *   // 成功時、Auth0 がリダイレクトを処理する。
   * } catch (error) {
   *   console.error('Passkey enrollment failed:', error);
   *   // エラーを処理する。WebAuthn API エラーの場合は reportBrowserError の呼び出しを検討する
   *   if (error.name && error.message) { // WebAuthn エラーかどうかを確認
   *     await sdk.reportBrowserError({ error: { name: error.name, message: error.message } });
   *   }
   * }
   * ```
   */
  submitPasskeyCredential(payload?: SubmitPasskeyCredentialOptions): Promise<void>;

  /**
   * WebAuthn の `navigator.credentials.create()` 操作中に発生したブラウザー側のエラーを報告します。
   * このメソッドはエラーの詳細をサーバーに送信します。
   *
   * @param {ReportBrowserErrorOptions} payload - ブラウザーエラーの詳細および任意のカスタムオプション。
   * @returns {Promise<void>} エラーレポートが正常に送信されたときに解決される Promise。
   * @throws {Error} 送信が失敗した場合にエラーをスローします。
   * @example
   * ```typescript
   * // 'sdk' は MfaWebAuthnPlatformEnrollment のインスタンスと仮定
   * // パスキー作成の試行（例: submitPasskeyCredential から）の catch ブロック内:
   * } catch (webAuthnError) {
   *   if (webAuthnError.name) { // WebAuthn API エラーの可能性があるか確認
   *      await sdk.reportBrowserError({ error: { name: webAuthnError.name, message: webAuthnError.message } });
   *   }
   *   // エラーメッセージをユーザーに表示するよう UI を更新する
   * }
````

<div id="properties">
  ## プロパティ
</div>

<ParamField body="branding" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnPlatformEnrollment">ScreenMembersOnMfaWebAuthnPlatformEnrollment</a></span>}>
  MFA WebAuthn プラットフォーム登録 に固有のデータ構造を持つ画面オブジェクト。
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## メソッド
</div>

<ParamField body="refuseEnrollmentOnThisDevice" type="Promise<void>">
  現在のデバイスでの WebAuthn プラットフォーム登録を、ユーザーが拒否できるようにします。
  このアクションは、このデバイスではプラットフォーム認証器を使用したくないというユーザーの意思を示します。

  拒否アクションが正常に送信されると resolve される Promise です。

  #### Throws

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnPlatformEnrollment のインスタンスであると仮定します
  await sdk.refuseEnrollmentOnThisDevice();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める任意のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="reportBrowserError" type="Promise<void>">
  WebAuthn の `navigator.credentials.create()` の実行中に発生したブラウザー側のエラーを報告します。
  このメソッドは、エラーの詳細をサーバーに送信します。

  エラーレポートが正常に送信されると resolve される Promise です。

  #### Throws

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnPlatformEnrollment のインスタンスであると仮定します
  // passkey の作成を試行した際の catch ブロック内 (例: submitPasskeyCredential):
  } catch (webAuthnError) {
    if (webAuthnError.name) { // WebAuthn API エラーである可能性が高いか確認します
       await sdk.reportBrowserError({ error: { name: webAuthnError.name, message: webAuthnError.message } });
    }
    // ユーザーにエラーメッセージを表示するよう UI を更新します
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/ReportBrowserErrorOptions">ReportBrowserErrorOptions</a></span>}>
      ブラウザーエラーの詳細と任意のカスタムオプション。

      **プロパティ**

      <ParamField body="error" type="object">
        <Expandable title="プロパティ">
          <ParamField body="message" type="string" />

          <ParamField body="name" type="string" />
        </Expandable>
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="snoozeEnrollment" type="Promise<void>">
  ユーザーが WebAuthn プラットフォーム登録を一時的に延期できるようにします。
  通常、このアクションは、後で登録を促す通知が再度表示されることを意味します。

  延期アクションが正常に送信されると resolve される Promise です。

  #### Throws

  送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnPlatformEnrollment のインスタンスであると仮定します
  await sdk.snoozeEnrollment();
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/CustomOptions">CustomOptions</a></span>}>
      リクエストに含める任意のカスタムオプション。
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="submitPasskeyCredential" type="Promise<void>">
  `this.screen.publicKey` で利用可能な公開鍵オプションを使用して
  WebAuthn プラットフォーム資格情報の作成プロセスを開始し、生成された資格情報をサーバーに送信します。
  このメソッドは内部で `createPasskeyCredentials` (`navigator.credentials.create()` をラップするメソッド) を呼び出します。

  資格情報の作成と送信が正常に完了すると resolve される Promise です。

  #### Throws

  `this.screen.publicKey` が利用できない場合、`createPasskeyCredentials` が失敗した場合
   (例: ユーザーによるキャンセル、ハードウェアの問題) 、またはサーバーへの送信に失敗した場合はエラーをスローします。

  ```typescript Example
  // 'sdk' が MfaWebAuthnPlatformEnrollment のインスタンスであると仮定します
  try {
    if (!sdk.screen.publicKey) { // オプションが利用可能か確認します
      throw new Error("Public key creation options are not available on the screen context.");
    }
    await sdk.submitPasskeyCredential(); // publicKey を明示的に渡す必要はありません
    // 成功時は、Auth0 がリダイレクトを処理します。
  } catch (error) {
    console.error('Passkey enrollment failed:', error);
    // エラーを処理します。WebAuthn API エラーの場合は reportBrowserError の呼び出しを検討します
    if (error.name && error.message) { // WebAuthn エラーのように見えるか確認します
      await sdk.reportBrowserError({ error: { name: error.name, message: error.message } });
    }
  }
  ```

  <Expandable title="パラメーター">
    <ParamField body="payload?" type={<span><a href="/docs/libraries/acul/js-sdk/Screens/interfaces/SubmitPasskeyCredentialOptions">SubmitPasskeyCredentialOptions</a></span>}>
      作成された資格情報とともに
      サーバーに送信する任意のカスタムパラメーター。
    </ParamField>
  </Expandable>
</ParamField>