---
description: Auth0 Dashboard と Management API を使用して、ユーザーに割り当てられた権限を確認する方法を学びます。
title: ユーザーの権限を表示する
---

import {AuthCodeBlock} from "/snippets/ja/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/ja/AuthCodeGroup.jsx";

ユーザーに割り当てられている[権限](/ja/docs/manage-users/access-control/rbac)は、<Tooltip tip="Auth0 Dashboard: サービスを設定するための Auth0 の主要製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip>で確認できます。この操作は、<Tooltip tip="Auth0 Dashboard: サービスを設定するための Auth0 の主要製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Management+API">Management API</Tooltip>を使用して行うこともできます。割り当てられた権限は、API の Authorization Core 機能セットで使用されます。

<div id="prerequisites">
  ## 前提条件
</div>

ロールベースアクセス制御 (RBAC) を正しく機能させるには、Dashboard または Management API を使用して、API で RBAC を有効にする必要があります。Authorization Core の機能は Authorization Extension とは異なります。比較については、[Authorization Core vs. Authorization Extension](/ja/docs/manage-users/access-control/authorization-core-vs-authorization-extension) を参照してください。

<div id="dashboard">
  ## Dashboard
</div>

1. [Dashboard &gt; Manage Users &gt; Users](https://manage.auth0.com/#/users) に移動し、表示するユーザーの名前をクリックします。
2. **Permissions** ビューをクリックします。

   <Frame>![このユーザーに割り当てられている権限の一覧を示す Auth0 Dashboard のユーザー詳細画面](/docs/images/cdy7uua7fh8z/4vShfxoUcSpCDnM5FnAbsR/de85942623430e3cedc097addbaa2f9d/User_Details_-_Permissions_-_English.png)</Frame>

各権限について、次の情報が表示されます。

| 列    | 説明                                           |
| ---- | -------------------------------------------- |
| 名前   | 権限定義に含まれる権限の名前。                              |
| 説明   | 権限定義に含まれる権限の説明。                              |
| API  | その権限が関連付けられている API の名前。                      |
| 割り当て | 権限がユーザーに直接割り当てられているか、ロールを介して割り当てられているかを示します。 |

<div id="management-api">
  ## Management API
</div>

[Get User Permissions endpoint](https://auth0.com/docs/api/management/v2#!/Users/get_permissions) に `GET` リクエストを送信します。`USER_ID` と `MGMT_API_ACCESS_TOKEN` のプレースホルダー値は、それぞれご使用のユーザー ID と Management API の <Tooltip tip="アクセストークン: API へのアクセスに使用される認可資格情報で、オペーク文字列または JWT の形式です。" cta="用語集を見る" href="/ja/docs/glossary?term=access+token">アクセストークン</Tooltip> に必ず置き換えてください。

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/users/USER_ID/permissions' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/users/USER_ID/permissions");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/users/USER_ID/permissions"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/users/USER_ID/permissions")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/users/USER_ID/permissions',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/users/USER_ID/permissions",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/users/USER_ID/permissions", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/users/USER_ID/permissions")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

| 値                       | 説明                                                                                                  |
| ----------------------- | --------------------------------------------------------------------------------------------------- |
| `USER_ID`               | 権限を取得する対象ユーザーの ID。                                                                                  |
| `MGMT_API_ACCESS_TOKEN` | スコープ `read:users` を持つ [Management API 用のアクセストークン](https://auth0.com/docs/api/management/v2/tokens)。 |

<div id="learn-more">
  ## 詳細情報
</div>

* [ユーザーに権限を割り当てる](/ja/docs/manage-users/access-control/configure-core-rbac/rbac-users/assign-permissions-to-users)
* [ユーザーから権限を削除する](/ja/docs/manage-users/access-control/configure-core-rbac/rbac-users/remove-permissions-from-users)