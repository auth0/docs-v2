---
description: Auth0 Dashboard または Management API を使用して、ロールに追加された権限を確認する方法を学びます。
title: ロールの権限を表示する
---

import {AuthCodeBlock} from "/snippets/ja/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/ja/AuthCodeGroup.jsx";

ロールに追加された[権限](/ja/docs/manage-users/access-control/rbac)は、<Tooltip tip="Auth0 Dashboard: サービスを設定するための Auth0 の主要製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Auth0+Dashboard">Auth0 Dashboard</Tooltip>または<Tooltip tip="Auth0 Dashboard: サービスを設定するための Auth0 の主要製品です。" cta="用語集を表示" href="/ja/docs/glossary?term=Management+API">Management API</Tooltip>を使用して確認できます。割り当てられた権限とロールは、API の Authorization Core 機能セットで使用されます。

<div id="prerequisites">
  ## 前提条件
</div>

ロールベースアクセス制御 (RBAC) を正しく機能させるには、Dashboard または Management API を使用して、API で RBAC を有効にする必要があります。Authorization Core の機能は Authorization Extension とは異なります。比較については、[Authorization Core と Authorization Extension](/ja/docs/manage-users/access-control/authorization-core-vs-authorization-extension)を参照してください。

<div id="dashboard">
  ## Dashboard
</div>

1. [Dashboard &gt; User Management &gt; Roles](https://manage.auth0.com/#/roles) に移動し、表示するロールの名前をクリックします。
2. **Permissions** ビューをクリックします。

各権限について、次の情報が表示されます。

| 列   | 説明                        |
| --- | ------------------------- |
| 名前  | 権限定義で指定された権限の名前です。        |
| 説明  | 権限定義で指定された権限の説明です。        |
| API | その権限が関連付けられている API の名前です。 |

<div id="management-api">
  ## Management API
</div>

[Get Role Permissions endpoint](https://auth0.com/docs/api/management/v2#!/Roles/get_role_permission) に `GET` リクエストを送信します。`ROLE_ID` と `MGMT_API_ACCESS_TOKEN` のプレースホルダー値は、それぞれ対象のロール ID と Management API の <Tooltip tip="アクセストークン: API へのアクセスに使用される認可資格情報で、不透明な文字列または JWT の形式を取ります。" cta="用語集を見る" href="/ja/docs/glossary?term=access+token">アクセストークン</Tooltip> に置き換えてください。

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/roles/ROLE_ID/permissions' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/roles/ROLE_ID/permissions");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/roles/ROLE_ID/permissions"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/roles/ROLE_ID/permissions")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/roles/ROLE_ID/permissions',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/roles/ROLE_ID/permissions",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/roles/ROLE_ID/permissions", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/roles/ROLE_ID/permissions")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

| 値                       | 説明                                                                                                  |
| ----------------------- | --------------------------------------------------------------------------------------------------- |
| `ROLE_ID`               | 権限を取得する対象のロールの ID。                                                                                  |
| `MGMT_API_ACCESS_TOKEN` | スコープ `read:roles` を持つ [Management API 用のアクセストークン](https://auth0.com/docs/api/management/v2/tokens)。 |

<div id="learn-more">
  ## 詳細
</div>

* [ロールベースアクセス制御の権限を管理する](/ja/docs/manage-users/access-control/configure-core-rbac/manage-permissions)
* [API 権限を追加する](/ja/docs/get-started/apis/add-api-permissions)
* [API 権限を削除する](/ja/docs/get-started/apis/delete-api-permissions)
* [ロールから権限を削除する](/ja/docs/manage-users/access-control/configure-core-rbac/roles/remove-permissions-from-roles)