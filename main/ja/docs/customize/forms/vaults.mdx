---
description: Actions の Forms 向け Vault の使用方法を説明します。
title: Vaults
sidebarTitle: 概要
---

Vault を使用すると、シークレットや共通設定 (例: API キー、<Tooltip tip="アクセストークン: API へのアクセスに使用される、不透明な文字列または JWT 形式の認可資格情報。" cta="用語集を見る" href="/ja/docs/glossary?term=access+tokens">アクセストークン</Tooltip>、<Tooltip tip="アクセストークン: API へのアクセスに使用される、不透明な文字列または JWT 形式の認可資格情報。" cta="用語集を見る" href="/ja/docs/glossary?term=refresh+tokens">リフレッシュトークン</Tooltip>) を Vault 接続として安全に保存し、外部サービスや統合へのリクエストの認証に利用できます。

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  * ユーザーが認証情報を提供する必要がある接続があります (たとえば、Auth0 でマシンツーマシン (M2M) アプリケーションを使用する場合、有効期限付きのアクセストークンとリフレッシュトークンを生成します) 。接続には両方のトークンが保存され、有効期限が切れる前に新しいアクセストークンが生成されます。
  * API 認証情報の生成にあたり、スコープの認可が必要な外部サービスがあります。スコープを変更した場合は、接続の問題を回避するため、それらのサービスへの接続を更新してください。
  * 接続を追加すると、フローですぐに使用できます。既存の接続への更新は自動的に反映されます。
</Callout>

<div id="create-and-modify-vault-connections">
  ## Vault 接続の作成と変更
</div>

Vault 接続は、Vault ページから、または Action で認証情報が必要な場合はフローエディターから作成および変更できます。

<div id="create-vault-connections-from-the-vault-page">
  ### Vault ページから Vault 接続を作成する
</div>

次の手順に従って、Vault ページから Vault 接続を追加します。

1. **Auth0 Dashboard &gt; Actions &gt; Forms** を選択して、フォームエディターを開きます。
2. 左上のテナントメニューを選択し、**Vault** を選択して Vault ページを開きます。

   <Frame>![Dashboard > Forms > Vault](/docs/images/cdy7uua7fh8z/1sPNPKO6nuEDCuiYGRWSur/d85dc616170eaf2d7d5f321750863ccf/vault-connection.png)</Frame>
3. **Add Vault Connection** を選択します。
4. 目的のアクションを選択し、**続行** を選択します。
5. Vault の **接続名** を入力し、**続行** を選択します。
6. 認証情報を入力します。
   必要な認証情報がない場合は、**Create an empty connection** を選択して、後で認証情報を追加できます。空の接続には、認証情報が不足していることを示すオレンジ色のアイコンが表示されます。
7. **Add Vault Connection** を選択します。

<div id="create-vault-connections-from-the-flow-editor">
  ### フローエディターから Vault 接続を作成する
</div>

以下の手順で、Twilio などのフローアクションから Vault 接続を追加します。

1. **Auth0 Dashboard &gt; Actions &gt; Forms &gt; Flows** を選択して、フローエディターを開きます。
2. フローを選択し、**Send SMS with Twilio** アクションを追加します。
3. アクションの **Vault connection** 設定で、**+** アイコンを選択します。

   <Frame>![Dashboard > Forms > Vault ](/docs/images/cdy7uua7fh8z/1AsaznhRKxm5LZxPlcbfUw/10e5a8af700aab909f5b03fcd266da7e/twilio-vault-connection.png)</Frame>
4. Vault の **接続名** を入力し、**続行** を選択します。
5. 認証情報を入力します。
   必要な認証情報がない場合は、**Create an empty connection** を選択して、後から認証情報を追加できます。空の接続には、認証情報が不足していることを示すオレンジ色のアイコンが表示されます。

   <Frame>![Dashboard > Forms > Vault > Twilio connection settings](/docs/images/cdy7uua7fh8z/yobXanutPRCO3FWek8cOk/bc29e39fb8593cff35e2738234bfe6fe/twilio-vault-settings.png)</Frame>
6. **Add Vault Connection** を選択します。