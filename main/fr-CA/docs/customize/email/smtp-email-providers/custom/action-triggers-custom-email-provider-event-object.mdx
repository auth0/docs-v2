---
#
# THIS FILE IS AUTO-GENERATED.
# DO NOT EDIT MANUALLY.
#
description: Découvrez l’objet `event` du déclencheur Actions custom-email-provider, qui fournit des renseignements contextuels sur son exécution.
title: 'Déclencheurs Actions : custom-email-provider - Objet `event`'
validatedOn: 2026-04-28
---

L’objet `event` du déclencheur Actions custom-email-provider fournit des renseignements contextuels sur son exécution.

<div id="eventclient">
  ## `event.client`
</div>

<ResponseField name="event.client" type="dictionary">
  Renseignements sur le client avec lequel cette opération de connexion a été amorcée.

  <Expandable title="propriétés du client" defaultOpen>
    <ResponseField name="client_id" type="string">
      L’identifiant client de l’application à laquelle l’utilisateur se connecte.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Un objet servant à stocker d’autres propriétés de l’application.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de l’application (tel qu’il est défini dans le Dashboard).
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventconnection">
  ## `event.connection`
</div>

<ResponseField name="event.connection" type="dictionary" post={["optional"]}>
  Renseignements sur la connexion utilisée pour authentifier l’utilisateur.

  <Expandable title="propriétés de la connexion" defaultOpen>
    <ResponseField name="id" type="string">
      L’identifiant unique de la connexion.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary" post={["optional"]}>
      Métadonnées associées à la connexion.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de la connexion utilisée pour authentifier l’utilisateur (par exemple `twitter` ou `some-g-suite-domain`).
    </ResponseField>

    <ResponseField name="strategy" type="string">
      Le type de connexion. Pour les connexions sociales, `event.connection.strategy === event.connection.name`. Pour les connexions d’entreprise, la stratégie peut être `waad` (Windows Azure AD), `ad` (Active Directory/LDAP), `auth0` (connexions de base de données), etc.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventcustom_domain">
  ## `event.custom_domain`
</div>

<ResponseField name="event.custom_domain" type="dictionary" post={["optional"]}>
  Renseignements sur le domaine personnalisé associé à la transaction en cours.

  <Expandable title="propriétés de custom_domain" defaultOpen>
    <ResponseField name="domain" type="string">
      Le nom de domaine personnalisé.
    </ResponseField>

    <ResponseField name="domain_metadata" type="dictionary">
      Les métadonnées du domaine personnalisé sous forme de paires clé-valeur.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventnotification">
  ## `event.notification`
</div>

<ResponseField name="event.notification" type="object">
  <Expandable title="propriétés de la notification" defaultOpen>
    <ResponseField name="from" type="string">
      Adresse courriel de l’expéditeur.
    </ResponseField>

    <ResponseField name="html" type="string">
      Modèle HTML généré.
    </ResponseField>

    <ResponseField name="locale" type="string">
      Paramètre régional dans lequel le message a été généré, par exemple `en_US`, tel que défini dans la spécification BCP-47.
    </ResponseField>

    <ResponseField name="message_type" type="string">
      Type de message envoyé, comme `verify_email` ou `welcome_email`.

      Valeurs possibles :

      * `verify_email`
      * `verify_email_by_code`
      * `reset_email`
      * `reset_email_by_code`
      * `welcome_email`
      * `verification_code`
      * `mfa_oob_code`
      * `enrollment_email`
      * `blocked_account`
      * `stolen_credentials`
      * `try_provider_configuration_email`
      * `organization_invitation`
    </ResponseField>

    <ResponseField name="subject" type="string">
      Objet du courriel.
    </ResponseField>

    <ResponseField name="text" type="string">
      Modèle texte généré.
    </ResponseField>

    <ResponseField name="to" type="string">
      Adresse courriel du destinataire.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventorganization">
  ## `event.organization`
</div>

<ResponseField name="event.organization" type="dictionary" post={["optional"]}>
  Détails sur l’organisation associée à la transaction en cours.

  <Expandable title="propriétés de l’organisation" defaultOpen>
    <ResponseField name="id" type="string">
      L’identifiant de l’organisation.
    </ResponseField>

    <ResponseField name="display_name" type="string">
      Le nom d’affichage de l’organisation.
    </ResponseField>

    <ResponseField name="metadata" type="dictionary">
      Les métadonnées associées à l’organisation.
    </ResponseField>

    <ResponseField name="name" type="string">
      Le nom de l’organisation.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventrequest">
  ## `event.request`
</div>

<ResponseField name="event.request" type="dictionary">
  <Expandable title="propriétés de la requête" defaultOpen>
    <ResponseField name="geoip" type="dictionary" post={["optional"]}>
      <Expandable title="propriétés de geoip" defaultOpen>
        <ResponseField name="cityName" type="string" post={["optional"]} />

        <ResponseField name="continentCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode" type="string" post={["optional"]} />

        <ResponseField name="countryCode3" type="string" post={["optional"]} />

        <ResponseField name="countryName" type="string" post={["optional"]} />

        <ResponseField name="latitude" type="number" post={["optional"]} />

        <ResponseField name="longitude" type="number" post={["optional"]} />

        <ResponseField name="subdivisionCode" type="string" post={["optional"]} />

        <ResponseField name="subdivisionName" type="string" post={["optional"]} />

        <ResponseField name="timeZone" type="string" post={["optional"]} />
      </Expandable>
    </ResponseField>

    <ResponseField name="hostname" type="string" post={["optional"]}>
      Le nom d’hôte utilisé pour le flux d’authentification.
    </ResponseField>

    <ResponseField name="ip" type="string" post={["optional"]}>
      L’adresse IP source de la requête.
    </ResponseField>

    <ResponseField name="query" type="dictionary" post={["optional"]}>
      Les paramètres de la chaîne de requête envoyés avec la requête d’autorisation.
    </ResponseField>

    <ResponseField name="user_agent" type="string" post={["optional"]}>
      La valeur de l’en-tête `User-Agent` reçue au moment d’amorcer la transaction.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventsecrets">
  ## `event.secrets`
</div>

<ResponseField name="event.secrets" type="secrets">
  Valeurs secrètes associées en toute sécurité à cette action.
</ResponseField>

<div id="eventtenant">
  ## `event.tenant`
</div>

<ResponseField name="event.tenant" type="object">
  <Expandable title="propriétés du tenant" defaultOpen>
    <ResponseField name="id" type="string">
      Le nom du tenant.
    </ResponseField>

    <ResponseField name="friendly_name" type="string" post={["optional"]}>
      Le nom d’affichage du tenant, généralement une version de l’ID plus facile à lire.
    </ResponseField>

    <ResponseField name="home_url" type="string" post={["optional"]}>
      L’URL d’accueil du tenant, si elle est définie et indiquée dans ses paramètres.
    </ResponseField>

    <ResponseField name="logo_url" type="string" post={["optional"]}>
      L’URL du logo du tenant, si elle est définie et indiquée dans ses paramètres.
    </ResponseField>

    <ResponseField name="support_email" type="string" post={["optional"]}>
      L’adresse courriel du service d’assistance du tenant, si elle est définie et indiquée dans ses paramètres.
    </ResponseField>

    <ResponseField name="support_url" type="string" post={["optional"]}>
      L’URL du service d’assistance du tenant, si elle est définie et indiquée dans ses paramètres.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventtransaction">
  ## `event.transaction`
</div>

<ResponseField name="event.transaction" type="object" post={["optional"]}>
  Détails sur la transaction en cours à des fins de suivi.

  <Expandable title="propriétés de la transaction" defaultOpen>
    <ResponseField name="correlation_id" type="string" post={["optional"]}>
      Un identifiant unique permettant de faire le lien entre cette requête et plusieurs services dans le cadre du suivi distribué.
    </ResponseField>
  </Expandable>
</ResponseField>

<div id="eventuser">
  ## `event.user`
</div>

<ResponseField name="event.user" type="dictionary">
  Objet décrivant l’utilisateur au nom duquel la transaction en cours a été lancée.

  <Expandable title="propriétés de l’utilisateur" defaultOpen>
    <ResponseField name="app_metadata" type="dictionary">
      Champs personnalisés qui stockent des renseignements sur un utilisateur et qui influencent ses accès, comme le forfait de soutien, les rôles de sécurité ou les groupes de contrôle d’accès.
    </ResponseField>

    <ResponseField name="email" type="string" post={["optional"]}>
      (unique) Adresse courriel de l’utilisateur.
    </ResponseField>

    <ResponseField name="email_verified" type="boolean">
      Indique si l’utilisateur a vérifié son adresse courriel.
    </ResponseField>

    <ResponseField name="family_name" type="string" post={["optional"]}>
      Nom de famille de l’utilisateur.
    </ResponseField>

    <ResponseField name="given_name" type="string" post={["optional"]}>
      Prénom de l’utilisateur.
    </ResponseField>

    <ResponseField name="name" type="string" post={["optional"]}>
      Nom complet de l’utilisateur.
    </ResponseField>

    <ResponseField name="nickname" type="string" post={["optional"]}>
      Surnom de l’utilisateur.
    </ResponseField>

    <ResponseField name="picture" type="string" post={["optional"]}>
      URL pointant vers la [photo de profil de l’utilisateur](https://auth0.com/docs/users/change-user-picture).
    </ResponseField>

    <ResponseField name="user_id" type="string">
      (unique) Identifiant unique de l’utilisateur.
    </ResponseField>

    <ResponseField name="user_metadata" type="dictionary">
      Champs personnalisés qui stockent des renseignements sur un utilisateur sans incidence sur ce à quoi il peut accéder, comme son adresse professionnelle, son adresse domiciliaire ou ses préférences.
    </ResponseField>

    <ResponseField name="username" type="string" post={["optional"]}>
      (unique) Nom d’utilisateur.
    </ResponseField>
  </Expandable>
</ResponseField>