---
description: Découvrez comment appliquer la contrainte de l’émetteur aux jetons dans Auth0.
title: Contrainte de l’émetteur
---

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  L’application de la contrainte de l’émetteur aux jetons au moyen de Demonstrating Proof-of-Possession (DPoP) est actuellement en Accès anticipé. Pour demander l’accès à cette fonctionnalité, communiquez avec votre représentant Auth0.
</Callout>

La contrainte de l’émetteur est un mécanisme de sécurité <Tooltip tip="" cta="Consulter le glossaire" href="/fr-CA/docs/glossary?term=OAuth+2.0">OAuth 2.0</Tooltip> et <Tooltip tip="" cta="Consulter le glossaire" href="/fr-CA/docs/glossary?term=OpenID">OpenID</Tooltip> Connect (OIDC) qui lie de façon cryptographique les jetons d’accès et les <Tooltip tip="" cta="Consulter le glossaire" href="/fr-CA/docs/glossary?term=refresh+tokens">jetons d’actualisation</Tooltip> à l’application qui en a fait la demande, ce qui empêche le vol et l’utilisation abusive des jetons.

Traditionnellement, les <Tooltip tip="" cta="Consulter le glossaire" href="/fr-CA/docs/glossary?term=access+tokens">jetons d’accès</Tooltip> OAuth 2.0 sont des jetons au porteur, ce qui signifie que quiconque « porte » ou possède le jeton peut l’utiliser. Si un jeton au porteur est volé ou divulgué, un attaquant peut le présenter à un <Tooltip tip="" cta="Consulter le glossaire" href="/fr-CA/docs/glossary?term=resource+server">serveur de ressources</Tooltip> (API) et obtenir un accès non autorisé en se faisant passer pour l’application cliente ou l’utilisateur légitime.

La contrainte de l’émetteur garantit que l’application cliente qui présente le jeton d’accès à un serveur de ressources en est bien le propriétaire légitime. Si l’application cliente n’est pas le propriétaire légitime du jeton d’accès, le serveur de ressources rejette la requête API.

<div id="how-it-works">
  ## Fonctionnement
</div>

Vous pouvez mettre en œuvre la contrainte de l’émetteur de l’une des deux façons suivantes :

* Association de certificat Mutual TLS (mTLS) dans la couche de transport :

  * **Mécanisme :** Lorsque l’application cliente demande un jeton d’accès au serveur d’autorisation Auth0, elle établit une connexion TLS mutuelle (mTLS), dans laquelle l’application cliente et le serveur présentent et vérifient mutuellement leurs certificats X.509.
  * **Liaison :** Le serveur d’autorisation Auth0 inclut une revendication de confirmation (`cnf`) contenant l’empreinte du certificat de l’application cliente directement dans le jeton d’accès émis.
  * **Preuve de possession :** Lorsque l’application cliente utilise le jeton d’accès lié à mTLS pour accéder à un serveur de ressources, elle doit de nouveau établir une connexion mTLS à l’aide du même certificat. Le serveur de ressources vérifie que le certificat présenté par l’application cliente correspond à celui lié au jeton d’accès. Sinon, le serveur de ressources rejette la requête.
  * **Avantage :** Même si un attaquant vole le jeton d’accès, il ne peut pas l’utiliser, car il ne possède pas la clé privée et le certificat correspondants nécessaires pour établir la bonne connexion mTLS. La contrainte de l’émetteur mTLS est généralement utilisée par des applications confidentielles, comme les applications côté serveur, qui peuvent stocker et gérer en toute sécurité des certificats X.509 et leurs clés privées.
* Demonstrating Proof-of-Possession (DPoP) dans la couche applicative :

  * **Mécanisme :** DPoP fonctionne à la couche applicative et ne nécessite pas mTLS. À la place, l’application cliente génère sa propre paire de clés cryptographiques (clé privée/clé publique).
  * **Liaison :** Lorsqu’elle demande un jeton d’accès, l’application cliente crée un JSON Web Token (JWT) appelé JWT de preuve DPoP. Ce JWT de preuve contient la clé publique de l’application cliente et est signé avec sa clé privée. L’application cliente envoie le JWT de preuve DPoP avec la demande de jeton d’accès. Le serveur d’autorisation Auth0 valide le JWT de preuve DPoP, puis lie le jeton d’accès émis à la clé publique.
  * **Preuve de possession :** Lorsque l’application cliente utilise le jeton d’accès lié à DPoP pour appeler un serveur de ressources, elle génère un autre JWT de preuve DPoP signé avec sa clé privée pour cette requête d’API. L’application cliente envoie le JWT de preuve DPoP dans un en-tête avec le jeton d’accès. Le serveur de ressources vérifie que le jeton d’accès est lié à la clé publique du JWT de preuve DPoP et que le JWT de preuve DPoP lui-même a été signé par la clé privée correspondante au moyen d’une revendication de confirmation (`cnf`).
  * **Avantage :** DPoP est plus flexible que mTLS, car il ne nécessite pas d’infrastructure à clés publiques. Divers types d’applications peuvent l’utiliser, y compris des applications publiques comme les SPA et les applications mobiles.

<div id="mtls-vs-dpop">
  ## mTLS vs. DPoP
</div>

Le tableau suivant résume les principales différences entre mTLS et DPoP pour les jetons soumis à une contrainte de l’émetteur :

| Attribut             | mTLS                                                               | DPoP                                                                         |
| -------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| Couche d’opération   | Couche transport (TLS/SSL)                                         | Couche application (en-têtes HTTP)                                           |
| Cryptographie        | Utilisation de l’infrastructure à clé publique (certificats X.509) | Utilisation de clés asymétriques (paires de clés générées par l’application) |
| Preuve de possession | Négociation TLS et validation du certificat                        | Preuve DPoP (JWT signé dans l’en-tête HTTP pour chaque requête)              |
| Type d’application   | Applications confidentielles                                       | Applications publiques (SPA, applications mobiles)                           |

Pour en savoir plus, consultez [Contrainte de l’émetteur mTLS](/fr-CA/docs/secure/sender-constraining/mtls-sender-constraining) et [Démonstration de la preuve de possession (DPoP)](/fr-CA/docs/secure/sender-constraining/demonstrating-proof-of-possession-dpop).

<div id="get-started">
  ## Pour commencer
</div>

Pour commencer avec la contrainte de l’émetteur dans Auth0, consultez ce qui suit :

| Lire                                                                                                                       | Pour en savoir plus                                                                                                 |
| -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| [contrainte de l’émetteur mTLS](/fr-CA/docs/secure/sender-constraining/mtls-sender-constraining)                               | Comment fonctionne la contrainte de l’émetteur mTLS dans Auth0, étape par étape.                                  |
| [Démonstration de la preuve de possession (DPoP)](/fr-CA/docs/secure/sender-constraining/demonstrating-proof-of-possession-dpop) | Comment fonctionne DPoP dans Auth0, étape par étape.                                                                |
| [Configurer la contrainte de l’émetteur](/fr-CA/docs/secure/sender-constraining/configure-sender-constraining)                 | Comment configurer la contrainte de l’émetteur pour une application cliente et un serveur de ressources dans Auth0. |

<div id="learn-more">
  ## En savoir plus
</div>

* [contrainte de l’émetteur mTLS](/fr-CA/docs/secure/sender-constraining/mtls-sender-constraining)
* [Démonstration de la preuve de possession (DPoP)](/fr-CA/docs/secure/sender-constraining/demonstrating-proof-of-possession-dpop)
* [Configurer la contrainte de l’émetteur](/fr-CA/docs/secure/sender-constraining/configure-sender-constraining)