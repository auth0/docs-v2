---
title: "MfaWebAuthnPlatformChallengeMembers"
---

MfaWebAuthnPlatformChallengeMembers

````ts Example
export interface MfaWebAuthnPlatformChallengeMembers extends BaseMembers {
  /**
   * Screen-specific properties and data, including WebAuthn challenge options and remember device preference.
   * @type {ScreenMembersOnMfaWebAuthnPlatformChallenge}
   */
  screen: ScreenMembersOnMfaWebAuthnPlatformChallenge;

  /**
   * Lance le défi de l'authentificateur de plateforme WebAuthn.
   * Cette méthode appelle en interne `navigator.credentials.get()` en utilisant les options de défi
   * fournies dans `screen.publicKey`.
   * En cas de succès, elle soumet les informations d'identification résultantes à Auth0 avec `action: "default"`.
   *
   * Si `navigator.credentials.get()` échoue (p. ex., annulation par l'utilisateur, délai d'attente ou autres erreurs de l'API WebAuthn),
   * il est recommandé d'appeler `reportBrowserError` avec les détails de l'erreur.
   *
   * @param {VerifyPlatformAuthenticatorOptions} [options] - Paramètres optionnels pour le processus de vérification,
   * tels que `rememberDevice` (si `screen.showRememberDevice` est vrai) et d'autres options personnalisées.
   * @returns {Promise<void>} Une promesse qui se résout lorsque la tentative de vérification est soumise.
   *                          Une opération réussie entraîne généralement une redirection.
   * @throws {Error} Génère une erreur si `screen.publicKey` est manquant,
   *                 si `navigator.credentials.get()` échoue avec une erreur inattendue non gérée par `getPasskeyCredentials`,
   *                 ou si la soumission du formulaire à Auth0 échoue.
   *
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnPlatformChallenge
   * try {
   *   const remember = sdk.screen.showRememberDevice && userCheckedRememberDeviceBox;
   *   await sdk.verify({ rememberDevice: remember });
   *   // On success, Auth0 handles redirection.
   * } catch (error) {
   *   console.error("Platform authenticator verification failed:", error);
   *   // If it's a WebAuthn API error, report it
   *   if (error.name && error.message) { // Basic check for DOMException like error
   *     await sdk.reportBrowserError({ error: { name: error.name, message: error.message } });
   *   }
   *   // Check sdk.transaction.errors for server-side validation messages if the page reloads.
   * }
   * ```
   */
  verify(options?: VerifyPlatformAuthenticatorOptions): Promise<void>;

  /**
   * Reports a specific WebAuthn API error (from `navigator.credentials.get()`) to Auth0.
   * This method should be used when `verify()` (or a manual `navigator.credentials.get()` call) fails due to
   * a browser-side WebAuthn issue (e.g., user cancellation `NotAllowedError`, timeout).
   * It submits the error details with `action: "showError::{errorDetailsJsonString}"` and an empty `response`.
   *
   * @param {ReportBrowserErrorOptions} options - Contains the `error` object (with `name` and `message`
   * from the WebAuthn API DOMException) and any other custom options.
   * @returns {Promise<void>} A promise that resolves when the error report is submitted.
   * @throws {Error} Throws an error if the form submission fails (e.g., network error, invalid state).
   *
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnPlatformChallenge
   * // And webAuthnDomException is an error object from a failed navigator.credentials.get() call.
   * try {
   *   await sdk.reportBrowserError({
   *     error: { name: webAuthnDomException.name, message: webAuthnDomException.message }
   *   });
   *   // Auth0 will process this error and may re-render the page or redirect.
   * } catch (submitError) {
   *   console.error("Failed to report WebAuthn browser error:", submitError);
   * }
   * ```
   */
  reportBrowserError(options: ReportBrowserErrorOptions): Promise<void>;

  /**
   * Allows the user to opt-out of the WebAuthn platform challenge and select a different MFA method.
   * This action submits `action: "pick-authenticator"` to Auth0, which should navigate
   * the user to an MFA factor selection screen.
   *
   * @param {TryAnotherMethodOptions} [options] - Optional custom parameters to be sent with the request.
   * @returns {Promise<void>} A promise that resolves when the 'pick-authenticator' action is submitted.
   * @throws {Error} Throws an error if the form submission fails.
   *
   * @example
   * ```typescript
   * // Assuming 'sdk' is an instance of MfaWebAuthnPlatformChallenge
   * try {
   *   await sdk.tryAnotherMethod();
   *   // On success, Auth0 handles redirection to MFA selection.
   * } catch (error) {
   *   console.error("Failed to switch MFA method:", error);
   * }
   * ```
   */
  tryAnotherMethod(options?: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>} />

<ParamField body="organization" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>} />

<ParamField body="prompt" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>} />

<ParamField body="screen" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnMfaWebAuthnPlatformChallenge">ScreenMembersOnMfaWebAuthnPlatformChallenge</a></span>}>
  Propriétés et données propres à l’écran, y compris les options du défi WebAuthn et le paramètre « se souvenir de l’appareil ».
</ParamField>

<ParamField body="tenant" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>} />

<ParamField body="untrustedData" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Méthodes
</div>

<ParamField body="reportBrowserError" type="Promise<void>">
  Signale à Auth0 une erreur précise de l’API WebAuthn (provenant de `navigator.credentials.get()`).
  Cette méthode doit être utilisée lorsque `verify()` (ou un appel manuel à `navigator.credentials.get()`) échoue en raison
  d’un problème WebAuthn côté navigateur (p. ex., annulation par l’utilisateur `NotAllowedError`, délai d’expiration).
  Elle transmet les détails de l’erreur avec `action: "showError::{errorDetailsJsonString}"` et une `response` vide.

  Une promesse qui se résout lorsque le signalement de l’erreur est envoyé.

  #### Lève

  Lève une erreur si l’envoi du formulaire échoue (p. ex., erreur réseau, `state` invalide).

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnPlatformChallenge
  // Et que webAuthnDomException est un objet d’erreur provenant d’un appel échoué à navigator.credentials.get().
  try {
    await sdk.reportBrowserError({
      error: { name: webAuthnDomException.name, message: webAuthnDomException.message }
    });
    // Auth0 traitera cette erreur et pourra réafficher la page ou rediriger.
  } catch (submitError) {
    console.error("Failed to report WebAuthn browser error:", submitError);
  }
  ```

  <Expandable title="Paramètres">
    <ParamField body="options" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnPlatformChallengeReportErrorOptions">MfaWebAuthnPlatformChallengeReportErrorOptions</a></span>}>
      Contient l’objet `error` (avec `name` et `message`
      de la DOMException de l’API WebAuthn) ainsi que toute autre option personnalisée.

      **Propriétés**

      <ParamField body="error" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>}>
        L’objet d’erreur de l’API WebAuthn (`navigator.credentials.get()`) à signaler.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Permet à l’utilisateur d’abandonner le défi de plateforme WebAuthn et de sélectionner une autre méthode MFA.
  Cette action envoie `action: "pick-authenticator"` à Auth0, ce qui devrait diriger
  l’utilisateur vers un écran de sélection d’un facteur MFA.

  Une promesse qui se résout lorsque l’action `pick-authenticator` est envoyée.

  #### Lève

  Lève une erreur si l’envoi du formulaire échoue.

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnPlatformChallenge
  try {
    await sdk.tryAnotherMethod();
    // En cas de réussite, Auth0 gère la redirection vers la sélection MFA.
  } catch (error) {
    console.error("Failed to switch MFA method:", error);
  }
  ```

  <Expandable title="Paramètres">
    <ParamField body="options?" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/MfaWebAuthnPlatformChallengeTryAnotherMethodOptions">MfaWebAuthnPlatformChallengeTryAnotherMethodOptions</a></span>}>
      Paramètres personnalisés facultatifs à envoyer avec la requête.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="verify" type="Promise<void>">
  Lance le défi d’authentification WebAuthn sur la plateforme.
  Cette méthode appelle en interne `navigator.credentials.get()` à l’aide des options de défi
  fournies dans `screen.publicKey`.
  Si l’opération réussit, elle envoie à Auth0 les informations d’authentification obtenues avec `action: "default"`.

  Si `navigator.credentials.get()` échoue (p. ex., annulation par l’utilisateur, délai d’expiration ou autre erreur de l’API WebAuthn),
  il est recommandé d’appeler `reportBrowserError` avec les détails de l’erreur.

  Une promesse qui est résolue lorsque la tentative de vérification est soumise.
  Une opération réussie entraîne généralement une redirection.

  #### Lève une erreur

  Lève une erreur si `screen.publicKey` est absent,
  si `navigator.credentials.get()` échoue avec une erreur inattendue non gérée par `getPasskeyCredentials`,
  ou si l’envoi du formulaire à Auth0 échoue.

  ```typescript Example
  // En supposant que 'sdk' est une instance de MfaWebAuthnPlatformChallenge
  try {
    const remember = sdk.screen.showRememberDevice && userCheckedRememberDeviceBox;
    await sdk.verify({ rememberDevice: remember });
    // En cas de réussite, Auth0 gère la redirection.
  } catch (error) {
    console.error("Platform authenticator verification failed:", error);
    // S'il s'agit d'une erreur de l'API WebAuthn, signalez-la
    if (error.name && error.message) { // Vérification de base pour une erreur de type DOMException
      await sdk.reportBrowserError({ error: { name: error.name, message: error.message } });
    }
    // Vérifiez sdk.transaction.errors pour les messages de validation côté serveur si la page se recharge.
  }
  ```

  <Expandable title="Paramètres">
    <ParamField body="options?" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/VerifyPlatformAuthenticatorOptions">VerifyPlatformAuthenticatorOptions</a></span>}>
      Paramètres facultatifs du processus de vérification,
      comme `rememberDevice` (si `screen.showRememberDevice` vaut `true`) et d’autres options personnalisées.

      **Propriétés**

      <ParamField body="rememberDevice?" type="boolean">
        Facultatif. Si `true`, tente de mémoriser le navigateur pour les prochains défis MFA.
        Correspond au champ de formulaire `rememberBrowser`. Cela s’applique uniquement si `screen.showRememberDevice` vaut `true`.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>