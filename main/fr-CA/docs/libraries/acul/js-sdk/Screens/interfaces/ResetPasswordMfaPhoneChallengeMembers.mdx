---
title: "ResetPasswordMfaPhoneChallengeMembers"
---

ResetPasswordMfaPhoneChallengeMembers

````ts Example
export interface ResetPasswordMfaPhoneChallengeMembers extends BaseMembers {
  /**
   * Accès aux informations propres à l'application (p. ex., ID client, nom).
   * @type {ClientMembers}
   */
  client: ClientMembers;

  /**
   * Accès aux informations propres à l'organisation, le cas échéant.
   * @type {OrganizationMembers}
   */
  organization: OrganizationMembers;

  /**
   * Accès aux détails de l'invite (p. ex., nom de l'invite).
   * @type {PromptMembers}
   */
  prompt: PromptMembers;

  /**
   * Accès aux propriétés et données spécifiques de l'écran 'reset-password-mfa-phone-challenge'.
   * @type {ScreenMembersOnResetPasswordMfaPhoneChallenge}
   */
  screen: ScreenMembersOnResetPasswordMfaPhoneChallenge;

  /**
   * Accès aux détails de la transaction en cours (p. ex., state, erreurs).
   * @type {TransactionMembers}
   */
  transaction: TransactionMembers;

  /**
   * Envoie le code de vérification au téléphone de l'utilisateur via la méthode spécifiée (SMS ou appel vocal).
   * Cette action correspond au clic de l'utilisateur sur le bouton « Envoyer le code par SMS » ou « Envoyer le code par appel vocal ».
   * Elle déclenche le backend Auth0 pour lancer le processus d'envoi du code.
   *
   * @param {ContinueOptions} payload - Les options précisant le type d'envoi ('sms' ou 'voice') et toute donnée personnalisée.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès au serveur.
   * @throws {Error} Génère une erreur si la soumission échoue (p. ex., problème réseau, state invalide).
   * @example
   * ```typescript
   * import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';
   *
   * const screen = new ResetPasswordMfaPhoneChallenge();
   *
   * // Envoyer le code par SMS
   * await screen.continue({ type: 'sms' });
   *
   * // Envoyer le code par appel vocal
   * await screen.continue({ type: 'voice' });
   * ```
   */
  continue(payload: ContinueOptions): Promise<void>;

  /**
   * Redirige l'utilisateur vers l'écran où il peut sélectionner une autre méthode MFA.
   * Cette action correspond au clic de l'utilisateur sur le bouton « Essayer une autre méthode ».
   *
   * @param {TryAnotherMethodOptions} payload - Les options précisant le type de défi en cours (`sms` ou `voice`) et toute donnée personnalisée. Le champ `type` est nécessaire pour indiquer au backend le flux que l'utilisateur quitte.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès au serveur.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';
   *
   * const screen = new ResetPasswordMfaPhoneChallenge();
   *
   * // Si l'option SMS a été présentée à l'utilisateur initialement :
   * await screen.tryAnotherMethod({ type: 'sms' });
   *
   * // Si l'option appel vocal a été présentée à l'utilisateur initialement :
   * await screen.tryAnotherMethod({ type: 'voice' });
   * ```
   */
  tryAnotherMethod(payload: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/ClientMembers">ClientMembers</a></span>}>
  Accès aux informations propres à l’application (p. ex., ID client, nom).
</ParamField>

<ParamField body="organization" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Accès aux informations propres à l’organisation, s’il y a lieu.
</ParamField>

<ParamField body="prompt" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/PromptMembers">PromptMembers</a></span>}>
  Accès aux détails de l’invite (p. ex., nom de l’invite).
</ParamField>

<ParamField body="screen" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/ScreenMembersOnResetPasswordMfaPhoneChallenge">ScreenMembersOnResetPasswordMfaPhoneChallenge</a></span>}>
  Accès aux propriétés et aux données propres à l’écran « reset-password-mfa-phone-challenge ».
</ParamField>

<ParamField body="tenant" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Accès aux détails de la transaction en cours (p. ex., state, erreurs).
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Méthodes
</div>

<ParamField body="continue" type="Promise<void>">
  Envoie le code de vérification au téléphone de l’utilisateur selon la méthode indiquée (SMS ou appel vocal).
  Cette action correspond au clic de l’utilisateur sur le bouton « Envoyer le code par SMS » ou « Envoyer le code par appel vocal ».
  Elle déclenche le backend d’Auth0 pour lancer le processus d’envoi du code.

  Une promesse résolue lorsque l’action est transmise au serveur avec succès.

  #### Lève

  Lève une erreur si l’envoi échoue (p. ex., problème de réseau, `state` invalide).

  ```typescript Example
  import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';

  const screen = new ResetPasswordMfaPhoneChallenge();

  // Envoyer le code par SMS
  await screen.continue({ type: 'sms' });

  // Envoyer le code par appel vocal
  await screen.continue({ type: 'voice' });
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/ResetPasswordMfaPhoneChallengeContinueOptions">ResetPasswordMfaPhoneChallengeContinueOptions</a></span>}>
      Les options qui précisent la méthode d’envoi (`sms` ou `voice`) et toute donnée personnalisée.

      **Propriétés**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        La méthode d’envoi choisie par l’utilisateur pour recevoir le code de vérification.

        * `sms` : envoie le code par message texte.
        * `voice` : envoie le code par appel vocal.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Redirige l’utilisateur vers l’écran où il peut sélectionner une autre méthode MFA.
  Cette action correspond au clic de l’utilisateur sur le bouton « Essayer une autre méthode ».

  Une promesse résolue lorsque l’action est transmise au serveur avec succès.

  #### Lève

  Lève une erreur si l’envoi échoue.

  ```typescript Example
  import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';

  const screen = new ResetPasswordMfaPhoneChallenge();

  // Si l’utilisateur a d’abord vu l’option SMS :
  await screen.tryAnotherMethod({ type: 'sms' });

  // Si l’utilisateur a d’abord vu l’option Voice :
  await screen.tryAnotherMethod({ type: 'voice' });
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload" type={<span><a href="/fr-CA/docs/libraries/acul/js-sdk/Screens/interfaces/ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions">ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions</a></span>}>
      Les options qui précisent le type de défi actuel (`sms` ou `voice`) et toute donnée personnalisée. Le champ `type` est nécessaire pour informer le backend du flux que l’utilisateur quitte.

      **Propriétés**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        La méthode d’envoi qui a d’abord été présentée ou sélectionnée sur l’écran actuel.
        Elle est requise par le point de terminaison de l’API pour traiter correctement l’action `pick-authenticator`.

        * `sms` : indique que l’écran actuel correspondait à un défi par SMS.
        * `voice` : indique que l’écran actuel correspondait à un défi par appel vocal.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>