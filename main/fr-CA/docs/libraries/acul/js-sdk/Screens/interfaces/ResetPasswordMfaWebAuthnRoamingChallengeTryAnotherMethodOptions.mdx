---
title: "ResetPasswordMfaWebAuthnRoamingChallengeTryAnotherMethodOptions"
---

TryAnotherMethodOptions

```ts Example
export interface TryAnotherMethodOptions extends CustomOptions {
  /**
   * Facultatif. Si `true`, et si le contexte de l'écran (`screen.showRememberDevice`)
   * indique que la mémorisation de l'appareil est une option, cela signale l'intention
   * de l'utilisateur de mémoriser ce navigateur/appareil.
   * @type {boolean | undefined}
   */
  rememberDevice?: boolean;
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="rememberDevice?" type="boolean">
  Facultatif. Si `true` et que `screen.showRememberDevice` est également `true`,
  le SDK inclut `rememberBrowser=true` dans la soumission du formulaire.
</ParamField>