---
title: "ContinuePayloadOptions"
---

Options pour continuer la vérification par courriel

```ts Example
export interface ContinueOptions {
  /** Le code saisi par l'utilisateur */
  code: string;
  /** Indique si l'appareil doit être mémorisé */
  rememberDevice?: boolean;
  /** Toute option personnalisée supplémentaire */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Toute autre option personnalisée

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Le code saisi par l’utilisateur
</ParamField>

<ParamField body="rememberDevice?" type="boolean">
  Indique si l’appareil doit être mémorisé
</ParamField>