---
title: "ResetPasswordMfaOtpChallengeContinueOptions"
---

Options pour poursuivre le défi OTP.

```ts Example
export interface ContinueOptions {
  /** Le code saisi par l'utilisateur. */
  code: string;
  /** Toutes les options personnalisées supplémentaires */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Toute autre option personnalisée

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Le code saisi par l’utilisateur.
</ParamField>