---
title: "MfaOtpContinueOptions"
---

Options permettant de poursuivre l’inscription MFA avec le code OTP.

```ts Example
export interface ContinueOptions {
  /**
   * Le code OTP saisi par l'utilisateur.
   */
  code: string;

  /**
   * Toutes les options personnalisées supplémentaires.
   */
  [key: string]: string | number | boolean | undefined;
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

Toute autre option personnalisée supplémentaire.

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string">
  Le code OTP saisi par l’utilisateur.
</ParamField>