---
title: "ResetPasswordMfaPhoneChallengeContinueOptions"
---

ContinueOptions

```ts Example
export interface ContinueOptions extends CustomOptions {
  /**
   * La méthode d'envoi sélectionnée par l'utilisateur pour recevoir le code de vérification.
   * - `sms`: Envoyer le code par message texte.
   * - `voice`: Envoyer le code par appel téléphonique.
   * @type {'sms' | 'voice'}
   */
  type: 'sms' | 'voice';
}
```

<div id="indexable">
  ## Indexable
</div>

[`key`: `string`]: `string` | `number` | `boolean` | `undefined`

<div id="properties">
  ## Propriétés
</div>

<ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
  La méthode d’envoi sélectionnée par l’utilisateur pour recevoir le code de vérification.

  * `sms` : Envoyer le code par message texte.
  * `voice` : Envoyer le code par appel téléphonique.
</ParamField>