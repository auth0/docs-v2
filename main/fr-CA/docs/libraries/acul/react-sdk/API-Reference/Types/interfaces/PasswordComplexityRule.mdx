---
title: "PasswordComplexityRule"
---

Représente une règle de complexité du mot de passe évaluée. Est renvoyée dans [`PasswordValidationResult`](/fr-CA/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PasswordValidationResult) par [`usePasswordValidation`](/fr-CA/docs/libraries/acul/react-sdk/API-Reference/Hooks/usePasswordValidation). Prend en charge la composition hiérarchique des règles au moyen du tableau facultatif `items`.

```typescript
export interface PasswordComplexityRule {
  code: string;
  label: string;
  status: "valid" | "error";
  isValid: boolean;
  args?: {
    count: number;
    total?: number;
    example?: string;
  };
  items?: PasswordComplexityRule[];
}
```

<div id="properties">
  ## Propriétés
</div>

<ParamField body="code" type="string" required>
  Un identifiant lisible par machine pour cette règle de complexité. Exemple : `"lengthAtLeast"`, `"containsAtLeast"`.
</ParamField>

<ParamField body="label" type="string" required>
  Un nom d’affichage en langage clair pour la règle, qui convient à un indicateur de force du mot de passe.
</ParamField>

<ParamField body="status" type="&#x22;valid&#x22; | &#x22;error&#x22;" required>
  Le résultat de la validation pour cette règle. `"valid"` si la règle est respectée, `"error"` si elle ne l’est pas.
</ParamField>

<ParamField body="isValid" type="boolean" required>
  Représentation booléenne du résultat de la règle. `true` lorsque `status` est `"valid"`.
</ParamField>

<ParamField body="args" type="object">
  Paramètres supplémentaires utilisés pour décrire le seuil de la règle ou fournir du contexte pour le message d’erreur.

  <Expandable title="propriétés">
    <ParamField body="count" type="number" required>
      Le seuil numérique ou le nombre associé à cette règle. Exemple : nombre minimal de caractères.
    </ParamField>

    <ParamField body="total" type="number">
      Le nombre total de caractères ou d’éléments pertinents pour cette règle, le cas échéant.
    </ParamField>

    <ParamField body="example" type="string">
      Une valeur d’exemple illustrant ce qui satisfait cette règle, le cas échéant.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="items" type={<span><a href="/fr-CA/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PasswordComplexityRule">PasswordComplexityRule</a>[]</span>}>
  Règles de complexité imbriquées pour les groupes de règles composites. Permet une composition hiérarchique dans laquelle une règle parente regroupe plusieurs règles enfants.
</ParamField>