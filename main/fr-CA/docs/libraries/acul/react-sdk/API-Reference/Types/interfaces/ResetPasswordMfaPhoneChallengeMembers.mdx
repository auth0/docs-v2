---
title: "ResetPasswordMfaPhoneChallengeMembers"
---

ResetPasswordMfaPhoneChallengeMembers

````ts Example
export interface ResetPasswordMfaPhoneChallengeMembers extends BaseMembers {
  /**
   * Accès aux informations propres à l'application (p. ex., ID client, nom).
   * @type {ClientMembers}
   */
  client: ClientMembers;

  /**
   * Accès aux informations propres à l'organisation, le cas échéant.
   * @type {OrganizationMembers}
   */
  organization: OrganizationMembers;

  /**
   * Accès aux détails de l'invite (p. ex., nom de l'invite).
   * @type {PromptMembers}
   */
  prompt: PromptMembers;

  /**
   * Accès aux propriétés et données spécifiques de l'écran 'reset-password-mfa-phone-challenge'.
   * @type {ScreenMembersOnResetPasswordMfaPhoneChallenge}
   */
  screen: ScreenMembersOnResetPasswordMfaPhoneChallenge;

  /**
   * Accès aux détails de la transaction en cours (p. ex., state, erreurs).
   * @type {TransactionMembers}
   */
  transaction: TransactionMembers;

  /**
   * Envoie le code de vérification au téléphone de l'utilisateur via la méthode spécifiée (SMS ou appel vocal).
   * Cette action correspond au clic de l'utilisateur sur le bouton « Envoyer le code par SMS » ou « Envoyer le code par appel vocal ».
   * Elle déclenche le traitement côté serveur Auth0 pour lancer le processus d'envoi du code.
   *
   * @param {ContinueOptions} payload - Les options précisant le type d'envoi ('sms' ou 'voice') et toute donnée personnalisée.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès au serveur.
   * @throws {Error} Génère une erreur si la soumission échoue (p. ex., problème réseau, state invalide).
   * @example
   * ```typescript
   * import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';
   *
   * const screen = new ResetPasswordMfaPhoneChallenge();
   *
   * // Envoyer le code par SMS
   * await screen.continue({ type: 'sms' });
   *
   * // Envoyer le code par appel vocal
   * await screen.continue({ type: 'voice' });
   * ```
   */
  continue(payload: ContinueOptions): Promise<void>;

  /**
   * Redirige l'utilisateur vers l'écran où il peut sélectionner une autre méthode MFA.
   * Cette action correspond au clic de l'utilisateur sur le bouton « Essayer une autre méthode ».
   *
   * @param {TryAnotherMethodOptions} payload - Les options précisant le type de défi en cours (`sms` ou `voice`) et toute donnée personnalisée. Le champ `type` est nécessaire pour indiquer au serveur le flux que l'utilisateur quitte.
   * @returns {Promise<void>} Une promesse qui se résout lorsque l'action est soumise avec succès au serveur.
   * @throws {Error} Génère une erreur si la soumission échoue.
   * @example
   * ```typescript
   * import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';
   *
   * const screen = new ResetPasswordMfaPhoneChallenge();
   *
   * // Si l'option SMS a été présentée à l'utilisateur initialement :
   * await screen.tryAnotherMethod({ type: 'sms' });
   *
   * // Si l'option appel vocal a été présentée à l'utilisateur initialement :
   * await screen.tryAnotherMethod({ type: 'voice' });
   * ```
   */
  tryAnotherMethod(payload: TryAnotherMethodOptions): Promise<void>;
}
````

<div id="properties">
  ## Propriétés
</div>

<ParamField body="branding" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/BrandingMembers">BrandingMembers</a></span>} />

<ParamField body="client" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ClientMembers">ClientMembers</a></span>}>
  Accès aux informations propres à l’application (p. ex., ID client, nom).
</ParamField>

<ParamField body="organization" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/OrganizationMembers">OrganizationMembers</a></span>}>
  Accès aux informations propres à l’organisation, s’il y a lieu.
</ParamField>

<ParamField body="prompt" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/PromptMembers">PromptMembers</a></span>}>
  Accès aux détails de l’invite (p. ex., le nom de l’invite).
</ParamField>

<ParamField body="screen" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ScreenMembersOnResetPasswordMfaPhoneChallenge">ScreenMembersOnResetPasswordMfaPhoneChallenge</a></span>}>
  Accès aux propriétés et aux données propres à l’écran « reset-password-mfa-phone-challenge ».
</ParamField>

<ParamField body="tenant" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TenantMembers">TenantMembers</a></span>} />

<ParamField body="transaction" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/TransactionMembers">TransactionMembers</a></span>}>
  Accès aux détails de la transaction en cours (p. ex., state, erreurs).
</ParamField>

<ParamField body="untrustedData" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UntrustedDataMembers">UntrustedDataMembers</a></span>} />

<ParamField body="user" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/UserMembers">UserMembers</a></span>} />

<div id="methods">
  ## Méthodes
</div>

<ParamField body="continue" type="Promise<void>">
  Envoie le code de vérification au téléphone de l&#39;utilisateur par la méthode indiquée (SMS ou appel vocal).
  Cette action correspond au clic sur le bouton « Envoyer le code par SMS » ou « Envoyer le code par appel vocal ».
  Elle déclenche le processus d&#39;envoi du code dans le backend Auth0.

  Une promesse qui se résout lorsque l&#39;action est soumise au serveur avec succès.

  #### Lève

  Lève une erreur si l&#39;envoi échoue (par exemple, en cas de problème réseau ou de `state` invalide).

  ```typescript Example
  import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';

  const screen = new ResetPasswordMfaPhoneChallenge();

  // Envoyer le code par SMS
  await screen.continue({ type: 'sms' });

  // Envoyer le code par appel vocal
  await screen.continue({ type: 'voice' });
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaPhoneChallengeContinueOptions">ResetPasswordMfaPhoneChallengeContinueOptions</a></span>}>
      Les options qui précisent le type d&#39;envoi (`sms` ou `voice`) ainsi que toute donnée personnalisée.

      **Propriétés**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        La méthode d&#39;envoi choisie par l&#39;utilisateur pour recevoir le code de vérification.

        * `sms`: Envoyer le code par message texte.
        * `voice`: Envoyer le code par appel vocal.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="tryAnotherMethod" type="Promise<void>">
  Redirige l&#39;utilisateur vers l&#39;écran où il peut sélectionner une autre méthode MFA.
  Cette action correspond au clic sur le bouton « Essayer une autre méthode ».

  Une promesse qui se résout lorsque l&#39;action est soumise au serveur avec succès.

  #### Lève

  Lève une erreur si l&#39;envoi échoue.

  ```typescript Example
  import ResetPasswordMfaPhoneChallenge from '@auth0/auth0-acul-js/reset-password-mfa-phone-challenge';

  const screen = new ResetPasswordMfaPhoneChallenge();

  // Si l'option SMS a été présentée initialement à l'utilisateur :
  await screen.tryAnotherMethod({ type: 'sms' });

  // Si l'option d'appel vocal a été présentée initialement à l'utilisateur :
  await screen.tryAnotherMethod({ type: 'voice' });
  ```

  <Expandable title="Paramètres">
    <ParamField body="payload" type={<span><a href="/docs/libraries/acul/react-sdk/API-Reference/Types/interfaces/ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions">ResetPasswordMfaPhoneChallengeTryAnotherMethodOptions</a></span>}>
      Les options qui précisent le type de défi actuel (`sms` ou `voice`) ainsi que toute donnée personnalisée. Le champ `type` est nécessaire pour indiquer au backend de quel flux l&#39;utilisateur se détourne.

      **Propriétés**

      <ParamField body="type" type="&#x22;sms&#x22; | &#x22;voice&#x22;">
        La méthode d&#39;envoi présentée ou sélectionnée initialement sur l&#39;écran actuel.
        Ce champ est requis par le point de terminaison de l&#39;API pour traiter correctement l&#39;action &#39;pick-authenticator&#39;.

        * `sms`: Indique que l&#39;écran actuel correspondait à un défi par SMS.
        * `voice`: Indique que l&#39;écran actuel correspondait à un défi par appel vocal.
      </ParamField>
    </ParamField>
  </Expandable>
</ParamField>