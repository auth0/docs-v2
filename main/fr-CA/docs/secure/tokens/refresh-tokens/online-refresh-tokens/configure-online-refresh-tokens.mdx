---
title: Configurer et utiliser des jetons d’actualisation en ligne
description: Comment configurer et utiliser des jetons d’actualisation en ligne.
---

import { ReleaseStageNotice } from "/snippets/fr-CA/ReleaseStageNotice.jsx"

<ReleaseStageNotice feature="Jetons d’actualisation en ligne" stage="beta" contact="votre responsable de compte technique" />

<div id="configure-orts">
  ## Configurer les ORT
</div>

Les jetons d’actualisation en ligne (ORT) doivent être activés au niveau de l’[API (serveur de ressources)](/fr-CA/docs/get-started/apis/api-settings), dans Auth0 Dashboard ou à l’aide de la Management API.

<div id="configure-using-the-dashboard">
  ### Configurer à l’aide d’Auth0 Dashboard
</div>

1. Accédez à [Auth0 Dashboard &gt; Applications &gt; API](https://manage.auth0.com/#/apis).

2. Sélectionnez l’API que vous souhaitez configurer.

3. Dans l’onglet **Paramètres**, activez l’interrupteur **Autoriser l’accès en ligne**.

   <Frame>![Paramètres des API des applications du Dashboard : autoriser l’accès en ligne](/docs/images/refresh-tokens/allow-online-access.png)</Frame>

4. Sélectionnez **Enregistrer**.

Pour consulter et configurer les valeurs du délai d’inactivité et de la durée de vie maximale des sessions, consultez les paramètres [Expiration de session du locataire](https://manage.auth0.com/#/tenant/advanced). Pour en savoir plus, consultez [Configurer la durée de vie de la session](/fr-CA/docs/manage-users/sessions/configure-session-lifetime).

<div id="configure-using-the-management-api">
  ### Configurer à l’aide de la Management API
</div>

Pour activer les ORT, envoyez une requête `PATCH` au point de terminaison [Update Resource Server](/fr-CA/docs/api/management/v2/resource-servers/patch-resource-servers-by-id) :

```bash
curl --request PATCH \
  --url 'https://<YOUR_DOMAIN>/api/v2/resource-servers/<YOUR_RESOURCE_SERVER_ID>' \
  --header 'authorization: Bearer <YOUR_MANAGEMENT_API_TOKEN>' \
  --header 'content-type: application/json' \
  --data '{"allow_online_access": true}'
```

Pour vérifier la configuration, effectuez une requête `GET` au serveur de ressources :

```bash
curl --request GET \
  --url 'https://<YOUR_DOMAIN>/api/v2/resource-servers/<YOUR_RESOURCE_SERVER_ID>' \
  --header 'authorization: Bearer <YOUR_MANAGEMENT_API_TOKEN>'
```

La réponse comprend la propriété `allow_online_access` :

```json
{
  "id": "resource-server-id",
  "name": "My API",
  "identifier": "https://my-api.example.com",
  "allow_online_access": true,
  ...
}
```

<div id="get-an-ort">
  ## Obtenir un ORT
</div>

Pour obtenir un ORT, incluez le `online_access` [scope](/fr-CA/docs/get-started/apis/scopes) dans votre requête d’autorisation en utilisant le [flux du code d’autorisation](/fr-CA/docs/get-started/authentication-and-authorization-flow/authorization-code-flow) ou le [flux du code d’autorisation avec PKCE](/fr-CA/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce).

```bash
https://<YOUR_DOMAIN>/authorize \
    audience=<YOUR_API_AUDIENCE> \
    scope=openid profile online_access \
    response_type=code \
    client_id=<YOUR_CLIENT_ID> \
    redirect_uri=<https://YOUR_APP/callback> \
    state=<OPAQUE_VALUE>
```

Une fois l’utilisateur authentifié, échangez le code d’autorisation contre des jetons :

```bash
curl --request POST \
  --url 'https://<YOUR_DOMAIN>/oauth/token' \
  --header 'content-type: application/x-www-form-urlencoded' \
  --data grant_type=authorization_code \
  --data 'client_id=<YOUR_CLIENT_ID>' \
  --data 'code=<YOUR_AUTHORIZATION_CODE>' \
  --data 'redirect_uri=<https://YOUR_APP/callback>' \
  --data 'code_verifier=<YOUR_CODE_VERIFIER>'
```

La réponse du jeton comprend l’ORT :

```json
{
  "access_token": "eyJ...",
  "id_token": "eyJ...",
  "refresh_token": "ORT...",
  "token_type": "Bearer",
  "expires_in": 86400
}
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Les ORT portent le préfixe `ORT` afin de les distinguer des jetons d’actualisation. Toutefois, vous devez considérer le jeton comme opaque et ne pas vous fier à sa structure interne.
</Callout>

<div id="use-an-ort">
  ## Utiliser un ORT
</div>

Échangez l’ORT contre un nouveau [jeton d’accès](/fr-CA/docs/secure/tokens/access-tokens) lorsque le jeton d’accès actuel a expiré ou est sur le point d’expirer :

```bash
curl --request POST \
  --url 'https://<YOUR_DOMAIN>/oauth/token' \
  --header 'content-type: application/x-www-form-urlencoded' \
  --data grant_type=refresh_token \
  --data 'client_id=<YOUR_CLIENT_ID>' \
  --data 'refresh_token=<YOUR_ONLINE_REFRESH_TOKEN>'
```

La réponse contient un nouveau jeton d’accès.
Si votre requête inclut un scope `openid`, la réponse contient un nouveau [jeton d’identité](/fr-CA/docs/secure/tokens/id-tokens):

```json
{
  "access_token": "eyJ...",
  "id_token": "eyJ...",
  "token_type": "Bearer",
  "expires_in": 86400
}
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Auth0 n’émet pas de nouveau `refresh_token` et, comme les ORT ne sont pas renouvelés par rotation, vous continuez à utiliser le même ORT pour les échanges suivants.
</Callout>

<div id="session-extension-behavior">
  ### Comportement de la prolongation de session
</div>

Chaque échange de jeton réussi :

* **Réinitialise le délai d’inactivité de la session :** Le délai d’inactivité est rétabli à sa durée complète.
* **Ne prolonge pas la durée de vie absolue :** L’expiration absolue de la session reste inchangée.
* **Maintient le SSO :** D’autres applications peuvent obtenir des jetons par SSO tant que la session est active.

<div id="revoke-an-ort">
  ## Révoquer un ORT
</div>

Lorsque vous révoquez un ORT, cela met fin à toute la session Auth0, et non seulement au jeton. Tous les ORT associés à cette session sont alors invalidés, et le SSO de l’utilisateur prend fin.

```bash
curl --request POST \
  --url 'https://<YOUR_DOMAIN>/oauth/revoke' \
  --header 'content-type: application/x-www-form-urlencoded' \
  --data 'client_id=<YOUR_CLIENT_ID>' \
  --data 'token=<YOUR_ONLINE_REFRESH_TOKEN>'
```

<div id="use-orts-with-actions">
  ## Utiliser des ORT avec Actions
</div>

Vous pouvez utiliser des ORT avec Auth0 [Actions](/fr-CA/docs/customize/actions/actions-overview) et le [déclencheur post-login](/fr-CA/docs/customize/actions/explore-triggers/signup-and-login-triggers/login-trigger).

Avec Actions, vous pouvez :

* Utiliser l’objet `event.refresh_token` pour déterminer si le jeton est un ORT.
* Accéder à des données propres à la session à l’aide de l’objet `event.session` afin de prendre des décisions en fonction de l’état actuel de la session

```javascript
exports.onExecutePostLogin = async (event, api) => {
  // Vérifier si le jeton est un jeton d'actualisation en ligne
  
  if (event.refresh_token?.access == 'online') {
    // Le jeton est un ORT, vous pouvez ensuite référencer event.session et api.session
    console.log('Exchanging Online Refresh Token bound to Session ID: ', event.session?.id);
    // Récupérer les métadonnées de session et les ajouter aux jetons
    // En supposant que les métadonnées de session ont été stockées au préalable
    const importantInformation = event.session?.metadata?.importantInformation;
    api.accessToken.setCustomClaim('info', importantInformation);
    api.idToken.setCustomClaim('info', importantInformation);
  }
};
```

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  Auth0 révoque l’intégralité de la session de l’utilisateur, et non seulement le jeton, lorsque la méthode `api.refreshToken.revoke()` est utilisée avec un ORT.
</Callout>

<div id="learn-more">
  ## Pour en savoir plus
</div>

* [Jetons d’actualisation en ligne](/fr-CA/docs/secure/tokens/refresh-tokens/online-refresh-tokens/online-refresh-tokens)
* [Rotation des jetons d’actualisation](/fr-CA/docs/secure/tokens/refresh-tokens/refresh-token-rotation)
* [Obtenir des jetons d’actualisation](/fr-CA/docs/secure/tokens/refresh-tokens/get-refresh-tokens)
* [Utiliser des jetons d’actualisation](/fr-CA/docs/secure/tokens/refresh-tokens/use-refresh-tokens)
* [Cycle de vie de la session](/fr-CA/docs/manage-users/sessions/session-lifecycle)
* [Configurer l’authentification silencieuse](/fr-CA/docs/authenticate/login/configure-silent-authentication)
* [Pratiques exemplaires relatives aux jetons](/fr-CA/docs/secure/tokens/token-best-practices)