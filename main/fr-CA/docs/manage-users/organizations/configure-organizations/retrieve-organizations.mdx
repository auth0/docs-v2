---
description: Découvrez comment récupérer des organisations à l’aide de l’Auth0 Dashboard et de la Management API.
title: Récupérer des organisations
---

import {AuthCodeBlock} from "/snippets/fr-CA/AuthCodeBlock.jsx";

import {AuthCodeGroup} from "/snippets/fr-CA/AuthCodeGroup.jsx";

Lorsque vous travaillez avec des [organisations](/fr-CA/docs/manage-users/organizations/organizations-overview) par programmation, vous pourriez avoir besoin de récupérer soit la liste de toutes les organisations, soit une organisation précise par son nom ou son ID.

<Warning>
  La Management API doit être utilisée uniquement avec un client confidentiel. Son utilisation est soumise aux [limites de débit de l’Auth0 Management API](/fr-CA/docs/troubleshoot/customer-support/operational-policies/rate-limit-policy/management-api-endpoint-rate-limits).
</Warning>

<div id="retrieve-tenant-organizations">
  ## Récupérer les organisations du locataire
</div>

Vous pouvez récupérer toutes les organisations d’un locataire au moyen de la <Tooltip tip="Management API : un produit qui permet aux clients d’effectuer des tâches administratives." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=Management+API">Management API</Tooltip>.

<Warning>
  Pour récupérer plus de 1 000 résultats, vous devez utiliser la méthode de pagination par point de contrôle. Pour en savoir plus, consultez le point de terminaison [Get Organizations](https://auth0.com/docs/api/management/v2#!/Organizations/get_organizations) dans notre API Explorer.
</Warning>

Effectuez un appel `GET` au point de terminaison `Get Organizations`. Assurez-vous de remplacer la valeur fictive `MGMT_API_ACCESS_TOKEN` par votre <Tooltip tip="Jeton d’accès : justificatif d’autorisation, sous forme de chaîne opaque ou de JWT, utilisé pour accéder à une API." cta="Voir le glossaire" href="/fr-CA/docs/glossary?term=Access+Token">Jeton d’accès</Tooltip> de la Management API.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/organizations' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/organizations")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/organizations',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/organizations", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Trouvez votre domaine Auth0**

  Votre domaine Auth0 est composé du nom de votre locataire, de votre sous-domaine régional (à moins que votre locataire se trouve dans la région des États-Unis et ait été créé avant juin 2020), puis de `.auth0.com`. Par exemple, si le nom de votre locataire était `travel0`, votre domaine Auth0 serait `travel0.us.auth0.com`. (Si votre locataire se trouvait aux États-Unis et a été créé avant juin 2020, votre nom de domaine serait alors `https://travel0.auth0.com`.)

  Si vous utilisez des domaines personnalisés, utilisez le nom de votre domaine personnalisé.
</Callout>

| Valeur                  | Description                                                                                                                              |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| `MGMT_API_ACCESS_TOKEN` | [Jeton d’accès de la Management API](/fr-CA/docs/secure/tokens/access-tokens/management-api-access-tokens) avec le scope `read:organizations`. |

<div id="response-status-codes">
  ### Codes d’état de la réponse
</div>

Les codes d’état de réponse possibles sont les suivants :

| Code d’état | Code d’erreur          | Message                                                                                                | Cause                                                                                             |
| ----------- | ---------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------- |
| `200`       |                        | Organisations récupérées avec succès.                                                                  |                                                                                                   |
| `400`       | `invalid_paging`       | La page demandée dépasse la limite maximale autorisée de 1000 enregistrements.                         | L’API ne peut renvoyer qu’un maximum de 1000 enregistrements.                                     |
| `400`       | `invalid_query_string` | Chaîne de requête invalide dans la requête. Le message varie selon la cause.                           | La chaîne de requête n’est pas valide.                                                            |
| `401`       |                        | Jeton invalide.                                                                                        |                                                                                                   |
| `401`       |                        | Signature invalide reçue lors de la validation du JSON Web Token.                                      |                                                                                                   |
| `401`       |                        | L’application n’est pas globale.                                                                       |                                                                                                   |
| `403`       | `insufficient_scope`   | Scope insuffisant; l’un des scopes suivants est attendu : `read:organizations`.                        | Tentative de lire ou d’écrire un champ non autorisé avec les scopes fournis dans le Bearer Token. |
| `429`       |                        | Trop de requêtes. Vérifiez les en-têtes X-RateLimit-Limit, X-RateLimit-Remaining et X-RateLimit-Reset. |                                                                                                   |

<div id="retrieve-organization-by-id">
  ## Récupérer une organisation à partir de son ID
</div>

Vous pouvez récupérer une organisation à partir de son ID au moyen de la Management API.

Effectuez une requête `GET` vers le point de terminaison `Get Organization`. Assurez-vous de remplacer les valeurs d’espace réservé `ORG_ID` et `MGMT_API_ACCESS_TOKEN` par l’ID de votre organisation et votre jeton d’accès de la Management API, respectivement.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/organizations/ORG_ID' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations/ORG_ID");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations/ORG_ID"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/organizations/ORG_ID")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/organizations/ORG_ID',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations/ORG_ID",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/organizations/ORG_ID", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations/ORG_ID")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Trouvez votre domaine Auth0**

  Votre domaine Auth0 se compose du nom de votre locataire, de votre sous-domaine régional (à moins que votre locataire se trouve dans la région des États-Unis et ait été créé avant juin 2020), puis de `.auth0.com`. Par exemple, si le nom de votre locataire était `travel0`, le nom de votre domaine Auth0 serait `travel0.us.auth0.com`. (Si votre locataire se trouvait aux États-Unis et avait été créé avant juin 2020, le nom de votre domaine serait alors `https://travel0.auth0.com`.)

  Si vous utilisez des domaines personnalisés, il doit s’agir du nom de votre domaine personnalisé.
</Callout>

| Valeur                  | Description                                                                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `ORG_ID`                | ID de l’organisation que vous souhaitez récupérer.                                                                                         |
| `MGMT_API_ACCESS_TOKEN` | [jeton d’accès de la Management API](/fr-CA/docs/secure/tokens/access-tokens/management-api-access-tokens) avec le scope `read:organizations`. |

<div id="response-status-codes">
  ### Codes d’état de la réponse
</div>

Les codes d’état de la réponse possibles sont les suivants :

| Code de statut | Code d’erreur          | Message                                                                                                | Cause                                                                                                      |
| -------------- | ---------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `200`          |                        | Organisation récupérée avec succès.                                                                    |                                                                                                            |
| `400`          | `invalid_query_string` | Chaîne de requête invalide dans la demande. Le message varie selon la cause.                           | La chaîne de requête n’est pas valide.                                                                     |
| `401`          |                        | Jeton invalide.                                                                                        |                                                                                                            |
| `401`          |                        | Signature invalide reçue lors de la validation du JSON Web Token.                                      |                                                                                                            |
| `401`          |                        | L’application n’est pas globale.                                                                       |                                                                                                            |
| `403`          | `insufficient_scope`   | Scope insuffisant; l’un des scopes suivants est requis : `read:organizations`.                         | Tentative de lire ou d’écrire un champ qui n’est pas autorisé avec les scopes fournis par le Bearer Token. |
| `429`          |                        | Trop de requêtes. Vérifiez les en-têtes X-RateLimit-Limit, X-RateLimit-Remaining et X-RateLimit-Reset. |                                                                                                            |

<div id="retrieve-organization-by-name">
  ## Récupérer une organisation par nom
</div>

Vous pouvez récupérer une organisation par son nom au moyen de la Management API.

Effectuez un appel `GET` au point de terminaison `Get Organization by Name`. Assurez-vous de remplacer les valeurs fictives `ORG_NAME` et `MGMT_API_ACCESS_TOKEN` par le nom de votre organisation et votre jeton d’accès de la Management API, respectivement.

<AuthCodeGroup>
  ```bash cURL
  curl --request GET \
    --url 'https://{yourDomain}/api/v2/organizations/name/ORG_NAME' \
    --header 'authorization: Bearer MGMT_API_ACCESS_TOKEN'
  ```

  ```csharp C#
  var client = new RestClient("https://{yourDomain}/api/v2/organizations/name/ORG_NAME");
  var request = new RestRequest(Method.GET);
  request.AddHeader("authorization", "Bearer MGMT_API_ACCESS_TOKEN");
  IRestResponse response = client.Execute(request);
  ```

  ```go Go
  package main

  import (
  	"fmt"
  	"net/http"
  	"io/ioutil"
  )

  func main() {

  	url := "https://{yourDomain}/api/v2/organizations/name/ORG_NAME"

  	req, _ := http.NewRequest("GET", url, nil)

  	req.Header.Add("authorization", "Bearer MGMT_API_ACCESS_TOKEN")

  	res, _ := http.DefaultClient.Do(req)

  	defer res.Body.Close()
  	body, _ := ioutil.ReadAll(res.Body)

  	fmt.Println(res)
  	fmt.Println(string(body))

  }
  ```

  ```java Java
  HttpResponse<String> response = Unirest.get("https://{yourDomain}/api/v2/organizations/name/ORG_NAME")
    .header("authorization", "Bearer MGMT_API_ACCESS_TOKEN")
    .asString();
  ```

  ```javascript Node.JS
  var axios = require("axios").default;

  var options = {
    method: 'GET',
    url: 'https://{yourDomain}/api/v2/organizations/name/ORG_NAME',
    headers: {authorization: 'Bearer MGMT_API_ACCESS_TOKEN'}
  };

  axios.request(options).then(function (response) {
    console.log(response.data);
  }).catch(function (error) {
    console.error(error);
  });
  ```

  ```php PHP
  $curl = curl_init();

  curl_setopt_array($curl, [
    CURLOPT_URL => "https://{yourDomain}/api/v2/organizations/name/ORG_NAME",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_ENCODING => "",
    CURLOPT_MAXREDIRS => 10,
    CURLOPT_TIMEOUT => 30,
    CURLOPT_HTTP_VERSION => CURL_HTTP_VERSION_1_1,
    CURLOPT_CUSTOMREQUEST => "GET",
    CURLOPT_HTTPHEADER => [
      "authorization: Bearer MGMT_API_ACCESS_TOKEN"
    ],
  ]);

  $response = curl_exec($curl);
  $err = curl_error($curl);

  curl_close($curl);

  if ($err) {
    echo "cURL Error #:" . $err;
  } else {
    echo $response;
  }
  ```

  ```python Python
  import http.client

  conn = http.client.HTTPSConnection("")

  headers = { 'authorization': "Bearer MGMT_API_ACCESS_TOKEN" }

  conn.request("GET", "/{yourDomain}/api/v2/organizations/name/ORG_NAME", headers=headers)

  res = conn.getresponse()
  data = res.read()

  print(data.decode("utf-8"))
  ```

  ```ruby Ruby
  require 'uri'
  require 'net/http'
  require 'openssl'

  url = URI("https://{yourDomain}/api/v2/organizations/name/ORG_NAME")

  http = Net::HTTP.new(url.host, url.port)
  http.use_ssl = true
  http.verify_mode = OpenSSL::SSL::VERIFY_NONE

  request = Net::HTTP::Get.new(url)
  request["authorization"] = 'Bearer MGMT_API_ACCESS_TOKEN'

  response = http.request(request)
  puts response.read_body
  ```
</AuthCodeGroup>

<Callout icon="file-lines" color="#0EA5E9" iconType="regular">
  **Trouvez votre domaine Auth0**

  Si votre domaine Auth0 correspond au nom de votre locataire, il sera composé de votre sous-domaine régional (à moins que votre locataire se trouve dans la région des États-Unis et ait été créé avant juin 2020), suivi de `.auth0.com`. Par exemple, si le nom de votre locataire était `travel0`, votre domaine Auth0 serait `travel0.us.auth0.com`. (Si votre locataire se trouvait aux États-Unis et avait été créé avant juin 2020, votre nom de domaine serait alors `https://travel0.auth0.com`.)

  Si vous utilisez des domaines personnalisés, il doit s’agir du nom de votre domaine personnalisé.
</Callout>

| Valeur                  | Description                                                                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `ORG_ NAME`             | Nom de l’organisation que vous souhaitez récupérer. Longueur maximale de 50 caractères.                                                    |
| `MGMT_API_ACCESS_TOKEN` | [jeton d’accès de la Management API](/fr-CA/docs/secure/tokens/access-tokens/management-api-access-tokens) avec le scope `read:organizations`. |

<div id="response-status-codes">
  ### Codes d’état de la réponse
</div>

Les codes d’état de la réponse possibles sont les suivants :

| Code d’état | Code d’erreur          | Message                                                                                                | Cause                                                                                                    |
| ----------- | ---------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| `200`       |                        | Organisations récupérées avec succès.                                                                  |                                                                                                          |
| `400`       | `invalid_query_string` | Chaîne de requête invalide. Le message varie selon la cause.                                           | La chaîne de requête n’est pas valide.                                                                   |
| `401`       |                        | Jeton non valide.                                                                                      |                                                                                                          |
| `401`       |                        | Signature invalide reçue lors de la validation du JSON Web Token.                                      |                                                                                                          |
| `401`       |                        | L’application n’est pas globale.                                                                       |                                                                                                          |
| `403`       | `insufficient_scope`   | scope insuffisant ; l’un des scopes suivants est attendu : `read:organizations`.                       | Tentative de lecture ou d’écriture d’un champ non autorisée avec les scopes fournis par le Bearer Token. |
| `429`       |                        | Trop de requêtes. Vérifiez les en-têtes X-RateLimit-Limit, X-RateLimit-Remaining et X-RateLimit-Reset. |                                                                                                          |